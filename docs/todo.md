# SkillForge ToDo

> 更新于：2026-05-17（**SYSTEM-AGENT-TYPING Phase 2 交付 = system agent 真分辨 + 监控面板 + 编辑保护**：F2 AgentList toggle "Show system agents" + 紫色 Tag + inline SystemAgentMonitorCard (cron / last_run / 7d trigger+output / Run Manually / View Sessions+Schedule deep-link) / F3 AgentDrawer system agent banner + readOnly 全 form + Delete disabled + fieldset disabled 兜底 behavior_rules+lifecycle_hooks (no Unlock per user simplify) / F5 BE GET /api/agents?agentType filter + GET /api/system-agents/monitor 跨表聚合 endpoint (5 system agent 各自 7d count from 5 产出表 + cron + lastRun) / F6 Chat send gate Alert + double-guarded. W2 mandatory fix: SessionList + Schedules useSearchParams 读 deep-link param 真消费 (F4 partial 修完整, +6 new test cases). mvn 1797/0/95 + tsc + npm build 双绿 + 21 FE tests PASS (5 file) + 0 regression + Iron Law 0 diff. 整包 SYSTEM-AGENT-TYPING active → archive/2026-05-17-SYSTEM-AGENT-TYPING/. **3 reviewer Sonnet 全 stalled 600s** (3059 行 diff watchdog) → **Opus 1M 跑通** PASS with 1 mandatory W2 fix. 上次：2026-05-17 SYSTEM-AGENT-TYPING Phase 1 交付 (commit e659b0a)：DB SQL 真活反查发现 117 outcome 标注全在 system agent / 110 user agent session 0 outcome（layer 1 覆盖断了）→ Hypothesis B 确认 starvation in `SessionAnnotationSignalService.findRecentByLimit("signal", cap*3=30)` createdAt DESC → 5 system agent cron 永远霸占最近 30 行 → user agent signal 永远到不了 first cap=10. **Fix**: (a) V89 加 `t_agent.agent_type` VARCHAR(16) NOT NULL DEFAULT 'user' + CHECK + UPDATE 5 system agent + AgentEntity 加字段 (Jackson 自动序列化无需新 DTO) + 5 Bootstrap idempotent self-heal (放 findFirstByName 之后/prompt-swap 短路之前) (b) `SessionAnnotationRepository.findRecentBySourceAndAgentType` 原生 SQL JOIN + `SessionAnnotationSignalService.findSessionsNeedingLlmAnnotation` 改 **3-tier** (user-first / system backfill / catch-all orphan fallback) + **W1 fix per-tier early dedup** (Phase 2 review java-reviewer 抓 edge case: Tier 1 全 LLM-annotated 时 guard 跳 Tier 2 → 空 queue, fix moves dedup to each tier). **真活验证**: BE 重启 + V89 migration apply + 触发 session-annotator-hourly cron 一轮 (50s 内) → t_session_annotation user agent outcome **0 → 9** (Main Assistant 5 failure / Design Agent 2 / Research Agent 1 / Session Analyzer 1, 全部首次出现). mvn 1776/0/95 BUILD SUCCESS (1775 phase 1.1+1.2 终态 + 1 W1 edge case test). Iron Law 核心 7+1 BE + 3 FE 0 diff. 3 reviewer (java/db/typescript Sonnet) 全 PASS, 0 blocker / 8 warning / 6 nit (1 mandatory W1 fix done, 7 follow-up). **Phase 2 UX 留 SYSTEM-AGENT-TYPING 包后续 PR**: AgentList toggle + AgentDrawer 锁 + Chat send gate + SystemAgents 监控面板 + BE filter endpoint）

> 上次更新（FLYWHEEL-LOOP-CLOSURE 飞轮 ⑤ A/B 自动 trigger 真闭环交付，commit `7886505`）：修 ③→④→⑤ 架构 gap (V3 attribution candidate 没 A/B endpoint / SkillDraft attribution stub / candidate_ready 没自动 trigger / system agent 没 EvalScenario) + canary logic disable (V87 cron + FE 删 embed) + V3.2 link UUID type mismatch 接通 (V88 旁路列). **Mid 1.5d brief → 用户拍板升 Full 档 ~2-3d → 实际 8 sub-batch (1.0-1.6) + Phase 2 review r1+r2 5 mandatory fix.** mvn 1750/0/89 BUILD SUCCESS (baseline 1712 + **38 真新测试**) + tsc/build 双绿 + Iron Law 核心 7+1 BE + 3 FE 全程 0 diff. 8 ratify + 11 reviewer findings 全处置 (5 fix + 1 false-pos + 1 nit backlog). **3 次 reviewer hallucination 记账** (Phase 1.3 B1 file name / Phase 1.4a+b W4 类级别 @Transactional 漏看 / V87 ✓ in diff false claim). **Phase 1.6 dogfood event 42 真跑通**: skill surface approve → SkillAbRunEntity d07f7ede 真创建 status=PENDING + transient SkillEntity #699 + baseline #700 真生成 (DB query 验); t_canary_rollout 0 new + V87 cron disabled 验过. **V1+V2+V3+V4+V5+V6 6 个版本累计完整 9 步飞轮 + 3 surface (skill / prompt / behavior_rule) + ⑤ A/B 自动 trigger** — prompt+skill 双 surface 闭环 (behavior_rule auto-trigger 留 V5.1 backlog explicit skip). 已知 follow-up: SKILL-AB-EPHEMERAL-CONSUMPTION (SkillAbEvalService.createAndTrigger 不收 scenarios) / EvalScenarioDraftRepository rename / prompt surface event 9/16 dogfood 还没跑 / abRun async eval LLM 跑完后完整 promote 链路验证）
> 规则：这里只放当前执行状态；需求和方案细节放在链接的需求包中。
> 旧版：重整前长版 ToDo 已保留在 [references/legacy-todo-2026-04-28.md](references/legacy-todo-2026-04-28.md)。

## 当前队列

| 顺序  | ID        | 标题                                | 模式   | 状态        | 优先级 | 风险   | 文档                                                        | 下一步                                   |
| --- | --------- | --------------------------------- | ---- | --------- | --- | ---- | --------------------------------------------------------- | ------------------------------------- |
| _(队列空闲)_ | — | — | — | — | — | — | — | — |

## 阻塞 / 待决策

| ID | 待决策 | 负责人 | 阻塞项 |
| --- | --- | --- | --- |
| (无) | — | — | — |

## 最近完成

| ID | 完成日期 | Commit | 交付 |
| --- | --- | --- | --- |
| **PROMPT-IMPROVER-GENESIS-BASELINE（修 flywheel A/B 全 agent 第一次都崩的架构 gap / Mid pipeline）** | 2026-05-21 | _(本次提交 pending)_ | OptEvent #118 真活 dogfood 暴露 `PromptImproverService:347` 抛 "no active prompt version" — 全部 11 个 agent (user+system) `active_prompt_version_id` 都是 NULL，整个 flywheel A/B 链路对每个 agent 第一次触发都死。修复加 **genesis 路径**：第一次某 agent 被 attribute 时自动从 `t_agent.system_prompt` 物化 `t_prompt_version(version=1, status='active', source='genesis')` + 更新 `agent.activePromptVersionId`，然后正常 A/B 跑 v1 baseline vs v2 candidate。**Race 防御**：用 `findByIdForUpdate` (PESSIMISTIC_WRITE) 锁 agent row，并发 2 个 runAbTestAgainst 第二个 re-find 看到非 null 走 skip。**Mid pipeline**: BE-Dev Opus 单 dev (50 行 + 6 test) → java-reviewer Sonnet PASS 0 blocker / 3 warning / 3 nit → 主会话 inline 加 W1 missing test (caller 显式传 baseline + virgin agent 不应 genesis) → 7/7 PASS → Judge PASS → Phase Final. **测试**: mvn 1939/0/109 (baseline 1932 + 7 新). Iron Law 全程守住 (核心 7+1 BE 0 diff / 0 schema / 0 @Transactional 既有路径变动). **已知 trade-off**: promote 后 BE restart Bootstrap 仍会 swap `.md` 覆盖优化 — 未来 Option Y (Bootstrap 退化为首次种 baseline + 后续 hands-off) 单独立项。**Backlog P3**: Layer 1 dispatcher 跳 system agent / per-agent run-now endpoint 4 模块 / attribution-curator 3 个 stuck candidate retry。 |
| **DISPATCHER-ORCHESTRATOR-REFACTOR（dispatcher 从 LLM-as-bridge 改 LLM-as-orchestrator / Mid pipeline / 主会话 r2 接手 6-fix bundle）** | 2026-05-21 | _(本次提交 pending)_ | 用户复盘上一个 PR：dispatcher "傻 wrapper" Java fan-out 不符合"管理端可配"设计理念。重构为真 agent 主导调度: 删 DispatchAttributionPatternsTool + 新 ListAttributionCandidatesTool (side-effect-free filter + sentinel reserve) + dispatcher prompt 重写 4-step (ListCandidates → outcome routing → per-pattern SubAgent dispatch → final JSON) + V94 update tool_ids 改 ["ListAttributionCandidates","SubAgent"] + maxTokens 1024→4096 + AttributionDispatcherService 拆 listAndReserveCandidates 抽 CandidateEntry/CandidateListResult record + curator constraint 5 加 infra ≥2 例外说明。**Mid pipeline**: BE-Dev Opus → 2 reviewer Sonnet 并行 r1 BLOCK 3 blocker (B1 prompt 用 prompt= 但 SubAgentTool 真读 task= silent fail / B2 V93 老 tool_ids fresh install 死须 V94 / B3 prompt final JSON 用 reserved 但 Tool 输出 reserved_count 字段飘移) + 3 mandatory warning (W1 error-skip / W2-java List.copyOf / W2-code curator constraint) → **主会话 Opus inline 接手 r2 fix** (避免 round-trip)。**真活 dogfood**: BE restart V94 Flyway apply + Bootstrap re-swap 1196 chars 新 prompt + curl POST /api/schedules/6/trigger 200 → trace `160905a0` 6.5s (LLM emit tool_use(ListAttributionCandidates) → Tool 返 {candidates:[], total_scanned:9, filtered_out:9, reserved_count:0} → LLM final JSON `reserved_count:0` 字段名 verified live)。9 patterns 全 cooldown+in-flight skip → 0 SubAgent 调，未触发 B1 task= 真活但 prompt 已改对。**Trade-off**: token cron 每次 2.4k→7-10k / 延迟 5s→20-30s 换 prompt-configurable routing + per-decision LLM observability。Iron Law: 核心 7+1 BE git diff 0 / 0 schema 触碰核心表。**测试**: mvn 1932/0/109 (net 0: 5 删 + 5 新 测试一比一替换)。**Backlog P3**: listAndReserveCandidates 直接 unit test (transitively cover 中) / concurrent REST + LLM 路径 race doc (pre-existing). |
| **ATTRIBUTION-DISPATCHER-AGENT（飞轮调度子系统真接 cron / Full pipeline / Plan r1+r2 + BE-Dev + 4 reviewer 对抗）** | 2026-05-21 | _(本次提交 pending)_ | 修 trace `7c1aa5cc` 暴露 cron #6 直接 fire curator 无 patternId 致 attribution loop 空跑 bug。新加 system agent `attribution-dispatcher` (agentId=12, twin Bootstrap pattern) + 新 Tool `DispatchAttributionPatterns` (单参 max_dispatch default=5 [1,20], try/catch W4) + V93__seed_attribution_dispatcher_agent.sql (B1 model=xiaomi-mimo:mimo-v2.5-pro / B2 tool_ids 双写 column+config / B3 agent_type='system' 显式 / B5 maxTokens=1024 / W1 execution_mode in config) + SystemAgentMonitorService 5→6 entry B4 复用 countByCreatedAtAfter 不加新 query + FE OUTPUT_LABEL `dispatches` + W2 3-级 fallback 防 BE+FE 不同步 deploy undefined。**Full pipeline**: plan-reviewer Opus 78 tool 后 socket-died → 主会话 inline 做 ground-truth verification 揪 5 blocker (haiku-4-5 model 不在 ModelConfig / tool_ids 应双写 / agent_type race / metric stage filter undercount / maxTokens 512 偏紧) + 4 warning → r2 → Judge PASS → BE-Dev Opus 6 new + 4 mod = 10 文件 / 14 新测试 PASS → Phase 3 4 reviewer 并行 (ts PASS 1 MEDIUM 3 LOW / java-design Opus PASS 1 P3 backlog SYSTEM-AGENT-BOOTSTRAP-DEDUP) / **java + db Sonnet stalled 600s watchdog "evidence ready" 后第 3 次同症状 — 主会话第 2 次接手做 review** (V93 真活 BEGIN/ROLLBACK dry-run id=11 行 + chk 全验) → Judge PASS。**真活 dogfood**: BE restart + V93 Flyway apply + Bootstrap swap 560 chars + DB 验全 land + `curl POST /api/schedules/6/trigger?userId=1 -H Bearer` 200 → trace `d27e0a09` 4.8s 跑通 (LLM tool_use → Tool 返 `{candidates_scanned:9, dispatched:0, skippedSurface:4, skippedCooldown:2, skippedActive:3}` → LLM final JSON echo) — 9 patterns 全 skip 是正确行为 (无新 eligible)。Iron Law: 核心 7+1 BE git diff 0 / 无 schema 触碰核心表 / 无 @Transactional 既有路径变动。**测试**: mvn -pl skillforge-server -am test 1932/0/109 (baseline 1918 + 14 真新) + FE tsc 0 + npm build 2 pre-existing tsc error 跟本 PR 无关。整包 active/ATTRIBUTION-DISPATCHER-AGENT/ → archive/2026-05-21-ATTRIBUTION-DISPATCHER-AGENT/。**P3 backlog**: SYSTEM-AGENT-BOOTSTRAP-DEDUP (6 Bootstrap twin 第 7 个 agent 加入前抽 AbstractSystemAgentPromptSwapper) / FE-COVERAGE-INSIGHT-PANEL (SystemAgentMonitorCard.test.tsx 加 dispatches + W2 fallback test, 当前 FE coverage baseline 不强不强制本 PR 修)。 |
| **MULTI-DIM-ATTRIBUTION（飞轮多维归因 / Full pipeline / Plan 对抗 r1+r2 + BE-Dev + java-reviewer Opus 1M + code-reviewer Opus 1M + Judge）** | 2026-05-21 | _(本次提交 pending)_ | 扩展 Optimization Loop attribution 输入维度: outcome=failure → 加 `infrastructure_failure` + `cost_high`；修 session-annotator 0-message error session 静默 skip P0 bug (synthetic agent_error signal 让 LLM annotator 有信号可标)；修 WriteOptimizationEventTool proposal_rejected 无 cooldown 致 infra failure 每小时 redispatch bug (BE-Dev bonus C-extra phase 24h auto-cooldown). `SessionPatternClusterService.MIN_MEMBERS_INFRA_OUTCOME=2` (infra 阈值降到 2) + `AttributionDispatcher.MIN_MEMBER_COUNT_INFRA=2` 同步对齐 (BE-Dev 实测发现 cluster 放过但 dispatcher 静默 drop). Filter 1 infra bypass + composeDispatchPrompt outcome 透传. session-annotator + attribution-curator system prompt md 加 STEP 2.1/2.2 (annotator) + STEP 1 fast-reject + STEP 3 cost_high 推理模式 (curator). **W1 code-reviewer 强制 follow-up 已 call-out**: 24h cooldown 对 ALL curator-written proposal_rejected 生效 (CONSTRAINT 1/2 + infra fast-reject), 操作员手动 reject 走 AttributionApprovalService.reject() 直写 entity 绕过 Tool 不受影响. **Full pipeline**: Plan r1 (Sonnet PASS_BLOCKED 2/4) → r2 全修 + BONUS cooldown → plan-reviewer Opus PASS → Judge PASS → BE-Dev Opus → Phase 3 reviewer 先 Sonnet 双 stalled 600s watchdog → **retry Opus 1M + incremental-write defense** 双双成功 → java-reviewer PASS 0/0/4nit + code-reviewer PASS 0 blocker 1 mandatory W1 docs follow-up + 3 nits → Judge 主会话 Opus → Phase Final. **Iron Law 守住**: 核心 7+1 BE git diff 0 (只有 import 行); 无 migration / 无新 endpoint / 无 FE / 无 DTO. **验证**: mvn -pl skillforge-server -am test 1918/0/109 (baseline 1903 + **15 真新测试** 4+2+3+4+2) + 35/35 regression PASS + 真活 dogfood Path 2 (OptEvent #116/117 cooldown_expires_at 设置 + 再 trigger skippedCooldown=2 验证 infra redispatch loop bug fixed). Path 1 (5/19 0-message session) 不能用真活 curl 验证因 origin=eval 不进 pipeline by design — 未来同形态 origin=user 0-message error session 会被新加 synthetic agent_error 路径捕获. 整包 active/MULTI-DIM-ATTRIBUTION/ → archive/2026-05-21-MULTI-DIM-ATTRIBUTION/. **MVP 不做** (P2 backlog): efficiency_low / latency / FE-DIM-ENTRY-NODES / 操作员 cooldown reset endpoint / multi-dim cost dashboard / 跨 tenant aggregation (待 USER-SSO-MULTITENANT). 5 nit follow-up: /tmp/nits-followup-multi-dim-attr.md (N1+N4 MIN_MEMBER constants DRY / N2 dispatcher log 分 infra vs regular / N3 cost_high 措辞 / N5 cooldown row GC backlog). |
| **FLYWHEEL-PER-RUN（飞轮 per-run 跟踪 / Mid pipeline 双 dev parallel + 2 reviewer 对抗 + r2 4 mandatory fix）** | 2026-05-20 | _(本次提交 pending)_ | 用户复盘 FLYWHEEL-FLOWCHART (commits 65c8643 + 389845d) framing：aggregate panel 不够，要"跑一次飞轮后这个任务运行到哪了 / 是否有问题"per-run tracking。本包加 `[Aggregate \| Per-Run]` mode toggle (localStorage `flywheel.mode`)。Per-run 模式：(a) 左侧 FlywheelRunsSidebar 320px 列最近 N=20 OptimizationEvent (agent name + signature snippet 60char + stage chip + age + status emoji 🔄/✅/⚠️/❌) + collapse + hideTerminal filter chip; (b) 点 sidebar run → activeRunId useState (不 persist localStorage 防 stale id)，DAG 高亮该 run currentStage 对应 step (绿 ring 替换 aggregate pulse), pre-OptEvent steps (ENTRY + step1/2/3) 灰化为 'context'，completed steps checkmark，error stage 红色覆盖 green current; (c) 选中 run 的 surface 自动 dictate display (surface tab disabled per-run+selected); (d) Drawer 内容 swap：header 加 OptEvent #N chip，content 替换为 run identity card + per-run stage info + event filter by `attr-{optEventId}` regex，footer drill-down → `?optEventId=N`; (e) `STAGE_TO_STEP` mapping 11 of 16 BE stages (5 transient/post-MVP unmapped 显式 doc'd); (f) prefers-reduced-motion fallback 全 honor。**BE 1 新 endpoint** `GET /api/flywheel/runs?agentType=&surface=&limit=&hideTerminal=` aggregate logic：3 SQL N+1-free (events + patterns batch + agents batch via findAllById), agentType→agentIds 早 resolve, errorLabel 映射 3 failure stages (proposal_rejected/candidate_failed/ab_failed), UTF-16 surrogate-safe snippet, limit clamped [1,100] default 20, hideTerminal default true。**Mid pipeline**: be-dev Opus + fe-dev Opus parallel → java-reviewer Sonnet (BE 61 mod + 5 new) + ts-reviewer Opus 1M (FE 2202 mod + 3 new ~2.7k LOC) 并行对抗 1 轮不循环 (前次 reviewer 双双 600s watchdog stalled — 网络抖动同 fe-dev 早些时撞的 529 同根因 — retry 加 incremental-write 约束后成功) → Judge 仲裁 → r2 4 mandatory bundle: be-dev 3 fix (W1 path A: 2 finders → 4 finders matrix 摆脱 Hibernate 6 IS NULL on Collection 风险 / W2 ALLOWED_AGENT_TYPES whitelist 400 BAD_REQUEST fail-fast / W3 测试 ObjectMapper findAndRegisterModules + disable WRITE_DATES_AS_TIMESTAMPS 对齐 Spring autoconfig + 8-line javadoc) + fe-dev 1 fix (STAGE_TO_STEP doc comment 列 5 unmapped stage + 8 新 types.test.ts case 验 fallback 不 crash + 1 flowchart render case unmapped stage). **Iron Law 守住**: 核心 7+1 BE + 核心 3 FE git diff 0 + 无 schema + 无 @Transactional 既有路径改动 + Jackson 契约 footgun #6 roundtrip + 真活 curl 验过 4 路径 (含 `?hideTerminal=false` Hibernate 6 path live verify + `?agentType=robot` 真返 400 W2 fix). **验证**: mvn -pl skillforge-server -am test 1902/0/109 (baseline 1894 + 8 真新 BE test: 7 ControllerTest 新 + 1 service path) + FE tsc 0 + vitest 22 passed + 1 todo (3 file: types.test.ts 8 + FlywheelRunsSidebar.test.tsx 5 + FlywheelFlowchart.test.tsx 10 含 1 unmapped stage fallback case) + 0 net regression。整包 active/FLYWHEEL-PER-RUN/ → archive/2026-05-20-FLYWHEEL-PER-RUN/。**MVP 不做** (future iter): stage transition history table (需新 schema 走 Full档) / per-run alerts WS push / cross-run diff 对比 / run-level operation buttons (cancel/retry — observability ≠ ops console)。 |
| **FLYWHEEL-FLOWCHART（飞轮工作流流程图视图 / Mid pipeline 1 dev + 2 reviewer 对抗 r1 + 1 round 5-fix bundle / Iron Law 0 触碰 / bundle 反而 -46KB gzip vs pre-flywheel baseline）** | 2026-05-20 | _(本次提交 pending)_ | 替换 FLYWHEEL-VISUAL-STATUS card-style panel 为 React Flow + dagre LR auto-layout DAG。4 类节点边框色 (AUTO 蓝 / USER 橙 / HYBRID 紫 / ENTRY 绿 / DORMANT 灰) + 运行中节点 1.5s 绿色慢闪 ring (CSS @keyframes box-shadow compositor-friendly, AUTO+HYBRID && (inFlight>0 OR cron lastRunStatus='running') 触发) + USER GATE 不闪保留静态红 PEND chip + edge `animated: true` when 相邻节点都 in-flight (dashed flow) + read-only invariant (nodesDraggable=false/connectable=false/selectable=false/Handle.isConnectable=false) + reduced-motion fallback (`.fw-node--running animation: none + outline 2px solid` + `.react-flow__edge-path animation: none !important` 覆盖 RF base.css dashdraw)。useFlywheelObservability 加 `runningByStep: Record<string,boolean>` derived field。StepCard 重构 compact-only (2 metric: in-flight + lag + inline ERR/PEND chip)。**11 文件 scope** (3 new: FlywheelFlowchart 349 LOC + FlywheelNode 66 + 测试 262 / 5 modified: pages/Insights.tsx 加 React.lazy + Suspense + FlywheelObservability/StepCard/flywheel.css/useFlywheelObservability/package.json + lock / 3 deleted: FlywheelObservabilityPanel + FlywheelTimeline + 旧测试)。**新 deps**: reactflow ^11.11.4 + dagre ^0.8.5 + @types/dagre ^0.7.54。**Mid pipeline**: ts-reviewer Sonnet PASS 0/4 + code-reviewer Sonnet NEEDS_FIX_R2 (warning-only) 2/4 → Judge 主会话 → r2 5 mandatory fix bundle (F1 reduced-motion edge fallback / F2 测试 className 对称 + waitFor / F3 it.todo 标 reduced-motion gap / F4 nodesFocusable=false 让键盘 Tab 走 inner `<Link>` / F5 React.lazy + Suspense lazy-load Flywheel tab)。**Bundle 净优化**: main chunk gzip 1361.93 KB → 1243.60 KB **-118KB** (lazy 把 reactflow+dagre+attribution chunk 推到 tab activation)，相比 pre-flywheel-flowchart baseline 1289.98 KB 反而 -46KB。验证: tsc 0 + vitest 4 passed + 1 todo + 0 net regression (22 failed / 269 passed 跟 baseline 一致) + npm build 2 pre-existing tsc error 不变。3 warning ratify fold nits (ts W3 reactflow CSS 全局 import unavoidable / ts W4 now tick metricsByStep new ref dagre re-run pre-existing 15-node sub-1ms / code WARN-1 package-lock 主会话 commit 时补 add)。整包归档 active/FLYWHEEL-FLOWCHART/ → archive/2026-05-20-FLYWHEEL-FLOWCHART/。Iron Law 全程守住 (核心 7+1 BE + 核心 3 FE git diff 0)。 |
| **FLYWHEEL-VISUAL-STATUS（飞轮可观测面板 / Full pipeline 1.0 取证 + 2 dev parallel + 3 reviewer 对抗 r1+r2 / 8 mandatory fix bundle / 0 Iron Law 触碰）** | 2026-05-20 | _(本次提交 pending)_ | observability framing：Insights 第 5 tab `flywheel`；AUTO/USER/HYBRID/ENTRY 4 类节点 (15 step: E1-E4 entry + 1-6 + G1-G3 gate + 7-9 dormant)；每节点 5 维 metric (in-flight / lastActivity / lag / recentError / todayAggregate)；5 健康颜色 (healthy/warn/stale/dormant/empty) + H/W/S/D/E 字母 a11y fallback (色盲)；双 tab agentType (user/system) × surface (skill/prompt/behavior_rule) + ARIA tablist；panel 只读 drill-down 跳现有 page。1B: Insights `?tab=&stage=&agentId=` URL-driven 真消费 + SkillList `?skillId=&panel=evolution\|abtest\|canary` auto-open + SessionList `?annotated=true` client-side filter + SkillDrafts `?status=` 2-way sync。2B BE 3 endpoint: `/skills/abtest?agentId=&status=&surfaceType=&page=&size=` global + `/canary/rollouts?agentId=` optional + `/skill-drafts?source=` filter。3 reviewer (java Sonnet + ts Opus 1M + code Opus 1M) 对抗 r1：java PASS 0/4 / ts NEEDS_FIX_R2 3/7 / code NEEDS_FIX_R2 2/8 → r2 mandatory bundle 8 fix (be-dev 1: NULLS LAST PG-only → createdAt 兜底; fe-dev 7: Date.now hoist out useMemo / FilterFormValues import 删 / `?stage=` 真消费 (PRD R3) / 真接 BE source endpoint / health dot 字母 a11y / ARIA tablist / SkillAbRunStatus 加 SKIPPED + ts-W4 stash leakage revert)。验证: mvn 1865/0/109 + tsc 0 + vitest 9/9 + Iron Law 7+1 BE + 3 FE 0 diff + BE 真活 curl 3 endpoint 验过 (PID 85058 重启后)。9 warning ratify fold nits-followup。整包归档 archive/2026-05-20-FLYWHEEL-VISUAL-STATUS/。 |
| **SYSTEM-AGENT-TYPING Phase 2（system agent UX 分辨 + 监控 inline + 编辑保护 + Chat gate + deep-link / Full / 6 phase + Phase 2 Opus review 1 mandatory W2 fix + W3 confirm）** | 2026-05-17 | _(本次提交 pending)_ | F2 AgentList toggle + Tag + inline SystemAgentMonitorCard (cron/last_run/7d/Run Manually/View Sessions+Schedule) / F3 AgentDrawer banner + 全 form readOnly + Delete disabled + fieldset disabled 兜底 behavior_rules+lifecycle_hooks (no Unlock per user simplify) / F5 BE GET /api/agents?agentType filter + monitor endpoint (5 agent 跨表聚合) / F6 Chat send gate. W2 fix: SessionList+Schedules useSearchParams 读 deep-link param 真消费. mvn 1797/0/95 + 21 FE tests + 0 regression + Iron Law 0 diff. 整包 archive. **3 Sonnet reviewer 全 600s stalled (3059 line diff watchdog) → Opus 1M 跑通**. |
| **SYSTEM-AGENT-TYPING Phase 1（agent_type 字段 + session-annotator user agent outcome 覆盖 = 飞轮 layer 1 root cause 真闭环 / Full / 6 phase + Phase 2 review 1 mandatory W1 fix）** | 2026-05-17 | `e659b0a` | DB SQL 真活反查 117 outcome 全 system agent / 110 user agent session 0 outcome (Hypothesis B starvation in `findRecentByLimit("signal", cap*3=30)` createdAt DESC). **Fix**: V89 加 `t_agent.agent_type` + CHECK + UPDATE 5 system + AgentEntity 字段 + 5 Bootstrap idempotent self-heal (放 findFirstByName 之后/prompt-swap 短路之前); `SessionAnnotationRepository.findRecentBySourceAndAgentType` 原生 SQL JOIN + `SessionAnnotationSignalService` 改 3-tier (user-first / system backfill / catch-all orphan) + W1 fix per-tier early dedup. **真活验证 50s 内 user agent outcome 0 → 9** (Main 5 failure / Design 2 / Research 1 / Session Analyzer 1, 全部首次). mvn 1776/0/95 BUILD SUCCESS (baseline 1775 + 1 W1 edge case test). Iron Law 核心 7+1 BE + 3 FE 0 diff. 3 reviewer (java/db/ts Sonnet) PASS 0/8/6 (1 W1 done, 7 follow-up). **Phase 2 UX 留同包后续 PR**. |
| **FLYWHEEL-LOOP-CLOSURE（飞轮 ⑤ A/B 自动 trigger 真闭环 / Mid → Full / 8 phase + Phase 2 r1+r2）** | 2026-05-17 | `7886505` | 修飞轮 ⑤ A/B 4 缺口 + canary logic disable + V3.2 link 接通: (a) **F1 双 endpoint** `POST /api/agents/{id}/prompt-versions/{versionId}/run-ab` + `POST /api/skills/{parentSkillId}/abtest-from-draft` + service fan-out 真 body. (b) **F2 SkillDraft sync LLM fill** `createDraftFromAttribution` 加 `generateCandidateSkillMdFromAttribution` (跟 `SkillDraftService.extractFromRecentSessions` 同款 xiaomi-mimo/mimo-v2.5-pro/maxTokens=4000/REQUIRES_NEW/audit-trail rethrow). (c) **F3 EventListener** 新 `OptimizationEventStageChangeEvent` record + `OptimizationEventAutoTriggerListener` (@Async + @TransactionalEventListener(AFTER_COMMIT) + @Transactional(REQUIRES_NEW) Spring 6.1+ P11 教训) + 4 处 fire + 4 道闸 + surface 路由 (prompt/skill 真调 service, behavior_rule explicit skip V5.1 backlog) + broadcastAbFailed 兜底. (d) **F4 ephemeral scenario fallback** (ratify #4): 反查 V88 sidecar UUID → OptimizationEvent.patternId → pattern.members 3 session → extractFromSession 生 status='ephemeral' + 跑完 `EphemeralScenarioCleanupService` sibling bean @Transactional(REQUIRES_NEW) cleanup (W5 fix 修 self-invocation 陷阱). (e) **V3.2 link 接通**: V88 加 `candidate_prompt_version_uuid` + `candidate_skill_draft_uuid` VARCHAR(36) 旁路列 (旧 Long 列 type mismatch — UUID surface 留 null + 向后兼容). (f) **ALLOWED_TRANSITIONS** 加 `ab_passed → promoted` 直接边跳 canary. (g) **AbEvalPipeline.run 5-arg overload** (`abRun`,`candidate`,`baselineVersion`,`agent`,`scenarios`) — baseline 跑活 eval 不依赖 `EvalTaskEntity` (additive). (h) **`promoteDraftToTransientSkill` helper** — transient SkillEntity name 后缀 `_candidate_<8char>` + source="attribution_ab_transient" 双 pivot (ratify #7-B path (a); SkillEntity 无 status 字段). (i) **Loser cleanup hook** `SkillSelfImproveLoop.cleanupAttributionLoserCandidate` 在 onAbCompleted loser 分支 delete transient + paired baseline (防 DB leak). (j) **FE canary disable**: SkillAbPanel + BehaviorRuleEvolutionPanel 删 `<CanaryPanel>` embed + orphan helper + agentNumericId ripple. (k) **V87 migration** disable metrics-collector-hourly cron + V88 加 sidecar UUID 列. **8 ratify** (canary disable / SkillDraft fill 跟 SkillDraftService.extractFromRecentSessions 修正 V3.1 假设错 / @EventListener / system agent fallback / V88 旁路列 / Phase 1.3-1.4 boundary placeholder / Phase 1.4 升 Full 档 / SkillEntity transient identity name 后缀). **Full pipeline**: Mid 1.5d brief 起 → dev NEEDS_CONTEXT 调研 + 用户拍板升 Full → **8 sub-batch** (1.4a/b/c/d batch1+2a/e/f/g, dev context risk-averse honest 拆批 + grep self-check 5 步) + mid-review 2 次 (Phase 1.3 + 1.4a+b) + **Phase 2 r1 3-reviewer 并行** (java + ts + code Sonnet diff-in-prompt) + Judge Opus 仲裁 → **r2 5 mandatory mechanical fix** (F1 deprecated getStatusCodeValue / F2 ObjectMapper static final / F3 controllers generic msg + log detail / F4 detached entity → pass ID + async reload / F5 删 catch dead delete @Transactional rollback 接管). **11 reviewer findings**: 5 r2 fix + W1 自然消除 + W2 message-text "not found" lock + **W4 reviewer hallucination 第 2 次** (类级别 @Transactional 漏看 line 69, dev grep 验证 confirm) + **W6 nit follow-up backlog** EvalScenarioDraftRepository rename. **3 次 reviewer hallucination 记账** (Phase 1.3 B1 不存在 file name / W4 类级别 @Transactional 漏看 / V87 ✓ in diff false claim) — 后续加 reviewer "grep self-check + 类级别 + import 全文 scope" 强制规则. **Iron Law 全程守住**: 核心 7+1 BE 文件 + 核心 3 FE 文件 git diff = 0; V4 OptimizableSurface 接口 0 修改; persistence-shape-invariant + identity-column-on-rewrite 未触发. **Phase 1.6 dogfood event 42 真跑通**: skill surface approve → candidate_ready (V88 candidate_skill_draft_uuid=`a9467836-...`) → SkillDraft AttrSkill6_42 LLM fill (hint=593chars/triggers=111) → AFTER_COMMIT listener → promoteDraftToTransientSkill (SkillEntity #699 + baseline #700) → SkillAbRunEntity `d07f7ede-...` PENDING 真创建; psql 验 t_canary_rollout 0 new + V87 cron enabled=false. **已知 follow-up**: (a) **SKILL-AB-EPHEMERAL-CONSUMPTION** backlog (SkillAbEvalService.createAndTrigger 不收 scenarios → 加 createAndTriggerWithScenarios overload 真用 ephemeral); (b) EvalScenarioDraftRepository rename (W6 nit); (c) prompt surface event (9/16) dogfood 还没跑; (d) abRun async eval LLM 跑完后完整 promote → `skill_auto_upgraded` WS broadcast 链路验证. **MVP 不做**: behavior_rule auto-trigger A/B (V5.1 backlog) / lifecycle_hook (V5+ ROI 不够) / cron auto-approve. mvn -pl skillforge-server -am test **1750/0/89 BUILD SUCCESS** (+38 真新测试 vs baseline 1712); tsc + npm build 双绿 + vitest SkillAbPanel 2/2 PASS; migrations V87 + V88. |
| **SKILL-AB-MULTITURN-FIX** | 2026-05-13 (fix) / 2026-05-16 (gap-fill test + archive) | `6a78dd5` → `ca6a58d` | **Skill A/B 多轮评测修复（EVAL-DYNAMIC-USER-SIM Phase 1 拆分包 / Mid pipeline）**：fix `SkillAbEvalService.runAbTestAsync` 当 scenario `conversationTurns` non-empty 时不再 fallback single-turn（去除 EVAL-V2 M2 R5 warning 路径），而是 dispatch 到新加私有 `runMultiTurnScenario(...)` (`SkillAbEvalService.java:880-1021`)：复用 `sandboxFactory.buildSandboxRegistryWithSkills(...)` 注入 candidate skill (`:887-888`)；同一 eval session 跨 turn 累积 history；per-turn 25s timeout + case-level 90s budget；finally `cleanupSandbox(...)`。Judge 走 `EvalJudgeTool.judgeMultiTurnConversation(...)` (`:583`)。Per-scenario error isolation 由 `runCandidateEvalSet` `catch (Exception)` (`:603-607`) + `runMultiTurnScenario` 内部 catch (`:1013-1017`) 双层兜底，AB run 整体 status 仍 COMPLETED 不变 FAILED。单轮 A/B 行为零漂移。**2026-05-16 发现 docs/code skew**（commit `6a78dd5` 当日 land 但状态滞留 `design-draft`）→ 闭合验收 #6 explicit test `multiTurn_perScenarioErrorIsolated` (mock multi-turn judge 抛 RuntimeException → 验 `:603-607` 外层 catch → ERROR scenario + COMPLETED AB run) + 归档。**测试新增 3**：`SkillAbEvalServiceMultiTurnTest` (multi-turn candidate registry+judge / single-turn regression / per-scenario error isolation)。`mvn -pl skillforge-server -am test` 1671/0/0/84 BUILD SUCCESS；核心 7+1 文件 0 diff。 |
| **ATTRIBUTION-AGENT（飞轮 V3）** | 2026-05-15 | `dcdefd0` → `99df219` (8 commits) | attribution-curator agent 自动归因 V1 cluster pattern + 写 proposal_pending + operator approve/reject/retry + 自动接 SkillDraftService.createDraftFromAttribution / PromptImproverService.startImprovementFromAttribution 触发 candidate 生成。V80 `t_optimization_event` 18 col + 4 index + FK CASCADE。V81 seed attribution-curator agent (owner_id=1 + is_public=TRUE) + ScheduledTask cron `0 15 * * * *`（半点错开 V75:00 / V79:30）。**核心 Service 4 个**：AttributionDispatcherService（5 阶段 filter: surface allowlist / member≥3 / cooldown 24h / ACTIVE_STAGES / cap 5）+ dispatch_initiated sentinel race-window 防御 + @Scheduled cleanup TTL=2h，AttributionApprovalService（approve/reject/retryCandidateGeneration + 12-edge ALLOWED_TRANSITIONS state machine + validateTransition），ProposeOptimizationTool（sentinel UPDATE 路径 preserve createdAt），AttributionEventBroadcaster（per-stage WS broadcast type=attribution_event_updated 用 UserWebSocketHandler raw WS 非 STOMP）。**Service 接入 2 个**：SkillDraftService.createDraftFromAttribution + PromptImproverService.startImprovementFromAttribution，都 `@Transactional(REQUIRES_NEW)` 防 rollback-only 污染外层 approve()。**REST controller** 5 endpoint (GET list+filter+total / GET detail / POST approve/reject/retry) + findFiltered @Query 条件 null binding 单 finder 真分页。**FE OptimizationEvents page** (Pending Approvals queue + Recent Events timeline + filter bar + Drawer 13 字段 + Approve/Reject/Retry modal + WS subscribe queryClient.setQueriesData 增量 patch 不全 refetch) + SkillEvolutionPanel embed in Drawer + Layout sidebar entry "Optimization"。**ratify 2 锁定**（tech-design §6 + §8 变更记录）：(1) PromptImprover BYPASS agent-level cooldown（attribution 路径不调 checkEligibility，仅 V3 24h pattern-level cooldown + risk gating）(2) dispatcher race window 加 dispatch_initiated sentinel（chatAsync 前先 save sentinel，ProposeOptimizationTool 后 UPDATE→proposal_pending）。**Mid pipeline 5 phase + 5 round adversarial review + 3 BLOCKER mandatory fix**：Phase 1.2 → java + code reviewer 抓 `@Transactional` on dispatchPendingPatterns rollback-only contamination → 删 @Transactional + repo `findByPatternIdAndStageIn` 单 EXISTS query 替 6-stage for-loop N+1；Phase 1.3 → java + code reviewer 抓 approve() catch 块 candidate_failed 写不进 DB（子 service REQUIRED JOIN 外层 tx，setRollbackOnly 污染）→ 子 service 改 REQUIRES_NEW + findByPatternIdAndStage @Query ORDER BY createdAt ASC + STAGE_CANDIDATE_FAILED 加 ACTIVE_STAGES；Phase 1.4 → java + code reviewer 0 BLOCKER 3 WARNING → broadcaster pre-commit trade-off 文档化 + findFiltered Page<> 真 total + countQuery；Phase 1.5 → ts-reviewer 0 BLOCKER 3 WARNING → navItemActive exact-or-subpath match + 4 字段加 `| null` + 渲染处 null guard。**Iron Law 全程 hold**：核心 7+1 文件（SessionEntity / SessionMessageEntity / ChatService / SessionService / CompactionService / AgentLoopEngine / Message / GetTraceTool）+ 核心 FE 3 文件 git diff = 0。**验证**：`mvn -pl skillforge-server -am test` Tests run **1608**, Failures 0, Errors 0, Skipped 80 → BUILD SUCCESS 32s（baseline 1547 V2 完工 + 61 真测试新增分布 Phase 1.1 IT 4 + 1.2 service+tool 23 + 1.3 approval+draft+improver+扩展 19 + 1.4 controller+cleanup+retry 13 + 1.4 fix 2）；`cd skillforge-dashboard && npx tsc --noEmit && npm run build` 双绿 EXIT=0 in 652ms；BE 真启 server PID up：`GET /api/attribution/events?stage=proposal_pending` HTTP 401（endpoint 路由通 + auth gate 工作 = 接线完整，401 而非 404 是关键证据）。**deferred backlog**：(a) PRD 验收点全闭环 dogfood spot-check 需用户真跑 cron + approve real pattern（V1 数据已就绪 6+ pattern in `t_session_pattern`）；(b) MEDIUM orphan sentinel 双发 race 已加 cleanup 但理论 race 仍存（concurrency_policy='skip-if-running' 已挡 cron-vs-cron）；(c) WS 无断线重连 → backlog 重构 useUserWebSocket hook w/ backoff；(d) WS double connection per page-visit → 同 WS hook 重构一并解决；(e) Layout WS payload 缺 surfaceType+agentId for dashboard 累计 KPI 不查 detail；(f) retry 计数无上限多 click 浪费 LLM；(g) DTO record null fallback "null" 字面量已修但 W2 nullable type contract 仍 partial；(h) tech-design §1 stage 命名 candidate_created→candidate_ready legacy alias 保留；nit followup 累计 12+ 项 `/tmp/nits-followup-attribution-agent-phase1.{1,2,3,4,5}.md`。 |
| **SKILL-CANARY-ROLLOUT（飞轮 V2）** | 2026-05-15 | `d19f1e3` → `32b2e16` (8 commits) | Skill 灰度（架构保留 + 默认一刀切 rolloutPercentage=100）+ 生产指标回流。**6 phase + 3 mandatory fix round + Full pipeline TeamCreate 流程严格遵守**：V77 schema（t_canary_rollout partial UNIQUE + t_canary_metric_snapshot） + V78 SkillEntity 加 rolloutStage/rolloutPercentage 2 列 + V79 seed metrics-collector system agent (owner_id=1 新约定) + hourly ScheduledTask。**CanaryAllocator** runtime 分流（sessionId hash % pct）注入点是 server 层 `DefaultSessionSkillResolver` 用 skill **name** 抽象（Phase 1.0 push back 修正了原 tech-design 误以为在 AgentLoopEngine 用 id 的 30 行改动 → 实际 AgentLoopEngine 改 ≤1 行只扩接口签名加 sessionId 透传），Iron Law persistence-shape-invariant + identity-column-on-rewrite 两条不变量 0 触。**CanaryRolloutService** 5 lifecycle + Controller 7 REST endpoint（startCanary / stepUp / publish / rollback + GET ×3）+ CanaryStateException → 409 / DataIntegrityViolationException → 409 / IAE → 400 / NSEE → 404。**CanaryMetricsService** hourly recompute V69 dogfood 同款（V1 W2 教训 native ON CONFLICT DO NOTHING **不再用** saveAndFlush + catch DIVE PG aborted-tx 模式）+ 4 维 sample/success/failure 聚合 + auto-rollback signal (fail_rate_ratio > 1.5 && sample > 50)。**outcome 4 值映射**（§6.1 ratify 2026-05-15 锁）：partial_success → success_count / cancelled → failure_count / 全 4 类入 sample_size。**empty-window snapshot 写 0/0/0**（§6.2 ratify 2026-05-15）支持 FE time-series 连续渲染。**FE CanaryPanel** 4 区（rollout gauge + 4-dim bar + outcome 分布+ fail_rate_ratio chip + low-sample warning + 操作按钮 stage-gated）+ Modal.confirm on publish/rollback + 嵌入 SkillAbPanel。**Full pipeline 3 phase Review + 3 mandatory fix**：Phase 1.2（CanaryAllocator + SessionSkillResolver + AgentLoopEngine 1 行）→ java + code reviewer 对抗 → W1 字段注入修构造器注入 + W2 catch 收窄 DataAccessException + W3 测试加 case；Phase 1.3（Service + Controller）→ java + code → DataIntegrityViolationException → 409 真 mandatory；Phase 1.4（MetricsService + Tool）→ java + code → autoRollbackCheck @Transactional(REQUIRES_NEW) 隔离防 tx-poisoning；Phase 1.5（FE）→ ts + code → metricsQuery.isError silent-failure HIGH fix + dead skillId prop。tech-design §6.1/§6.2 + §0.3 + §5 全部 Phase 1.0 BE-Dev push back 修正后 ratify。验证：`mvn -pl skillforge-server -am test` **1547/0/0/76 BUILD SUCCESS in 27s**（baseline 1449 V1 完工 + 98 真测试新增）；`cd skillforge-dashboard && npx tsc --noEmit && npm run build` 双绿 EXIT=0；BE 真启 V77/V78/V79 Flyway 应用 + RecomputeMetricsTool registered + 4 ScheduledTask enabled + `GET /api/canary/rollouts?agentId=1` HTTP 200 `[]` 验通；`git diff` 对 7 核心文件（SessionEntity / SessionMessageEntity / ChatService / SessionService / CompactionService / AgentLoopEngine 1 行接口扩展 / SkillAbEvalService）— AgentLoopEngine 仅 1 行（signature 透传 sessionId），其它 6 个 0 行；Iron Law 守住。**已知 follow-up**：(a) PRD 验收点 V1 dogfood 1 周 spot-check 仍待数据；(b) V2 期间累计 nit followup `/tmp/nits-followup-skill-canary-rollout.md`（listQuery 无 retry / 无 30s polling / dead code java W3 / outcome decision text styling / `<Tag color={hex}>` AntD preset / pre-existing `@RestControllerAdvice` 缺失等约 10 项）。**MVP 不做**：cross-agent canary 协调 / multi-tenant / non-skill surface（prompt/behavior_rule 等留 V4）。 |
| **PROD-LABEL-CLUSTER（飞轮 V1）** | 2026-05-14 | `bff5ae3` → `807da2e` (9 commits) | 生产 session 自动标注 + 失败聚类 MVP。V69 memory-curator dogfood 同款"1 ScheduledTask + 1 agent + 3 tool orchestrate"模式；3 张新表 (`t_session_annotation` + `t_session_pattern` + `t_pattern_session_member`，V74 schema + V75 t_agent + t_scheduled_task seed + V76 GetTrace 接入)；`session-annotator` system agent (owner_id=1 新约定) + 3 tool (DetectSignalAnnotations 复用 TraceScenarioImportService 6 reason 检测 / AnnotateSession 持久化 outcome + suspect_surface llm 判断 / RecomputeClusters 4 维 bucket 聚类)；InsightsController 2 endpoint + Dashboard `/insights/patterns` 页 (PatternList + drill-down Drawer)。**6 phase 渐进 commit**：1.0 证伪 + 红测试 + 3 push back 修 (`52867d0`) → 1.1 schema+entity+bootstrap+JPA IT (`8358681`) → 1.2 Signal Service + Tool + detectReasons 抽 public static (`076ab5e`) → 1.3 LLM Service + Tool + V76 GetTrace 接入 (`376a6e8`) → 1.4 Clustering + InsightsController (`4db5125`) → 1.5 Dashboard (`3cbf6f3`)。**Phase 2 Mid review** 1 轮 java + ts + db 3 reviewer 并行对抗 → Judge 主会话升 W2 为 Blocker (PG aborted-tx 静默丢数据) → BE-Dev 一次修完 (`86d4057`，ON CONFLICT DO NOTHING RETURNING id)。**V2-V6 整体方案** B 级升级 commit (`807da2e`)。验证：`mvn -pl skillforge-server -am test` **1449/0/0/71 BUILD SUCCESS in 28-32s** (baseline 1402 + 47 真测试新增)；`cd skillforge-dashboard && npm run build` EXIT=0；`git diff bff5ae3..HEAD -- '<6 核心文件>'` **0 行**（SessionEntity / SessionMessageEntity / ChatService / SessionService / CompactionService / AgentLoopEngine 零改动）；GetTraceTool 也 0 行改动（复用，不动）。**已知 follow-up**：(a) PRD 验收点 8 "跑 1 周 dogfood spot-check 20 条 LLM outcome 准确率 > 70%" 等服务器真跑（开 V2 SKILL-CANARY-ROLLOUT 前必跑通）；(b) 13 nit warning（BE W1/W3/W4/W5 + FE W1-W6 + DB W1-W4）记 `/tmp/nits-followup-prod-label-cluster.md`，V2 视情况修；(c) Docker 不在本机 → 4 个 SessionAnnotationPersistenceIT 跟项目其它 14+ AbstractPostgresIT 同款本机 skip，CI 跑。 |
| **MULTIMODAL-MVP Phase 1.5 redesign + Phase 2 thumbnails** | 2026-05-14 | _(本次提交)_ | **Phase 1.5 — 设计反转**（基于 Phase 1 实跑反馈）：去掉 `t_agent.multimodal_model_id` 独立字段（V72 DROP COLUMN），agent 只一个主 `modelId`；BE `LlmModelsController.ModelOption` 加 `supportsVision: boolean`（按 `LlmProperties.providers.<n>.vision-models` 白名单算）；`LlmProperties.supportsVision` 同时接 prefixed `xiaomi-mimo:mimo-v2.5` 与裸 `mimo-v2.5` 两种形式（修 Phase 1 上线时 prefix mismatch silent 401 bug）；ChatService 去 effective-model 切换路径，只在含附件 turn 做 `MultimodalNoVisionException` defense-in-depth；ChatController upload gate `requireVisionCapableModel` 检 `agent.modelId` 是否 vision-capable，409 错误码 `MAIN_MODEL_NOT_VISION_CAPABLE`；application.yml 加 `claude.vision-models: [claude-sonnet-4-20250514]` + `xiaomi-mimo.vision-models: [mimo-v2-omni, mimo-v2.5]`；FE AgentDrawer 删 Multimodal model picker，主 Model picker option 上对 `supportsVision=true` 的渲染紫色"多模态" Tag chip；Chat.tsx `multimodalEnabled = visionCapableSet.has(activeAgent.modelId)`；ChatWindow tooltip 改"请把 agent 的主模型切换为多模态模型"；409 message.error JSX 跳转链接同步更新；删 11 obsolete tests (5 AgentService + 6 effective-model) + 改 6 controller gate tests + 3 capability check tests + FE AgentDrawer test 改 "no multimodal picker" assertions。**Phase 2 — 真缩略图**：BE 新 `GET /api/chat/attachments/{id}/data?userId=&sessionId=` endpoint stream 二进制 + Content-Type from mime + Cache-Control private 1d + Content-Disposition inline + 严格 ownership (attachment→session→user) 404/403 (新 6 case `ChatControllerAttachmentDataTest`)；`ChatAttachmentService.findReadable` + `readBytes` 配套 helper。FE 新 `AttachmentThumbnail.tsx`：image_ref → axios `responseType:'blob'` fetch + `URL.createObjectURL` + AntD `Image` (max 200×200 + click lightbox)，pdf_ref → AntD `Tag` + FilePdfOutlined icon + filename + page count chip 点击新 tab open，loading 骨架 + failed fallback chip + useEffect cleanup `URL.revokeObjectURL`；`useChatMessages.normalizeMessages.extractBlocks` 收集 `attachmentRefs: ChatAttachmentRef[]`（image_ref / pdf_ref → kind/attachmentId/filename/pageCount），ChatMessage 类型加 `attachments?: ChatAttachmentRef[]`，ChatWindow 在 user bubble msg-body 头部渲 `.msg-attachments` flex row；解决纯附件 user message（无文字）也保留行；CSS `.msg.user .msg-attachments` 右对齐 flex + 8px gap + 缩略图 wrap + `@keyframes pulse` skeleton。**附带 bug 修复**：(a) Chat.tsx doSend 带附件 turn 不做乐观插入（旧 string-only dedup vs 新 ContentBlock[] WS payload 导致重复气泡）；(b) 数据库脏 V70 checksum mismatch (`491892500` vs `781974161`) 用 standalone zonky postgres + 4 句 SQL 在场升级 t_chat_attachment.created_at/bound_at → TIMESTAMPTZ + 加 FK CASCADE + 改 flyway_schema_history checksum (保留 2 条昨天测试 attachment row)；(c) Phase 1 行内 emoji+filename string injection 已替换为真 thumbnail。验证：`mvn -pl skillforge-server test` **1330/0/67** BUILD SUCCESS (+6 新增 attachment data test) + `npx tsc --noEmit` clean + `npm run build` EXIT=0 + Server PID 9393 端到端：curl `Authorization: Bearer` 拉昨天测试图返 200 / `Content-Type: image/png` / 181904 bytes；dashboard 真跑用户确认缩略图 + click lightbox UX OK。**完整多模态后续方向**：见"暂缓" 区 `AUDIO-INPUT-MVP` / `MULTIMODAL-VIDEO-FILE` / `MULTIMODAL-VIDEO-LINK` / `MULTIMODAL-WORD-EXCEL` / `MULTIMODAL-PDF-SCAN-FALLBACK` / `MULTIMODAL-IMAGE-COMPRESSION` / `MULTIMODAL-ATTACHMENT-CLEANUP` 等候选。 |
| **MULTIMODAL-MVP Phase 1** | 2026-05-14 | `79a270e` | 已交付图片+PDF 上传 + agent 独立配置 `multimodalModelId` + Chat 上传按钮 gate（disabled + tooltip + 跳转链接）+ multimodal turn 切 effective model + provider vision capability check 显式 `MULTIMODAL_MODEL_NO_VISION_CAPABILITY` 错误。V70 `t_chat_attachment`（FK CASCADE + TIMESTAMPTZ）+ V71 `t_agent.multimodal_model_id`。Core 新增 `MessageMaterializer` interface 让 engine 边界材质化 image_ref→image base64（B2 持久化 shape 不变量保护）。Magic bytes 校验（PNG/JPEG/WebP/PDF signature 检测）防 Content-Type spoofing。`OpenAiProvider` 真序列化 image_url block，不再 silently drop。FE AgentDrawer 加 Multimodal model picker；ChatWindow attach button `aria-disabled`（不剥夺键盘 tab）+ Tooltip 跳转；chip 显示 type/size；Chat.tsx 真调 `uploadChatAttachment` per file → `sendMessage(attachmentIds[])`；409 错误 message.error 含 JSX 跳转链接。**Full pipeline retroactive r1+r2**：WIP 937 行 + 新功能 ~600 行 → java-reviewer r1 NEEDS_FIX_R2（B1 magic bytes + B2 persistence-shape 违反）+ ts-reviewer r1 PASS_WITH_WARNINGS（W1/W2 spec partial gap + W4 a11y）→ Judge 主会话 Opus 仲裁 → r2 fix BE 9 文件 + FE 3 fix → 主会话 Phase Final 亲跑全量验证 `mvn -pl skillforge-core,skillforge-tools,skillforge-server -am test` **1335 run / 0 fail / 67 skipped** BUILD SUCCESS in 25.3s + `npx tsc --noEmit` clean + `npm run build` EXIT=0；31 净新增测试（BE 28 + core 5 - removed legacy 2）；browser e2e 用户手动验证。**注意 (2026-05-14 当天后续 Phase 1.5 redesign 已部分推翻)**：multimodalModelId 独立字段已被 V72 DROP（用户实跑反馈 UX 反着了 — 应该 FE picker 只列 vision-capable 而非 BE 兜底拒），effective-model 切换也已撤回。Phase 1 其它部分 (V70 attachment 表 / MessageMaterializer / magic bytes / OpenAiProvider 序列化) 保留。 |
| **SKILL-SECURITY-SCAN** | 2026-05-13 | _(本次提交)_ | 已交付 Skill 安装时快速静态安全扫描：新增 `SkillSecurityScanner` 规则扫描器，接入 `SkillImportService.importSkill(...)` copy/upsert/register 前置路径；`ImportSkillTool` 增加 `allowMediumRisk`；`HIGH` 阻断，`MEDIUM` 默认阻断但可显式 override，`LOW` 放行并在 `ImportResult.scanWarnings` 返回；batch import 单个 blocked skill 进入 failed bucket 且其它 skill 继续。验证：定向 34/0/0；server reactor 1304/0/0/67；后端已重启 PID 78771。 |
| **REMINDER-TODOWRITE-SOURCE** | 2026-05-12 | `ecbd775` | 已交付 TodoWrite ↔ system-reminder 闭环：新增 `TodoListSource implements ReminderSource`，从 `TodoStore` 读取当前 session todos，按 in_progress / pending / completed 分组注入 `<system-reminder>`；注册顺序为 ContextUsage → TodoList → MemoryAge → FileActivity；默认 `skillforge.reminder.todo-list.enabled=true`、每 turn emit、最多 20 条；新增 `TodoListSourceTest` 覆盖分组、skip、截断和 debounce。 |
| **MEMORY-LLM-SYNTHESIS（LLM 驱动梦境 / 完全 dogfood option A）** | 2026-05-11 | _(pending)_ | LLM 经 SkillForge 自身 P12 ScheduledTask + 'memory-curator' system agent + 4 Agent Tool 跑夜间梦境整理，4 类 proposal (dedup/reflection/optimize/contradiction) 经 t_memory_proposal + 人审 gate；V68 schema 17 col + 4 索引 + GIN source_memory_ids；V69 seed agent (id=6, 4423 char system prompt) + ScheduledTask (id=3, daily 04:30, enabled=false 观察期) + SESSION_END hook 推 WS broadcast；MemoryProposalService approve 4 类分支 + reject/edit(PATCH)/revert/auto-archive/contradiction-pick 单 step + PESSIMISTIC_WRITE 双锁；FE Memory 加 Pending Reflections tab + 4 React 组件 + TanStack Query；dogfood platform 修补：D22 SubAgent fan-out exemption (depth=0 only) / SYSTEM task partial perm (toggle enabled allow, delete/edit deny) / ListActiveUsers filter userId>0 / CreateMemoryProposal SYSTEM context cross-user 豁免 / listForUser 汇入 SYSTEM tasks + isSystem chip / listProposals 返 raw array。Pipeline 全程：r1 17 决策 ratify → r2 PASS_WITH_NITS → dogfood pivot D18-D23 ratify → Phase 2 三 BE Dev + 1 FE Dev 并行 → Phase 3 java+ts reviewer 对抗 + Judge Opus (r1 NEEDS_FIX_R2 → r2 PASS) → Phase Final mvn 1290/0/67 + npm build EXIT=0 + 真 server run #9 端到端 fan-out 真产 6 proposal (dedup×4/reflection×1/optimize×1)。Migration V68/V69 真库 applied；review record `/tmp/review-tech-design-{r1,r2}.md` + `/tmp/review-phase3-{java,ts}-r1.md` + `/tmp/judge-phase3-r1.md`。**Judge downgrade 2 项拆离 backlog**：SCHEDULED-TASK-FINDBY-INDEX / SECURITY-ADMIN-RBAC。 |
| **OBS-2 M5/M6 收尾** | 2026-05-11 | `f49d5b1` | M5 观察通过：旧表 `t_trace_span` 最后写入停在 `2026-05-02 16:17:16 UTC`，M4 后无新增；M6 删除旧表并移除旧实体/repository/read path，需求包归档到 `requirements/archive/2026-05-02-OBS-2-trace-data-model-unification/`。验证：server 全量测试 1185/0/0/66；真实库 Flyway version 67 success，`to_regclass('public.t_trace_span')` 为空。 |
| **SKILL-DASHBOARD-POLISH / V2 文档收尾** | 2026-05-11 | `5cfd03b` `311ff34` | 已交付但仍留在 active 的两个 dashboard polish 需求包归档到 `requirements/archive/2026-05-08-SKILL-DASHBOARD-POLISH*/`，补登 README 与 delivery-index。 |
| **MEMORY-DREAM-CONSOLIDATION 文档收尾** | 2026-05-11 | `05d0593` `6468fe3` `118f887` | 梦境系统交付状态对齐：需求包归档到 `requirements/archive/2026-05-08-MEMORY-DREAM-CONSOLIDATION/`，补登 delivery-index，暂缓区标记为已交付。 |
| **EVAL-LATENCY-DIM-EXPLICIT-NULL（M4_V2 latency not_measured + 综合分按 measured 维度重归一 / Mid pipeline）** | 2026-05-11 | `e373251` | 修 `EvalScoreFormula.normalizeThresholdMetric(Long, Long)` 当 `performanceThresholdMs` 未配（null/<=0）时返回满分 100 的 silently-misleading 行为：EVAL-V2 多数 scenario 没配 latency threshold → A/B 比对两边 latency 维度都 100 掩盖真实性能差距 / latency 维度 0.15 权重永远白送 15 分。**核心改动**：(1) `FORMULA_VERSION` `"M4_V1"` → `"M4_V2"`（语义变了的诚实标签，DB 老 row 仍 V1 不重算）；(2) 新私有方法 `latencyScoreOrNull(Long, Long)` 返 `Double` (nullable)：threshold null/<=0 时返 null 表 not_measured；删 orphan 老方法；(3) `calculate()` measured 走原 4 维加权 / not_measured 走 `(Q*0.55 + E*0.20 + C*0.10) / 0.85` 重归一让 latency 不参与；新常量 `LATENCY_NOT_MEASURED_REWEIGHT_DIVISOR = 0.85` (reviewer W2 fix)；(4) `Result` record 末尾追加 `Map<String, String> dimensionStatus` 字段（向后兼容 append；LinkedHashMap + Collections.unmodifiableMap 保 Jackson 输出顺序稳定）；`latencyScore` 字段 `double` → `Double` (nullable)；(5) `breakdownJson` 加 `dimensionStatus` 字段 + latency not_measured 时 scores.latency 写 JSON null；(6) `EvalOrchestrator` + `EvalTaskController.toItemMap` + `toCompareRow` 把 dimensionStatus 透传到 wire (reviewer W1 fix —— 之前 dev 算了没透传 = 契约不完整 silent bug)，`EvalTaskController` 抽 `extractDimensionStatus(scoreBreakdownJson)` parse JSON 取字段，M4_V1 legacy row 路径 fallback null；(7) `SkillAbEvalService` 抽 static `aggregateBaselineHistory(List<Result>)` + latency 累加 null-guard 仅按已测样本算平均；(8) **不动**：`normalizeThresholdMetric(BigDecimal, BigDecimal)`（cost 维度有全局兜底 0.01 USD 实际不触发 null path）/ entity 类型 / schema migration。**FE 改动**：4 个 score-bearing interface (EvalTaskItem / EvalTaskCompareEntry / EvalHistoryEntry / SkillEvaluateResult) 全加 `dimensionStatus?:` 字段统一契约 / 新 helper `isDimensionNotMeasured(item, metric)` 显式 dimensionStatus 优先 + 缺失 fallback null score / TaskItemCard latency chip 灰色 "not measured" + header 不 fallback composite (修 dev DONE_WITH_CONCERNS Concern #3 数据撒谎 bug，泛型 `subMetric` 写法不 hard-code latency) / EvalHistoryPanel cell italic muted "not measured" + tooltip 用 `label` 变量 (FE WARN-1 fix copy-paste trap) / EvalHistoryChart `connectNulls: false` 防 null 渲染成 0 / `evals.css` `.kv-chip-sf.dimmed` + `.scn-result-score.na` 灰色 italic 样式。**Mid pipeline r1 PASS**：BE-dev + FE-dev 并行 → 2 reviewer (java + ts Sonnet) 对抗 1 轮 → Judge 主会话 Opus → 双 PASS 0 blocker / BE 2 warning + FE 2 warning → 一次 fix 后主会话目检 → Phase Final 亲跑全量验证。**FE 实施期间 dev DONE_WITH_CONCERNS 自报 4 concerns**（#3 header fallback 必修 / #1 SkillEvaluateResult 契约一致性必修 / #2 brief "3 处" 数错 observation / #4 inline Pick 类型不全 nit），按 pipeline.md "正确性涉及先解决再 review" 让 fe-dev in-place fix #3 + #1，#4 dev 顺手做 bonus。**已知边界**：(a) FORMULA_VERSION bump = 同输入下新算法分数与老算法不可直接比，FE 当前不 guard cross-version compare（A/B 一般同期跑实际影响低），后续可在 compare endpoint 检 `formulaVersion` 不同时返 warning chip；(b) M4_V1 老 task DB row 永远停在 V1 算法语义不重算（设计 — score 写入时一次性快照）；(c) FE WARN-2 `#8a8a93` light-theme 对比度 ~3.0:1 < WCAG AA 4.5:1，SkillForge dark-mode 主导可接受，light-theme 上线时需重新评估；(d) FE `isDimensionNotMeasured` 在 `dimensionStatus` 缺失时 fallback 到 null score 推断 = M4_V1 legacy row 走 fallback，新 V2 row BE 已透传 dimensionStatus 走显式分支不触 fallback。**MVP 不做** (V2)：cross-version FE warning chip / dimensionStatus 字段持久化到 entity column（避免每次 parse JSON）/ cost 维度同款 not_measured 处理（cost 有全局兜底实际不触发）/ FE light-theme 对比度调整。验证：`mvn -pl skillforge-server -am test` Tests run: **1187**, Failures: 0, Errors: 0, Skipped: 66 → BUILD SUCCESS in 49.1s（baseline 1175 + 12 新增 case：EvalScoreFormulaTest +4 + EvalTaskControllerTest +5 + 新建 SkillAbEvalServiceAggregateBaselineHistoryTest +4）；`cd skillforge-dashboard && npm run build` EXIT=0 in 2.01s。BE 5 文件改 + 1 新测试 / FE 6 文件改 / 无 schema/migration。 |
| **REMINDER-MVP Phase A 连环 follow-up（5 commit / 4 Full pipeline + 1 Solo-then-retroactive-Full）** | 2026-05-10 | `a4100f7` `c712ffa` `37ff1d3` `cc87776` `b2c7039` | 围绕 REMINDER-MVP Phase A 上线后暴露的 regression / 防御性硬化连环修复，按时间序：(1) `a4100f7` **fix(compact): preserve trace_id across rewrite + retained block** — 修 P9-5 commit `fda2374` 引入的 trace_id wipe regression：light/full compact 重写 t_session_message 时 3-arg AppendMessage 默认 null traceId 导致整列被清，FE SessionDetail 页 trace input/output 显空（用户报告 sessions `5f3f1923` / `1ac075b7`）。B1 方案：SessionService.rewriteMessages 加 identity column preserve 层（snapshot non-null trace_id by seq_no → patch null traceId 项）+ CompactionService full compact retained-as-appended 块显式查 tail trace_ids 透传 7-arg AppendMessage。`SessionMessageRepository` 加 TraceIdView projection + `findNonNullTraceIdProjections` / `findTailTraceIdProjections`。**Known limitation 注释**：index 对齐仅在 LightCompactStrategy Rule 1 truncate-large-tool-output 下精确，Rule 2-4 (dedupConsecutiveTools/foldFailedRetries/dropEmptyNarration) 会 working.remove() 让列表 shrink → newList[i] 可能来自 oldSeqNo>i，trace_id 误填；accepted trade-off vs 100% NULL wipe。新增 `SessionServiceTraceIdPreservationIT` 4 case + `CompactionServiceTest.full_compact_retainedBlock_inheritsTailTraceIds`。**Full pipeline r1 PASS**（dev + java/code reviewer + Judge + 1 round 修 W1 invariant comment / W2 `atLeastOnce`→`times(1)` / W3 concurrency note）。**(2)** `c712ffa` **fix(reminder): hide recovery payload from FE display + backlog deferred items** — REMINDER-MVP D6 把 P9-5 recovery payload 包成 `<system-reminder>...</system-reminder>` String content；FE strip helper 之前只处理 array shape，String shape 整段是 reminder 的不识别 → chat 页面渲染成 raw `<system-reminder>` 文本气泡。两层 fix：`useChatMessages.ts` 加 RECOVERY_PAYLOAD msgType filter（与 COMPACT_BOUNDARY 平行）+ `stripSystemReminderBlocks` extend 接受 entire-string-is-reminder（startsWith+endsWith 严格匹配 collapse 成 `""`）。**Solo 档**（4 文件 +42/-2，纯 FE 无核心文件）。同步 backlog 加 REMINDER-MVP-PARTIAL-PATHS（enqueue/answerAsk/answerConfirmation 路径不带 reminder）+ REMINDER-FE-CONTENT-TYPE-DRIFT（TS 类型 drift）。**(3)** `37ff1d3` **feat(chat): map agent loop exceptions to user-friendly runtimeError messages** — 之前 ChatService.runLoop catch 块把 `StackTraceFormatter.format(e, 10)` 整段 stack trace 写到 SessionEntity.runtimeError + WS sessionStatus 推给前端，dashboard 用户看到裸 java.net.SocketTimeoutException 调用栈。改成 `toFriendlyChatError(Throwable)` 沿 cause chain 识别常见 HTTP / 网络异常映射成中文 actionable hint：SocketTimeoutException → "模型响应超时... 可重试，或在 application.yml 调高对应 provider 的 read-timeout-seconds" / UnknownHostException / ConnectException / SSLHandshakeException / InterruptedIOException 各自映射 + fallback 不带 stack 的 top message。`log.error(... e)` 完整堆栈仍记日志。删除 unused `StackTraceFormatter` import。**Solo 档**（46/7 行单文件）。**(4)** `cc87776` **fix(reminder): propagate user msg with reminder through engine + TodoWrite todos rename** — 修 Q2 commit `bdb0453` 半残品：ChatService 持久化 array-shape userMsg 含 `<system-reminder>` ContentBlock，但 `AgentLoopEngine.runInternal` line 374 拿 `String userMessage` 后用 `Message.user(userMessage)` 重建 String-shape Message，**完全忽略** ChatService 已构造的 array-shape Message。结果：DB 持久化对了（含 reminder），LLM 当前 turn 请求里**没有** reminder（用户实测 trace `a681e7a4` request blob 108 messages 0 个 `<system-reminder>`），cache 跨 turn replay 也字节漂移。Fix：AgentLoopEngine 加 7-arg `run(AgentDef, String, Message userMessageBlock, List, String, Long, LoopContext)` overload，runInternal 优先用 caller-supplied `userMessageBlock` (`messages.add(userMessageBlock)`) 否则 fallback `Message.user(userMessage)`；ChatService.chatAsync lambda 显式 `final Message userMsgWithReminder = userMsg` 透传到 runLoop（3 个 overload 全加 Message 参数 + 委托链 null 透传到 8-arg 实现）。同 commit 修 TodoWriteTool schema rename `tasks` → `todos`：与 Anthropic Claude Code canonical `{"todos":[...]}` 对齐，原因 Claude-family 模型从 training memory 经常按 `todos` 字段调用 → SkillForge schema 要 `tasks` 拒绝 → `[RETRY NEEDED]` 浪费 token；error msg + output `Tasks updated`→`Todos updated` 同步改。新增 `ChatServiceModelOverrideTest.reminderEnabled_passesArrayShapeMessageToEngineRun` 用 ArgumentCaptor 锁链路。**Solo 起手 → 用户拦截要求改 Full pipeline retroactive review** → java-reviewer + code-reviewer + Judge PASS w/ 4 mandatory follow-up（W1 invariant 注释 / W2 mock null assumption 注释 / Code WARN-1 reminder→engine 链路无回归测试 → 加 ArgumentCaptor 测试 / Code nit task→todo 术语统一 + 局部变量 rename），dev fix 后进 Phase Final。**(5)** `b2c7039` **feat(persist): mid-prefix divergence guard in updateSessionMessages** — Q2/Q3 修复后的结构性兜底：把 `SessionService.updateSessionMessages` 的 prefix-mismatch 触发 rewrite 条件从 `prefixLen == 0`（仅完全发散）扩到 `prefixLen < persistedContext.size()`（任何 mid-prefix 发散），加 log.warn 含 `divergeAt` / `persistedSize` / `engineSize` / `persistRole` / `engineRole` / **`persistContentType` / `engineContentType`** （getClass().getSimpleName() 暴露 ArrayList vs String 这种 Q2-style shape mismatch 一眼可见，无 PII）。Block comment 解释 Q2/Q3 历史 + invariant（compact 路径 sync DB BEFORE this called 不会触发 false-positive）。新增 `SessionServiceDivergenceGuardIT` 2 case：(a) midPrefixDivergence_triggersRewrite 验证 mid-prefix 触发 rewrite + ListAppender capture log.warn 含 contentType 字段 / (b) perfectPrefix_appendsDeltaOnly 控制对照 append-delta 路径 created_at 保留 + traceId stamping 对。Q2 era 残留 5 条 dup 行（1ac075b7 4 + fee25e3b 1）SQL DELETE + message_count 重算（事务包裹 + verified 0 dup）。**Full pipeline r1 PASS**（reviewer 双 PASS w/ 3 mandatory：log diag contentType 字段 + IT 加 + 冗余条件简化）。**所有 5 commit 累计验证**：`mvn -pl skillforge-core,skillforge-server -am test` 1178/0/0/66 BUILD SUCCESS（baseline 1175 + 3 新 IT case：1 SessionServiceTraceIdPreservationIT skipped Docker-only + 2 SessionServiceDivergenceGuardIT skipped Docker-only），server 重启 2 次（cc87776 / b2c7039）live。**配套 backlog 入 docs/todo.md**：REMINDER-MVP-PARTIAL-PATHS（enqueue/answerAsk/answerConfirmation 路径未覆盖 reminder） / REMINDER-FE-CONTENT-TYPE-DRIFT（TS raw msg 类型 string vs ContentBlock[] 漂移） / DATA-LAYER-ORIGIN-SPAN-LINK（chat 页面"展开看截断 tool_result 原文"按钮 + t_session_message origin_span_id 列）。 |
| **REMINDER-MVP Phase A（system-reminder 框架 + 3 source + P9-5 recovery payload 迁移到 `<system-reminder>` 包装）** | 2026-05-09 | `bdb0453` | (1) 新建 `skillforge-core/.../reminder/` 包：`ReminderSource` interface + `ReminderEntry` record + `ReminderContext`（11 字段含 systemPrompt + tools + requestMaxTokens + jsonMapper + thresholds 等）+ `ReminderBuilder` 主类（按 D7 ordered list 遍历 source / 5K token budget / 首条豁免 / try-catch 隔离 source 异常不阻 LLM）+ `MemoryAgeStatsProvider` 跨模块解耦 interface + 3 source 实现：`MemoryAgeSource`（Turn Count Debounce + 单 DB query via `getStats` 单事务一致快照 + staleCount==0 返 null 让 builder skip）/ `ContextUsageSource`（interval=1 / 用 `RequestTokenEstimator` 全 envelope 估算 / 三档阈值 hint）/ `FileActivitySource`（minAge fast-path filter + Debounce / FileStateCache.snapshot 复用 P9-5）；(2) `LoopContext.reminderLastEmitted` ConcurrentHashMap per-source state（per-run 自动清理）；(3) `AgentLoopEngine.runInternal` line 740 后注入：构造 ReminderContext → 调 `ReminderBuilder.build` → append 到 `promptSuffix`（动态段，不破 P13 stable cache hash）；(4) `RecoveryPayloadBuilder.build` 输出加 `<system-reminder>...</system-reminder>` 包装（履行 P9-5 D3）；P9-5 既有测试 `RecoveryPayloadBuilderTest` 2 处 + `CompactionServiceTest` 4 路径用例（B2 / Preemptive / Post-overflow / SessionMemory）全部更新断言含 `\n` 边界；(5) `MemoryRepository.aggregateActiveStats` 单 JPQL `COUNT/SUM(CASE)/MAX` 返 `Object[3]` 替代 r1 4 round-trips；`MemoryService.getMemoryAgeStats @Transactional(readOnly=true)`；(6) `application.yml` 加 `skillforge.reminder.{enabled, total-budget-tokens, memory-age.*, context-usage.*, file-activity.*}`；(7) `SkillForgeConfig` 5 @Bean wire（D7 顺序 [ContextUsage, MemoryAge, FileActivity]，setter 注入 `engine.setReminderBuilder`）。**D1-D8 ratify**：D1 `REMINDER-MVP` ID（不接 P9 系列）/ D2 复用 promptSuffix 不动 user message stream / D3 Turn Count Debounce per-source 单门槛 / D4 yaml 嵌套结构 / D5 默认（Memory interval=5 stale=7d / Context interval=1 pct=70 / File interval=5 max=5 minAge=30s）/ D6 P9-5 改 builder 输出最小改动 trigger 不变 / D7 [Context, Memory, File] 顺序 + 5K budget / D8 仅 Turn Count Debounce 不加 hash dedup。**Full pipeline r1+r2 对抗循环 PASS**：r1 双 reviewer Sonnet PASS / 0 blocker / 5 warning（W1 `ContextUsageSource.ObjectMapper` field 死代码 + bean 多余注入 / W2 `MemoryAgeSource` 4 DB round-trips + race window / W3 spec 偏差用 `TokenEstimator.estimate(msgs)` 而非 `RequestTokenEstimator` 让 70% 阈值偏迟 15-30% / W4 `CompactionServiceTest` 4 路径断言缺 `\n` / W5 `FileActivitySource.shouldEmit` 漏 minAge fast-path）；r2 dev fix 5 项 + 6 新测试（W1+W3 合并：删 ObjectMapper field + 用 RequestTokenEstimator + estimateUsesRequestTokenEstimator 验 used > TokenEstimator 严格大；W2：MemoryAgeStatsProvider 3 方法→1 getStats 单 JPQL aggregate + shouldEmit 零 DB + emit staleCount==0 返 null + noDbCalls/singleDbCall 测试；W4：4 处全改 startsWith("<system-reminder>\\n") + endsWith；W5：shouldEmit 加 stream filter at-least-1 entry age >= minAge + 不写 lastEmitted state；顺手 java W3-W5：dead hint branch 4 档单测 + double estimate W3 自然消除 + TOCTOU W2 自然消除）；r2 java-reviewer PASS 0/0/0 + r2 code-reviewer PASS 0 blocker / 1 warning **W-NEW-1**（`MemoryService.getMemoryAgeStats` row[2] `instanceof Instant` pattern 缺 IT test 覆盖：生产 Hibernate 6 + Postgres 路径正确，仅 future Hibernate upgrade / H2 compatibility 风险让 `lastRecalledAt` silently 丢失，**reviewer 自判 non-blocking for MVP，作为 follow-up**）+ 3 nit。验证：`mvn -pl skillforge-core,skillforge-tools,skillforge-server -am test` 跨模块 **Tests run: 1517**（core 269 + tools 44 + observability 38 + server 1166），Failures: 0, Errors: 0, Skipped: 60 → BUILD SUCCESS in 27.1s（主会话 Phase Final 亲跑 verification-before-completion Iron Law）；REMINDER-MVP 共 ~50 新增测试全绿（ReminderBuilderTest 10 + MemoryAgeSourceTest 11 + ContextUsageSourceTest 13 + FileActivitySourceTest 11 + AgentLoopEngineReminderTest 4 + 既有 P9-5 测试加 wrapper/`\n` 断言）。FE 不动；无 schema/migration。**Phase B 候选**（V2）：Date Change / Skill enable 变化 / Compact 失败计数 / MCP server 状态变化；**Phase C**：Lifecycle Hooks 联动 + dashboard /reminders 配置页；**Phase D**（不一定做）：claude-code user message prepend 模式；**永不做**：TodoWrite / Plan-mode / Malware warning（cli-only）。**W-NEW-1 follow-up**：补 `MemoryRepositoryAggregateIT` 验 Postgres `MAX(Instant)` 真返 Instant 类型。 |
| **P9-5 Post-Compact Recovery（FileStateCache + RecoveryPayloadBuilder + 4 路径自动注入 / 触碰核心 compact 文件清单 Full pipeline）** | 2026-05-09 | `fda2374` | (1) 新建 `skillforge-core/.../compact/recovery/FileStateCache.java`（framework-free POJO + `@Bean` wire；双层 ConcurrentMap `sessionId → path → FileEntry`；`safeCutLen` UTF-16 surrogate-safe 截断；构造器接 `maxTokensPerFile` 配置；LRU snapshot 按 lastReadAt DESC + budget 截）；(2) 新建 `RecoveryPayloadBuilder.java` 拼 plain user message 含 path + lastRead + content head + enabled=false / cache 空 / sessionId blank 三 fallback 返 null；(3) FileReadTool/Write/Edit 各 execute 末尾 hook `fileStateCache.put` + W3 fix `log.warn(sessionId+path+err)`（保留无参构造器兼容 EvalEngineFactory）；(4) `CompactionService.persistCompactResult` line 559 后插入 recovery user row（新增 `SessionService.MSG_TYPE_RECOVERY_PAYLOAD = "RECOVERY_PAYLOAD"` 常量），4 路径自动汇聚（B2 hard / Preemptive / Post-overflow callback path / SessionMemory Phase 1.5）；(5) `AgentLoopEngine.run()` try-finally 包整方法（私有 `runInternal()` 抽出原 body）保 normal/max_loops/exception 三路径都 evictSession；(6) `application.yml` 加 `skillforge.compact.recovery.{enabled,max-files,max-tokens-per-file}` 默认 `true / 5 / 5000`，**单一 yaml key 同时驱动 cache 截断 + builder 预算**（W4 fix 单一真相源）。**D1-D5 ratify**：D1 内存 FileStateCache（0 schema） / D2 仅文件（tool schema 已每次随 request 全发，skill listing 已通过 SkillLoaderTool description 每次发，不重注） / D3 plain user message（system-reminder 框架后再统一迁移） / D4 全局 @Bean（跨 session 命中需要全局视图，session 终态 evict） / D5 5 文件 × 5K token（与 claude-code POST_COMPACT_MAX_FILES=5 / MAX_TOKENS_PER_FILE=5K 对齐）。**P9-4 partial_head/partial_tail 同期暂缓** → [deferred/P9-4-partial-compact](requirements/deferred/P9-4-partial-compact/index.md)，痛点已被 microcompact 更精确覆盖（按内容粒度 + 不调 LLM 零 token）。**Full pipeline r1+r2 对抗循环 PASS**：r1 code-reviewer Sonnet PASS_WITH_WARNINGS / 0 blocker / 4 warning（W1 UTF-16 surrogate split / W2 4 路径仅 hard 覆盖 / W3 catch silent / W4 hardcoded budget 配置失效）+ java-reviewer Sonnet socket disconnect 失败重跑；r2 dev fix 4 项 + 6 新测试（W1 `safeCutLen` + `truncation_doesNotSplitSurrogate` 含 emoji / W2 3 路径 ArgumentCaptor 验真覆盖 Preemptive / Post-overflow / SessionMemory（mock previewMemoriesForPrompt 命中 Phase 1.5 而非 Phase 2 LLM） / W3 3 FileTool log.warn / W4 `constructorBudget_isHonored` budget=30 真验）；r2 java-reviewer Sonnet PASS / 0 blocker / 0 warning / 3 nit + r2 code-reviewer Sonnet PASS / 0 blocker / 0 warning / 1 nit（surrogate integration 测试落点偶数边界未实命中 split branch，unit test 覆盖足够）。验证：`mvn -pl skillforge-core,skillforge-tools,skillforge-server -am test` Tests run: **1166**, Failures: 0, Errors: 0, Skipped: 60 → BUILD SUCCESS in 27.6s（主会话亲跑）；27 r1 测试 + 6 r2 测试 = **P9-5 共 33 新增测试全绿**。FE 不动；无 schema/migration。**MVP 不做** (V2)：partial_head/partial_tail compact（P9-4 暂缓）/ system-reminder 包装（独立需求包）/ time-based microcompact / cache_edits（Anthropic 私有 API） / 跨 session FileStateCache hard cap LRU eviction（多 session 累积内存上限保护，当前仅 session 终态 evict）。 |
| **LLM-OUTPUT-BUDGET-AND-TRUNCATE（max_tokens 默认 4096→16384 + Light compact 字符截断 + Claude 模型感知 clamp）** | 2026-05-09 | `715c386` | (1) `LlmRequest.DEFAULT_MAX_TOKENS = 16384` 单一来源常量 + `AgentDefinition.getMaxTokens()` fallback 改用常量；(2) `LightCompactStrategy.LARGE_TOOL_OUTPUT_BYTES` 5KB→10KB + `truncateToHeadTail()` 字符级 substring 替换老 `truncateLines()`（头 4096 chars + 尾 2048 chars + UTF-8 多字节安全 + 自检 marker 幂等）；(3) `ClaudeProvider.MODEL_MAX_OUTPUT_CAPS` Map 加 model-aware clamp（claude-3-5-sonnet 8192 / opus 4096 / haiku 4096）。**Full pipeline r1+r2 对抗循环 PASS**：r1 java-reviewer Sonnet PASS / 0 blocker / 3 warning + code-reviewer Sonnet PASS_WITH_WARNINGS / 0 blocker / 1 HIGH (W4 Claude clamp) + 3 MED + 2 LOW → r2 单轮 fix 4 项 (W4 ClaudeProvider clamp / W2 CJK 测试 / W5 rename / W6 multi tool_result 测试)。验证：`mvn -pl skillforge-core,skillforge-server test` Tests run: 1159, Failures: 0, Errors: 0, Skipped: 60 → BUILD SUCCESS（baseline 1151 + 8 新增）。Phase Final：CI 137 次 testcontainers HikariPool timeout infra 问题持续挂（与本改动无关），用户 e2e 待 server 重启验 Session Analyzer / Design Agent 不再报 Output truncated。**MVP 不做** (V2)：per-provider yaml maxOutputTokens / per-model KNOWN_MODEL_OUTPUT_CAPS / LLM-based summary / AgentLoopEngine:1406 continuation cap follow-up / marker 加 sentinel 前缀。无 migration。 |
| **SKILL-EVOLVE-LOOP（skill 自进化闭环：cron 生产 + 单 skill EVAL + 历史表 + EvalFailures 注入 + 自循环 + WS push + dashboard 曲线）** | 2026-05-08 | `9e2e584` | 5 phase 闭环：(1) `SkillDraftScheduledExtractor` 凌晨 3AM cron extract from session；(2) V63 `t_skill_eval_history` 表 + `SkillAbEvalService.runBaselineOnly` + `POST /api/skills/{id}/evaluate` + `GET /eval-history`；(3) `SkillScheduledEvaluator` 周一 4AM cron + 7 天跳过；(4) `EvalTaskRepository.findRecentFailuresForSkill` 通过 `t_agent.skill_ids` JSON LIKE 反查 + LIKE `% _ \` 转义 + ESCAPE 子句 + `SkillEvolutionService.callLlmForImprovement` 拼最近 5 个 composite_score < 60 失败进 LLM prompt（INV-12 limit=5 + agent_final_output 截 400 char）；(5) `SkillSelfImproveLoop` 周二 5AM cron score < 60 触发 `createAndTrigger(SYSTEM_USER_ID=0L)` + `SkillAbCompletedEvent` + `SkillAbCompletedEventPublisher` `@Transactional` 包装类（绕开 self-invocation proxy 让 AFTER_COMMIT 真 fire）+ `SkillSelfImproveLoop.onAbCompleted` `@TransactionalEventListener(AFTER_COMMIT) + @Transactional(propagation=REQUIRES_NEW)` Spring 6.1+ P11 教训 + promote 成功复用 `UserWebSocketHandler.broadcast` 推 `skill_auto_upgraded` + INV-9 失败仅 INFO log；(6) FE 5 新文件（evalScore.ts / SparklineCell SVG / EvalHistoryChart echarts y[0,100] / EvalHistoryPanel chart+Evaluate Now agentId null disabled / AutoEvolveRunsList）+ SkillTable Score/Trend 两列 + SkillDrawer 双 tab + WS handler 弹 notification + invalidate 4 query key + `staleTime: 60_000` + `enabled` guard + React.memo；(7) yaml 4 开关默认 true 可关 + `score-threshold: 60.0`。**12 INV ratify** 详见 [tech-design.md](requirements/archive/2026-05-08-SKILL-EVOLVE-LOOP/tech-design.md) §"12 Invariants"。**Full pipeline r1+r2 对抗循环 PASS**：r1 BE PASS / 0 blocker / 5 warnings (W1 Repository 直注 Controller / W2 dead agentService / W3 LIKE 通配符未转义 / W4 createdAt 手动 / W5 NumberFormatException 泄露) + FE PASS / 0 blocker / 4 warnings (W1 staleTime / W2 useQueries 引用稳定 / W3 enabled guard / W4 React.memo) → r2 单轮 fix 8 warning（W2 FE skip 留 V2 batch endpoint）→ Claude 主会话目检 PASS。验证：`mvn -pl skillforge-server test` Tests run: 1105, Failures: 0, Errors: 0, Skipped: 60 → BUILD SUCCESS（baseline 1060 + 45 新增）+ `cd skillforge-dashboard && npm run build` EXIT=0。Phase Final：用户自验端到端 e2e（手动 /evaluate 落 history → low score 触发 self-improve → A/B → promote → WS toast → dashboard 列表更新）。**MVP 不做** (V2)：scheduled evaluator scenario 并行（接 EVAL-PARALLEL-SCENARIO 暂缓）/ Skill 退役 / 多 candidate 并行 evolve / 用户 review feedback 影响 next iteration / 跨 skill 能力 graph / 失败超 N 次 manual review 提示 / batch endpoint 替代 N 个 GET /eval-history（FE W2 skip）/ Phase Final 用户 e2e 验证（系统现在等用户跑闭环）。Migration: V63。 |
| **P13 Prompt Cache MVP（5 provider 全链路：稳定 prefix + Claude 3 breakpoint + Usage normalize + dashboard hit rate）** | 2026-05-08 | `9a7cdc3` | 5 个 LLM provider 启用 prompt cache 全链路。**核心改动**：(1) `skillforge-core/.../llm/cache/` 新建 6 文件协议层 — SystemPromptParts (record stable+dynamic) / CacheBoundary marker `<!-- SKILLFORGE_CACHE_BOUNDARY -->` / CacheControlMarker / ToolNormalizer (trim + sort by name + ORDER_MAP_ENTRIES_BY_KEYS + per-tool SHA256 hash + cached canonical mapper r2) / UsageNormalizer (按 ProviderProtocolFamily 分支 5 provider，r3 OpenAI-family `input = Math.max(0, input - cacheRead)` 让 inputTokens 跨 provider 统一"非 cached"语义)；(2) ToolHashTracker / CacheBreakDetector @Component (5% 容差 + 2K 阈值算法 + per-session prevCacheRead state)；(3) SystemPromptBuilder.buildWithBoundary(claudeMd) 拆 stable / dynamic 两段，stable 含 agent.systemPrompt + soulPrompt + toolsPrompt + behaviorRules（不含 Instant.now / userId / sessionId）—— SHA256 跨调用严格相等 (INV-1)；(4) ClaudeProvider.buildRequestBody 改 system structured array + 3 breakpoint cache_control（system stable 末 + tools 末 + 最后 user message 末 block，参考 claude code 模式，用 4 上限的 3）；(5) OpenAiProvider 不动 request body (INV-5)，parseResponse 用 UsageNormalizer 统一解析；(6) LlmResponse.Usage 加 cacheReadInputTokens + cacheCreationInputTokens 字段（保留 2-arg constructor backward compat），所有 provider parseResponse 走 UsageNormalizer；(7) V62 migration `t_llm_span.cache_creation_tokens INTEGER` nullable + LlmSpan domain / Entity / DTO / PgLlmTraceStore attributes_json round-trip / TraceLlmCallObserver doWrite 写 cache 字段 + cache_break attr + 双层 try/catch 隔离 (INV-10)；(8) AgentLoopEngine 用 buildWithBoundary 拼接 stable + MARKER + dynamic（r2 W3 fix：promptSuffix 非空但 systemPrompt 没 MARKER 时强制插入 MARKER 防 dynamic 漏进 stable cache）；(9) FE LlmSpanDetail 加 cacheCreationTokens + metadata 字段 + 新建 CacheStatsRow (cache 数字 + hit rate 徽章颜色阈值 + break event 红徽章 + Tooltip + 千分位 + 1 位小数) + LlmSpanDetailView 接入。**13 INV ratify** 详见 [tech-design.md](requirements/archive/2026-05-08-PROMPT-CACHE-MVP/tech-design.md) 含跨 provider inputTokens 语义统一定义。**Full pipeline r1+r2+r2.5+r3 对抗循环** PASS：r1 BE PASS / 0 blocker / 4 warning + FE NEEDS_FIX_R2 / 2 blocker（B1 hit rate denominator 算 16000% / B2 wire shape mismatch silent failure 红徽章不渲染 - P11 教训重演）+ 2 warning → r2 fix BE B2 dto metadata Map + W1/W2/W3/W4，FE B1 prop 重命名内部聚合 + W1 label `prompt` + W2 6 wire-level integration test + r1 B1 broken example regression → **FE r2 push-back 抓 cross-provider 语义 architectural flaw**（FE 读 UsageNormalizer.java:53 发现 OpenAI-family `prompt_tokens` 是 TOTAL 含 cached，DeepSeek 真实场景 hit rate 偏低）→ r3 BE mini-fix UsageNormalizer 4 个 OpenAI-family case 加 `input - cacheRead` + dirtyWireDataClampedToZero edge case（cached > total → 0 不负）。**Phase Final 修 Maven 多模块 jar 一致性 bug**：spring-boot:run 加载 ~/.m2 旧 SNAPSHOT jar 触发 NoSuchMethodError，需 `mvn install -pl core/tools/observability` 安装新 jar 后再启 backend。验证：`mvn test` Tests run: 1060, Failures: 0 (baseline 1057 + r1 38 新增 r2/r3 4 新增) → BUILD SUCCESS；`tsc --noEmit` + `npm run build` EXIT=0；FE 25 cache tests + BE 38 cache tests 全绿；端到端 Phase Final：用户用 OpenAI-family provider (DeepSeek/Qwen/mimo) e2e 验证，dashboard cache stats 行正常显示（无 ANTHROPIC_API_KEY 暂不验 Claude 手动 cache_control 真命中，留下次有 key 时验证）。**MVP 不做** (V2)：Qwen 手动 cache_control / Anthropic 1h TTL / Messages sliding window / cost 美刀估算 / xiaomi-mimo `cache_write_tokens` / cache hit rate 告警 / LRU eviction CacheBreakDetector / ToolHashTracker 持久化。Migration: V62。 |
| **P11 MCP Client MVP（stdio + per-agent + dashboard CRUD + dogfood time server）** | 2026-05-07 | `c4f7102` | 引入 Model Context Protocol 客户端能力。**核心改动**：(1) `skillforge-tools/.../mcp/` 协议层（McpStdioTransport NDJSON 帧 / McpServerSession initialize 状态机+tools cache+lazy 重连 / McpToolAdapter inputSchema 透传 / McpServerSessionRegistry / JSON-RPC records）+ skillforge-server/.../mcp/ 服务端（Entity / Repo / Service / Lifecycle @TransactionalEventListener(AFTER_COMMIT, REQUIRES_NEW) reload / McpToolRegistrar 命名前缀 + 冲突拒绝 / 6 REST endpoints / DTO + events + exceptions）+ `LoopContext.allowedMcpServerNames` 双层防护（INV-4 collectTools 暴露过滤 + dispatch NOT_ALLOWED 短路）；(2) V61 schema (`t_mcp_server` + `t_agent.mcp_server_ids VARCHAR(512)` comma-list + idx + 默认 seed time uvx) + ChatService 默认空集（agent 必须显式 enable）；(3) FE `/mcp-servers` 页面 + AgentDrawer multi-select + Layout MCP 入口 + 编辑 drawer Form.List args/env + name regex `[a-z0-9_]+` ≤32 + edit name 禁用 + 测试连接 + 删除 409 双 shape；(4) env var `${VAR}` 严格 regex + Matcher.quoteReplacement 防注入 + ProcessBuilder array form 防 shell injection；(5) DELETE 引用校验 split+equals 防 substring 误匹配（INV-12）。**13 INV ratify**（详见 [tech-design.md](requirements/archive/2026-05-07-MCP-CLIENT-MVP/tech-design.md)）含新增 INV-13: env mask sentinel `"***"` FE/BE 两端同步严格定义。**Full pipeline r1+r2+r2.5+r3 对抗循环** PASS：r1 BE 0 blocker / 4 warning（W1 W4 accept；W2 stderr WARN leak / W3 env plaintext API 暴露）+ FE 1 blocker（实测证伪 main pre-existing）+ 2 warning（W1 edit 仍发 name 隐患 / W2 listMcpServers 静默 fallback）→ r2 fix BE W2/W3 + FE B/W1/W2/W3-FE → r2.5 BE 加 update name 兼容 + env preserve-on-***（FE concern 联动）→ r2 reviewer 抓 architectural flaw（FE *** filter 让 BE preserve never fires）→ r3 FE 撤销 filter 让两边防御 compound。**Phase Final 修 2 个 unit test 漏抓的 startup bug**（reviewer 都没抓到）：SkillForgeApplication EntityScan/EnableJpaRepositories 漏 mcp package + McpServerLifecycle.onUpserted @TransactionalEventListener+@Transactional 在 Spring 6.1+ 必须 propagation=REQUIRES_NEW。**MVP 不做**（V2）：MCP Server 反向 / HTTP-SSE transport / OAuth flow / 用户级 server / Firecrawl/Postgres/Notion/Calendar 等具体 server（dashboard CRUD 自助加）/ 监控告警 / resources/prompts / dedicated mcpExecutor。**默认 dogfooding**: time MCP server (Anthropic 官方 uvx mcp-server-time) 提供 `mcp_time_get_current_time` + `mcp_time_convert_time` 2 tools。**协议测试**走 Mockito 单测（不写 Java IT fixture，用户 ratify）+ Phase Final 真 time server e2e 兜底。验证：`mvn test` Tests run: 1057, Failures: 0 (baseline 1009 + 48 新增) → BUILD SUCCESS；`tsc --noEmit` + `npm run build` EXIT=0；FE 26 + BE 48 tests 全绿；端到端 Phase Final：uvx mcp-server-time 进程 pid=50548 connected → 2 tools 注入 → dashboard `/mcp-servers` status=connected → test-connection 返完整 inputSchema。Migration: V61。 |
| **P10 聊天斜杠命令 MVP（8 命令 + dashboard popup + channel BE 拦截）** | 2026-05-07 | `e069057` | 8 命令（/new /compact /model /models /skill /tool /context /help）+ V60 schema (`t_session.runtime_model_override` 字段)+ ChatService 两处 model 解析改造（runLoop / completeConfirmedTool）+ `SlashCommandService` registry 模式 + 8 个 `SlashCommandHandler` + `CommandResult` DTO 三 displayMode (redirect/toast/modal) + `SlashCommandController` REST `POST /api/commands/execute` (record `{sessionId, command, args, userId}` + @Valid + IT 真过 Jackson + LocalValidatorFactoryBean) + `ModelCatalog` 用 LlmProperties YAML（INV-8 校验 modelId 存在性）+ `ChannelSessionRouter` 拦截 `/` 前缀早于 chatAsync + `ChannelConversationRebindService` (r2 抽 @Transactional 防 close+insert 非原子)+ `chatLoopExecutor.submit` 异步触发 /compact (B2 fire-and-forget 0.008s) + FE `CommandPopup` (8 命令 fuzzy match + 上下箭头 + Enter/Tab/Esc)+ `CommandResultModal` (markdown 渲染) + `ChatInput` 改造 (`COMMAND_NAME_REGEX = /^\/[A-Za-z]*$/` 仅首字符 `/` 触发) + `commands.ts` API + Chat.tsx `slashCommandConfig` useMemo + `refreshCompactStats` useCallback (r2 修 W1 破坏 React.memo)。**9 决策 ratify**：见 [tech-design.md §"Ratified Decisions"](requirements/archive/2026-05-07-P10-slash-commands/tech-design.md)。**Full pipeline r1+r2 对抗循环 PASS**：r1 BE 2 blocker (B1 silent failure FE/BE contract mismatch + B2 /compact sync 阻塞) + 2 warning + FE 0 blocker + 2 warning → team-lead 仲裁 must-fix-r2: BE B1/B2/W1/W2 + FE W1/W2；defer: BE C1 t_model_provider 不存在 (用 LlmProperties) / BE C2 SYSTEM_TOOL_NAMES cosmetic / BE C5 broadcaster mock noise / FE C1 model header chip (V2) → r2 reviewer 复审 PASS。验证：`mvn -pl skillforge-server test` 1009 tests 0 failures (baseline 991 + 18 新增) + `cd skillforge-dashboard && npm run build` EXIT=0 + 7 命令 curl e2e 全过 (/help /models /context /model /compact 0.008s 异步 /unknown 等) + INV-15 验证 (/model qwen-32b 路由到 ModelCommandHandler 不被 ModelsCommandHandler 拦截)。**MVP 不做** (V2)：自定义命令 / `/agent` `/session` `/cancel` `/branch` `/cost` 边缘命令 / chat header model chip / `Tool.isSystem()` 接口 / 专用 compactExecutor / 命令参数 fuzzy 补全。Migration: V60。 |
| **P12 定时任务 MVP（user-type cron + one-shot + 5 Tool + dashboard）** | 2026-05-07 | `72b8980` | V59 schema (`t_scheduled_task` + `t_scheduled_task_run`) + `ScheduledTaskService` (CRUD + 互斥校验 + 互转) + `UserTaskScheduler` (ThreadPoolTaskScheduler + per-task ReentrantLock + skip-if-running ConcurrentHashMap.newKeySet) + `ScheduledTaskExecutor` (session new/reuse + onSessionLoopFinished hook 监听 → channel push) + `SchedulerChannelDispatcher` (ReplyDeliveryService.deliver + catch Throwable + INV-9 严格化) + 5 Agent Tool (Create/Update/Delete/List/GetScheduledTask 均 silent + creator_user_id 隔离) + REST 7 endpoints (`/api/schedules` CRUD + manual trigger + run history) + `ChatService.SessionLoopFinishedEvent` 在 finally 内发布（核心文件改动）+ FE `/schedules` 页面（列表 + 编辑 drawer + cron↔one-shot radio 互转 + per-row triggeringId state + run history drawer + Tag 颜色状态展示）+ `app.dashboard-url` 配置化（waiting_user 推绝对 URL）。**11 决策 ratify** 见 [archive/2026-05-07-P12-scheduled-tasks/tech-design.md §"Ratified Decisions"](requirements/archive/2026-05-07-P12-scheduled-tasks/tech-design.md)。**Full pipeline r1 + r2 对抗循环 PASS**：r1 BE 6 warnings / FE 2 blocker + 2 warning → team-lead Judge 仲裁 must-fix-r2: BE W1/W3/W4/W5 + FE B-1/W-1/W-2；defer: B-2 cron next-5-fires (FE 简化) / W2 listRuns 分页 / W6 sync listener → r2 reviewer 复审 PASS。验证：`mvn -pl skillforge-server test` 934 tests 0 failures (含 49 IT skipped)、`cd skillforge-dashboard && npm run build` EXIT=0。Migration: V59。 |
| **EVAL-V2 M5+ Smart trace/session dataset drafts** | 2026-05-07 | `a06bd3d` | 管理端 trace 列表新增 Smart Import：`GET /api/eval/traces/suggestions` 根据 `agent_error / tool_failure / span_error / high_token / multi_turn / has_tool_calls` 等原因推荐适合沉淀为评测集的 trace/session；用户选择后调用 `POST /api/eval/scenarios/batch-import` 批量生成 `draft` 场景，后续沿用 Scenario Drafts approve 流程入 dataset。前端 `TraceImportSuggester.tsx` 接真实建议接口，`Traces.tsx` 支持单条 Add to dataset 和多选 smart import；同时清理 eval UI 的 TS/build 问题。验证：`mvn -pl skillforge-server -Dtest=TraceScenarioImportServiceTest,EvalControllerTraceImportTest test` 7/7 通过；`cd skillforge-dashboard && npx tsc --noEmit` 通过；`cd skillforge-dashboard && npm run build` 通过。运行侧 follow-up：重建并重启 server，补齐 launchctl LLM provider env；本地 DB 热修 Code Agent `owner_id NULL -> 1`。 |
| **EVAL-V2 M6 apply suggestion → PromptImprover** | 2026-05-06 | `99ad06f` | 新增 `POST /api/eval/tasks/{id}/apply-improvement`，从 task 读取 `improvementSuggestion` 并触发 `PromptImproverService.startImprovement(...)`；`PromptImproverService` 新增 suggestion-aware 入口，把分析建议写入 `PromptVersion.improvementRationale`，并在 candidate prompt 生成时把该建议注入 LLM prompt，避免“Apply suggestion”只是空转已有 auto-improve 流程；前端 `Eval.tsx` 的 task drawer 在 `Improvement suggestion` 区块新增 `Apply suggestion` 按钮，调用后返回 `abRunId` 并提示 improvement 已启动。验证：`mvn -pl skillforge-server -Dtest=EvalTaskControllerTest,PromptImproverServiceTest,EvalScoreFormulaTest test` 通过；`cd skillforge-dashboard && npm run build` 通过。 |
| **EVAL-V2 M4 score 多维度** | 2026-05-06 | `99ad06f` | 新增 `V58__add_eval_task_item_dimension_scores.sql`，给 `t_eval_task_item` 增加 `quality_score / efficiency_score / latency_score / cost_score / cost_usd / score_formula_version / score_breakdown_json`；后端新增 `EvalScoreFormula`，按 `0.55 quality + 0.20 efficiency + 0.15 latency + 0.10 cost` 聚合总分，使用全局 cost threshold，并在 `quality < 30` 时将总分封顶到 39；`EvalOrchestrator` 从 `rootTraceId` 聚合整条 trace tree 的 `total_cost_usd`，写回 item 与 scenarioResults；`EvalTaskController` / compare 接口补齐多维字段；前端 `Eval.tsx` 增加 metric selector，task cases 与 compare modal 支持按 `composite / quality / efficiency / latency / cost` 切换查看，并按当前维度实时计算 delta。验证：`mvn -pl skillforge-server -Dtest=EvalTaskControllerTest,EvalScoreFormulaTest test` 通过；`cd skillforge-dashboard && npm run build` 通过。 |
| **EVAL-V2 M5 trace 数据回流 dataset** | 2026-05-06 | `2d10edf` | 新增 `POST /api/eval/scenarios/from-trace`，从 `rootTraceId` 反查 root trace 的首条 user message 与末条 assistant message，自动生成 `EvalScenarioEntity`（默认 `category=trace_import`、`split=held_out`、`oracleType=llm_judge`，agentId/sourceSessionId 从 trace 继承）；前端 `Traces.tsx` 详情头部新增 `Add to dataset` 按钮，直接透传 `rootTraceId`，成功后刷新 eval dataset 查询。验证：server 定向测试新增 `EvalControllerTraceImportTest`、`TraceScenarioImportServiceTest` 并与 scenario versioning 测试一起通过；dashboard `npm run build` 通过。下一步：M4 score 多维度。 |
| **EVAL-V2 M3c 归因闭环 + M3e case → trace** | 2026-05-06 | `2d10edf` | M3c：新增 `V55__create_eval_analysis_session.sql`、`EvalAnalysisSessionEntity/Repository/Service`、`EvalAnalysisController`、`POST /api/eval/scenarios/{id}/analyze`、`POST /api/eval/tasks/{id}/analyze`、`POST /api/eval/tasks/{id}/items/{itemId}/analyze`、`GET /api/eval/tasks/{id}/analysis-sessions`，前端 `AnalyzeCaseModal` 统一支持 `scenario/task/item` 三种上下文；新增 `AnalyzeEvalTaskTool` 让 analysis chat 可把 `attributionSummary / improvementSuggestion` 回写到 `t_eval_task`。M3e：task item 卡片新增 `View trace`，跳到 `/traces?traceId=...`；`Traces.tsx` 加 query-param 深链接选中逻辑。验证：server 定向测试 32/32 通过；dashboard `npx tsc --noEmit` + `npx vite build` 通过。下一步：M3d compare runs。 |
| **EVAL-V2 M3d compare runs + M3f annotation queue** | 2026-05-06 | `2d10edf` | M3d：新增 `POST /api/eval/tasks/compare?ids=a,b`，返回共享 `scenarioId` 交集下两侧 task 的 `status / compositeScore / attribution / latency / loops / tools / rootTraceId / agentFinalOutput`，并计算 `scoreDelta / outputDiffers`；前端 `Eval.tsx` 新增 2-task compare 选择和 compare modal。M3f：新增 `V56__create_eval_annotation.sql`、`EvalAnnotationEntity/Repository/Controller`、`POST /api/eval/annotations`、`GET /api/eval/annotations`、`PATCH /api/eval/annotations/{id}`；前端 `Eval.tsx` 新增 Annotation Queue，支持从 task item 发起人工标注、录入 `correctedScore/correctedExpected`、查看 pending/applied、标记 applied，并继续跳 `View trace`。当前边界：annotation queue 只负责人工修分/修 expected 的暂存与状态流转，真正 scenario versioning / re-judge 留给 M3g。验证：server 定向测试 37/37 通过；dashboard `npx tsc --noEmit` + `npx vite build` 通过。下一步：M3g dataset versioning。 |
| **EVAL-V2 M3g dataset versioning（M3 收尾）** | 2026-05-06 | `2d10edf` | 新增 `V57__add_eval_scenario_versioning.sql`，`t_eval_scenario` 加 `version` + `parent_scenario_id`；后端新增 `EvalScenarioVersionService`、`GET /api/eval/scenarios/{id}/versions`、`POST /api/eval/scenarios/{id}/version`，并把 Agent dataset 列表收口为“每个 scenario family 只展示最新版本”。前端 `ScenarioDetailDrawer` 新增 version selector / history，`DatasetBrowser` agent 卡片展示版本号；同时把 `AnnotationQueue` 里的 `correctedExpected` 接到“Create version”动作，DB 场景可直接从人工标注创建新版本并自动标记 annotation applied。验证：server 定向测试 43/43 通过；dashboard `npx tsc --noEmit` + `npx vite build` 通过。下一步：M4 或 M5。 |
| **EVAL-V2 M3a-b2 task/item 双写收尾 + r2 review fix** | 2026-05-06 | `2d10edf` | M3a 第二段收尾：落地 `EvalTaskEntity` / `EvalTaskItemEntity`、`EvalTaskController`、`RunEvalTaskTool`、V52/V53/V54 三个 migration，以及 reviewer r2 fix 5 项：`ScenarioRunnerTool` 单/多轮统一在 `finally` 做 `rewriteTraceOriginToEval`，V52 rename 加 idempotent guard，V54 task-level idempotent guard 防重复迁移，V53 `idx_task_item_session` 改 partial index，`EvalOrchestrator` dual-write warn log 补 throwable。新增 `ScenarioRunnerToolTest` 回归锁 early-return origin rewrite；`EvalTaskMigrationIT` 保留但改为 `-Dskillforge.runMigrationIT=true` 显式 opt-in，避免本地日常回归受 Docker/Testcontainers 干扰。下一步：M3c 归因闭环；M3a-b3 FE 切换并入后续任务面板演进。 |
| **EVAL-V2 M3a-b1 origin 字段** | 2026-05-05 | `cd69350` | M3a 三段拆分第一段：V50 (server: t_session + t_llm_trace 加 origin VARCHAR(16) NOT NULL DEFAULT 'production' + partial index) + V51 (observability: 同步加 origin to t_llm_trace) + ORIGIN_PRODUCTION/ORIGIN_EVAL 常量 + 5 处过滤 (SessionService / TracesController / DashboardService LEFT JOIN preserves channel-only / CompactionService / Recovery) + 3 处 spawn 复制 + GetTraceTool 6th caller 加 filter（reviewer 抓的 BLOCKER）。Full pipeline 2 reviewer + r2 fix 4 件。**已知 trade-off**：b1 部署后 EvalOrchestrator 仍未改造（b2 scope），eval 流量当前仍 origin='production' 直到 b2 落地。781/766 server +1 + 25/25 observability + V50/V51 实跑 |
| **EVAL-V2 M3b Dataset UI 完整化（Gap A）** | 2026-05-05 | `b349f6f` | EvalController.toBaseScenarioMap 补 surface 5 字段（toolsHint / tags / setupFiles / maxLoops / performanceThresholdMs）+ FE EvalDatasetScenario / BaseScenario 类型扩展 + ScenarioDetailDrawer 加 5 段（Description / Tools / Tags / Setup files / Technical info，全 null/empty guard）+ DatasetBrowser 卡片加 description preview（line-clamp 2 + max-height 3em fallback）+ 前 3 tag chip + "+N" overflow chip（title hover 显隐藏 tag 名）。Mid 简化：1 dev → 1 ts-reviewer (Sonnet) PASS (0 blocker / 4 warning) → r2 fix (W1 +N title hover / W2 max-height 2.8em→3em fallback)。766/766 mvn 无 regression / tsc 0 / vite build 成功。无 schema migration |
| **EVAL-V2 M2 多轮 case** | 2026-05-05 | `5608cd0` | EvalScenarioEntity 加 conversation_turns（V49）+ EvalOrchestrator multi-turn 分支 + EvalJudgeTool.judgeMultiTurnConversation（per-turn 30% + overall 70% weight 配置化）+ ScenarioRunnerTool.runScenarioMultiTurn（单 sandbox 累积 history，90s budget）+ SessionScenarioExtractor 多轮抽取（≥2 user→multi-turn）+ AbEvalPipeline / SkillAbEvalService R5 fallback warning + FE ScenarioDetailDrawer 多轮气泡渲染 + AddBaseScenarioModal multi-turn turns editor（advanced disclosure，stable id key 防 stale state）。Mid pipeline：1 dev → 2 reviewer (java + ts Sonnet) 并行 → r2 fix（FE 2 blocker：silent task 验证死锁 + key={i} stale 漏洞）。766/766 mvn + V49 migration 实跑 |
| **EVAL-V2 PRD v2 重写** | 2026-05-05 | `74c096b` | 完整闭环 7 milestones (M2 / M3a-g / M4-6) + D1-D14 决策（D10 origin 字段 + D9 t_eval_analysis_session 独立表 + D6 rename t_eval_run + D7 复用 OBS trace）+ Gap A/B/C/D 整合 |
| **EVAL-V2 Q1+Q2+Q3** | 2026-05-05 | `3554569` | EVAL-V2 M0+M1 follow-up 3 件：Q1 SessionEntity 加 `source_scenario_id VARCHAR(64)` (V48) + AnalyzeCaseModal 传 + ScenarioDetailDrawer 反查"Analysis sessions"段；Q2+Q3 ScenarioLoader 双路径（classpath + home dir）+ POST `/eval/scenarios/base` 写 home dir JSON + DatasetBrowser `[Base][Agent]` tab + `+ 新增` modal + 中文 chip 系统内置/用户新增 + AddEvalScenarioTool（agent 通过 tool 加 scenario）。**已知 trade-off**：scenario 持久化双轨（per-agent → DB / global → JSON），M3 dataset versioning 时统一。Mid pipeline：1 dev → 2 reviewer (java + ts Sonnet) 并行对抗 1 轮 → r2 fix (FE 2 blocker AddBaseScenarioModal + 中文 chip / BE W1 length 36→64) → 749/749 mvn test |
| **EVAL-V2 M0+M1 MVP** | 2026-05-04 | `47b331c` | Datasets browser tab + Eval 详情卡片化（big score + attribution badge 仅 fail 显示 + 折叠 task/judgeRationale/agentFinalOutput）+ ScenarioDetailDrawer + AnalyzeCaseModal（"分析这个 case" D4 stretch）+ EvalOrchestrator WS push case_running/case_passed/case_failed/eval_run_completed + throttle (>50 case 按 5%) + FE progress bar |
| **SUBAGENT-CONTINUATION** | 2026-05-04 | `ee3dd53` | SubAgentTool 加 `continue runId task`（reuse idle child session 继续聊）+ `terminate runId`（强制结束 child + 释放并发额度）两个 action；preserveActiveRoot=true 让 continue 内部 trace 继承同 root。`onSessionLoopFinished` + `ChatService.runLoop` teardown 双 TERMINATED guard 让"父显式终止"成 sticky 终态。Mid pipeline 走完：r1 dev → DONE_WITH_CONCERNS → r2 fix (terminate race + userId) → java-reviewer 对抗 1 轮 (NEEDS_FIX 2 warning) → r3 fix (ChatService guard + markRunResumed defensive) → 主会话目检 PASS。717/717 全量回归绿 |
| **P12-PRE** | 2026-05-04 | `a27cc39` | Sprint 4 前置决策闭环：Cost Dashboard 复用现有 `pages/ModelUsage`；PG 备份 + 多用户/权限模型均"暂不实现，accept current risk"。需求包归档；P12 解除 blocker |
| **TOOL-CC-NAME-ALIAS** | 2026-05-04 | `be5788b` | 内置 file 工具 LLM-facing 名 `FileRead/FileWrite/FileEdit` → `Read/Write/Edit`（3 Tool + 2 Sandboxed* 改 `getName()`；46 文件 string literals + V47 migration backfill `t_agent.skill_ids/tool_ids`）。**实施时改方案**：原 todo 写"加别名重定向"，实测后用户决定走"直接改名"（避免长期维护双名歧义）。Reviewer PASS，`mvn test` 695 pass / 0 fail |
| **OBS-4 全交付（M0-M4）** | 2026-05-03 | `eda937d`(M0) + `e89c593`(M1) + `877495b`(M2) + `a496a7e`(M3) + 本 commit (M4 收尾归档)；root_trace_id 一等公民字段 + 跨 agent / 跨 session trace 串联 + FE 二级折叠瀑布流；session `cf4202a6` 7 traces / 5 sessions / 60 spans 真实场景 INV-1/3/4/5/6 全验证 | [交付索引](delivery-index.md)；需求包归档到 archive |
| **OBS-2 M4** | 2026-05-03 | M2 ETL 实跑（33 trace + 920 tool + 4 event 回填）+ M4 关旧轨写入 `b31cfaf` | [交付索引](delivery-index.md)；M5/M6 已于 2026-05-11 收尾 |
| **OBS-2 Trace 数据模型统一（M0-M3）** | 2026-05-02 | `cf9123c` + `06b2372` + `edd5a21`(M0) + `4412729`(M1) + `26023c5`(M2) + `69ee35b`(M3) | [交付索引](delivery-index.md)；需求包已归档 |
| **xiaomi-mimo provider** | 2026-05-02 | `08a6d4d` + `afa8cf4` + `f6be1ab` | [交付索引](delivery-index.md) |
| SKILL-IMPORT-BATCH | 2026-05-01 | `478e135` | [交付索引](delivery-index.md) |
| SKILL-IMPORT | 2026-05-01 | `a09a17a` + `4490e14` | [交付索引](delivery-index.md) |
| MSG-1 + BUG-30 + BUG-31 | 2026-04-30 | `543a60e` | [交付索引](delivery-index.md) |
| P9-2 + BUG-32 | 2026-04-30 | `fe0404c` | [交付索引](delivery-index.md) |
| SKILL-LOAD | 2026-04-30 | `8d1fb45`(merged via `d74e01c`) | [交付索引](delivery-index.md) |
| P1-D | 2026-04-30 | `6b19439` + `223e5a8`(V33→V38 fix) | [交付索引](delivery-index.md) |
| CTX-1 | 2026-04-30 | `1de1fe1` | [交付索引](delivery-index.md) |
| OBS-1 | 2026-04-29 | 见交付索引 | [交付索引](delivery-index.md) |
| P1-C | 2026-04-28 | `555bdba` | [交付索引](delivery-index.md) |
| Memory v2 | 2026-04-27 | `9f36b59`, `8330d32`, `86703ed`, `96676b9` | [交付索引](delivery-index.md) |
| BUG-F | 2026-04-26 | `e9b48f3` | [交付索引](delivery-index.md) |
| SEC-2 | 2026-04-25 | 见交付索引 | [交付索引](delivery-index.md) |
| thinking-mode-v1 | 2026-04-25 | `55969db` | [交付索引](delivery-index.md) |

## 暂缓

> **2026-05-20 新增 3 项**（user 提 + 我在 FLYWHEEL-PER-RUN dogfood 时发现的 dual-schedule bug）:
> - **USER-SSO-MULTITENANT** (P1) — SSO 认证 + 多租户工作空间隔离（当前 single-tenant dogfood, owner_id=1 / system agent owner=1）。需要做：identity provider 接入 (OIDC / SAML) / `t_workspace` 或 `t_tenant` 表 / 所有 agent/skill/session 加 tenant_id / RLS or service-layer scope filter / FE login flow / RBAC 基础。Full 红灯（schema + 协议 + 跨模块）。**触发条件**：开放给真用户 / 团队协作 / 公网上线前必做。
> - **FE-RENDER-AUDIT** (P2) — 审计 dashboard 各 page 用户点按钮是不是重复渲染多次（如 Insights tab 切换 / Drawer 开关 / sidebar selected change）。手段：React DevTools Profiler + `<Profiler onRender>` 包关键 page / 找 useMemo 缺 / useCallback 漏 / context 太大 re-render 全树。**触发条件**：dogfood 期间有顿、复杂 panel 点击有视觉 lag、或加新 feature 前先把 baseline 性能锁住。
> - **CRON-DUAL-SCHEDULE-FIX** (P1 bug, NOT feature) — `AttributionDispatcherService.java:173,356` Spring `@Scheduled(cron="0 15 * * * *")` annotation hardcoded，跟 `t_scheduled_task #6 attribution-dispatcher-hourly` 同 cron 但**完全独立、不读 DB enabled**。Dashboard 关掉 task 后 Spring annotation 还在 fire → 每小时仍创建 OptEvent。修法 path A 推荐：删 @Scheduled annotation，完全 delegate to UserTaskScheduler / t_scheduled_task （UI 控制单一真实）。Mid 档，pure BE 改动，1-2 文件。**触发条件**：现在就该修，每小时浪费 token + sidebar 列噪音 + user UI 表达跟 BE 行为不一致。

> **ROI 优先级概览（2026-05-10 audit）** —— 点 ID 跳到下方详细行查看完整方案 + 触发条件
>
> | 优先 | ID | Cost | Impact | 何时做 |
> | --- | --- | --- | --- | --- |
> | 🔴 P1 战略 | **USER-SSO-MULTITENANT** | Full 红灯 (schema + 协议 + 跨模块) | 高 — 开放给真用户 / 团队协作 / 公网上线前必做 | 单用户 dogfood 阶段过渡到多用户时 |
> | 🟡 P2 | **FE-RENDER-AUDIT** | Light ~1-2d FE 调研 + Profiler 报告 + targeted fix | 中 — dogfood 期间有顿 / 复杂 panel 点击有 lag / 加新 feature 前 baseline | 用户报顿或上 1-2 个新 panel 之前 |
> | 🔴 P1 **bug** | **CRON-DUAL-SCHEDULE-FIX** | Mid ~半天 BE | 高 — UI 关 schedule 不生效 / 噪音 OptEvent + 浪费 token | **现在就修** |
> | 🟢 P0 | MEMORY-DEDUP-COSINE-ACTIVATION | 0 新代码 + 1 migration | 中-高 — memory 质量保障 | 拿到 embedding API key 立刻激活 |
> | 🟡 P1 | DATA-LAYER-ORIGIN-SPAN-LINK | ~80 行 + 1 migration | 中 — chat "展开看截断 tool 原文" | 用户调试大 tool 输出痛点 |
> | 🟡 P1 | SUBAGENT-APPROVAL-FORWARD | Mid ~80-130 行 | 中 — sub-agent 审批转发到主 chat | 用户多次报"sub-agent 卡住" |
> | 🟡 P1 | GAP-FILEEDIT-MULTIMODAL (4a Read-before-Edit) | Mid ~100 行 | 中 — 防 stale edit 覆盖外部修改 | 用户报 stale edit |
> | ⏩ 已升级到当前队列 | ~~SKILL-SECURITY-SCAN~~ | Full | **高** — 防 OpenClaw 类注入 | 见 [当前队列](#当前队列) |
> | 🔴 P1 战略储备 | GAP-PRETOOL-HOOK-PERMISSION | Full | 高 — 三级权限 + 危险命令 backstop | 公网用户 / 真 prod / SKILL-SECURITY-SCAN 互补 |
> | ✅ 已交付 | ~~MULTIMODAL-MVP Phase 1 (Image + PDF)~~ + ~~Phase 1.5 redesign (single picker + 多模态 chip)~~ + ~~Phase 2 (inline thumbnails + PDF chip)~~ | Full | 高 — Chat 上传图片/PDF + agent 主模型 picker 多模态 chip + 上传 gate + 真缩略图 lightbox | 见 [最近完成](#最近完成) |
> | 🟢 P2 多模态续作 | **MULTIMODAL-IMAGE-COMPRESSION** | Mid ~100 行 | 中 — 大图省 token + 防 provider 拒 | 用户上传大图后 LLM 拒绝或 token 飙升（强烈推荐下一个做）|
> | 🟦 P2 多模态续作 | **MULTIMODAL-OBSERVABILITY-COLUMNS** | Light ~50 行 + V73 | 中 — debug PDF / image 失败原因 | 客户增多后 PDF 解析问题反复，没字段只能翻日志 |
> | 🟦 P2 多模态续作 | **MULTIMODAL-PDF-SCAN-FALLBACK** | Mid ~200 行 | 低-中 — 扫描型 / 图片型 PDF 真处理 | 用户上传扫描件 PDF 后 LLM 拿到空文本 |
> | 🟦 P3 多模态续作 | **MULTIMODAL-ATTACHMENT-CLEANUP** | Light ~100 行 | 低 — 物理文件 24h 孤儿清理 | 磁盘膨胀 / 长时间不重启 server |
> | 🔴 P3 战略储备 | **AUDIO-INPUT-MVP**（多模态续作）| Full | 中-高 — 音频上传 + ASR | 用户需要会议录音 / 语音输入 |
> | 🔴 P3 战略储备 | **MULTIMODAL-VIDEO-FILE** | Full ~600-1000 行 | 中 — 视频文件帧抽 + ASR | 用户开始上传视频要求理解 |
> | 🟦 P4 战略储备 | **MULTIMODAL-VIDEO-LINK** | Full ~800-1500 行 | 中 — B 站 / YouTube 链接抓取 + 帧抽 + ASR | 用户给视频 URL 期望分析 |
> | 🟦 P4 战略储备 | **MULTIMODAL-WORD-EXCEL** | Full ~400-800 行 | 中 — Word / Excel / CSV 表格抽取 | 用户上传业务文档 |
> | 🟡 P2 | EVAL-PARALLEL-SCENARIO | Mid ~100-150 行 | 中 — dataset >50 case 加速 | dataset 大 / nightly auto-eval 痛点 |
> | 🟡 P2 | REMINDER-MVP-PARTIAL-PATHS | Light ~30-50 行 | 低 — reminder feature 覆盖率（用户暂不做）| 触发概率低 |
> | 🟡 P2 | EVAL-SCENARIO-EXTRACT-DEDUP | Light ~30-50 行 | 低 — dataset 重复条目防爆 | 用户感知重复时 |
> | 🔴 P2 | LSP-TOOL | Full ~3-5 day | 中-高 — IDE 级代码导航 | 大型 codebase 调研 / 重构频繁 |
> | 🔴 P2 | STREAM-TOOL-EARLY-DISPATCH | Full ~480 行 | 中 — 1-3s/turn latency 节省 | 接入 Claude API key 后立做 |
> | 🟦 P3 | AUDIO-INPUT-MVP | Full TBD | 中 — 音频上传转写进 chat | 用户需要语音/会议录音输入 |
> | 🟦 P3 | MEMORY-EVAL-QUALITY-GATE | Full TBD | 中-高 — memory 变更后的质量退出策略 | memory 提取/合成后出现错误记忆或回答质量下降 |
> | ⏩ Phase 1 已升级到当前队列 | ~~EVAL-DYNAMIC-USER-SIM Phase 1~~ → SKILL-AB-MULTITURN-FIX | Mid | 中-高 — skill A/B 多轮 fallback 修复 | 见 [当前队列](#当前队列) |
> | 🟦 P3 | EVAL-DYNAMIC-USER-SIM Phase 2/3 | Full TBD | 中-高 — skill 优化闭环的动态多轮验证 | 等 SKILL-AB-MULTITURN-FIX 实测后开独立包 |
> | 🟦 P3 | OBS-2-ADMIN-RUN-ETL | Mid | 低 — 运维痛点 | ETL 频繁触发时 |
> | 🟦 P3 | P10-INLINE-CMD-RESULT | Mid ~80-150 行 FE | 低 — UX 优化 | 用户觉得 modal 体验差 |
> | 🟦 P3 | SKILL-EXTRACT-MODEL-PICKER | Mid ~80-150 行 | 低-中 — 切模型不用改代码重启 | 想给非默认 provider 跑抽取 / 多 provider 对比 |
> | 🟦 P3 | SKILL-EXTRACT-AND-AB-VIA-AGENT | Full ~500-800 行 | 中-高 — 抽取 + A/B Eval 都走 agent loop | LLM 单 prompt 抽得不够准 / A/B Eval 想用 tool 拿 session 细节 / 想接 session 外数据源 |
> | 🟡 P2 | **FLYWHEEL-VISUAL-STATUS** | Mid ~2-3 天 | 中-高 — 飞轮 9 步 + 三 surface 完整可视化，operator 一眼看现在到哪步 | dogfood 期数据多了 / 想跟 stakeholder 演示 / 看 attribution-curator 跑得对不对 |
> | 🟡 P2 | **SYSTEM-AGENT-TYPING** | Mid ~2-3 天 | 中 — t_agent 加 agent_type enum 区分 user/system + FE AgentList toggle + AgentDrawer 锁 system agent 关键字段 + /insights/system-agents 监控面板 | system agent (memory-curator/session-annotator/metrics-collector/attribution-curator/user-simulator) 混在 user agent 列表里 / 用户误改 system agent system_prompt 可能破飞轮链路 / 想看 cron 跑得对不对没集中入口 |
> | 🟡 P2 | **DYNAMIC-SIM-LIVE-TRANSCRIPT** | Light ~30 行 BE | 中 — V5 UserSim trial 跑过程中 dashboard 实时刷 transcript | 不用 polling 不优雅，等 BE `SimulatorTrialOrchestrator.runPingPongLoop` append 后 publish WS event 接 useChatWebSocket 同款 topic |
> | 🟦 P4 | SKILL-VERSION-MAIN-DETAIL-REFACTOR | Full 红灯 ~600-1000 行 | 中 — 架构改善 | 多 owner / multi-tenant 需要时 |
> | 🔴 P2-P3 调研 | GEN-UI-HTML-RENDERING | Full ~600-1000 行（先调研选型）| 高 — 跟上 LLM 直产 HTML 趋势（Claude Artifacts / MCP Apps SDK / Generative UI）| 用户报需 chart 可视化 / 接 MCP UI client / 竞品压力 |
> | 🟦 deferred | P9-4 partial compact | — | — | microcompact 覆盖大部分 |
> | ⏩ 已升级到当前队列 | ~~LLM-driven weekly synthesis (V2 推迟项)~~ → 升级为 **MEMORY-LLM-SYNTHESIS**（2026-05-11 用户驱动 + 业界调研后 Full scope 入队列）| — | — | 见 [当前队列](#当前队列) |
> | ✅ 已交付 | ~~EVAL-LATENCY-DIM-EXPLICIT-NULL~~ (2026-05-11 Mid pipeline) / ~~MEMORY-DREAM-CONSOLIDATION~~ (`05d0593` `6468fe3` `118f887`) / ~~REMINDER-FE-CONTENT-TYPE-DRIFT~~ (`67d50bb`) / ~~GAP-PROMPT-CACHE~~ (P13) / ~~GAP-MCP~~ Client (P11) / ~~SKILL-FROM-SESSION~~ / ~~EVAL-SKILL-EVOLVE-USE-FAILURES~~ / ~~OBS-3~~ (OBS-4) | — | — | — |

| ID              | 原因                                                      | 重评触发条件                                                | 文档                                                                      |
| --------------- | ------------------------------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------- |
| **P9-4**        | 按位置切的 partial_head/partial_tail 痛点已被 microcompact（按内容粒度切 + 不调 LLM 零 token 成本）更精确覆盖；partial_tail 几乎无场景 | microcompact 落地后用户报告"还是想要按位置切的 mini summary" / 出现 microcompact 不能解决的真实场景 | [需求包](requirements/deferred/P9-4-partial-compact/index.md) |
| BUG-G           | 根因已修，剩余 sanitizer / 尾部不变量属于防御性补强                        | 再次出现 dangling assistant `tool_use` / 缺失 `tool_result` | [需求包](requirements/deferred/BUG-G-defensive-hardening/index.md)         |
| SEC-1           | 当前本地 / 单用户使用下重要但不紧急                                     | 多端部署、共享环境或 P12 正式上线                                   | [需求包](requirements/backlog/SEC-1-channel-config-encryption/index.md)    |
| P14             | 暂无真实多轮 benchmark 需求                                     | 出现 tau-bench 类真实业务评测需求                                | deferred package TBD                                                    |
| MEMORY-EVAL-QUALITY-GATE | 合并旧 `P3-2 Memory Eval 模式` + `P3-4 记忆质量闭环`：memory 提取 / 合成 / dream consolidation 后跑 memory eval；如果质量下降，则阻止、隔离或标记本次 memory batch，作为“评测后退出策略” | 出现错误记忆污染回答、memory 合成后质量下降，或需要让 MEMORY-LLM-SYNTHESIS 从人审 gate 升级到 eval gate | 需求包待真做时建；旧编号 `P3-2/P3-4` 不再单独维护 |
| P15-3/P15-6     | Analyzer MVP 暂不需要 eval run 读取或分析 tab 落库                 | Analyzer MVP 证明有持续价值                                  | deferred package TBD                                                    |
| SCHEDULED-TASK-SYSTEM-OPS | 合并旧 `P12-3 SystemJobRegistry` + `P12-6 可靠性/权限`：系统级 scheduled job registry、UI 可观测、告警推送、admin 权限边界和可靠性增强；不与 OBS 诊断项合并 | 系统任务数量增加、需要 admin 观测/操作，或出现 scheduled task 失败无人感知的问题 | 需求包待真做时建；旧编号 `P12-3/P12-6` 不再单独维护；参考 [P12](requirements/archive/2026-05-07-P12-scheduled-tasks/index.md) |
| P10-4/P10-5     | 四条命令 MVP 已覆盖主要场景                                        | 出现明确自定义命令或 help menu 需求                               | [P10](requirements/archive/2026-05-07-P10-slash-commands/index.md)                  |
| OBS-DIAGNOSTIC-LENSES | 合并旧 `OBS-1-4 压缩验证视角` + `OBS-1-5 Provider quirk 诊断信号`：trace/session 详情页的诊断层，包括 compact 前后对比、provider quirk 自动检测、tool_use/tool_result pairing 问题提示；不与 scheduled task ops 合并 | 排查 compact / provider / tool-call 问题时仍需要人工读 raw payload，或重复出现同类 provider 兼容事故 | 需求包待真做时建；旧编号 `OBS-1-4/OBS-1-5` 不再单独维护；参考 [OBS-1 PRD](requirements/archive/2026-04-29-OBS-1-session-trace/prd.md) |
| **P10-INLINE-CMD-RESULT** | P10 5 只读命令（/skill /tool /context /help /models）当前用 modal 弹窗显示 result。用户 2026-05-07 反馈：modal 切窗口不自然，希望在 chat 列表里 inline 渲染（不污染 LLM 上下文 = BE 不写 session_message 已实现，前端只需加 per-session local React state） | 用户实际用 5 只读命令时觉得 modal 体验差 | 主旨：FE only 改造（BE 不动）。ChatWindow.tsx 加 `localCommandResults: Map<sessionId, CommandResult[]>` state，命令 result 不存 DB 仅 React state，chat 列表渲染时按时间顺序混入 messages，新消息类型 `command_result` 特殊样式（灰背景 + 🔧 角标 + markdown body 渲染）。删 CommandResultModal.tsx + 引用。切 session / 刷新页面 result 消失（接受）。Mid 档，约 80-150 行 FE 改动 + 测试反断言。类似 Claude Code / Cursor 的 /help 设计 |
| SKILL-UNINSTALL | dashboard delete 按钮已能用但不清 `t_agent.skill_ids`（silent dangling）；agent 没对应卸载 Tool；本期 SKILL-IMPORT 后浮现的对称缺口 | agent 主动需要卸载 skill / 用户报告删 skill 后 skill_ids 残留 stale 名字 | 主旨：UninstallSkill Tool（dry-run + confirm 双调用）+ SkillService.deleteSkill 内部加 unbind 修复（dashboard 同步受益）+ 全局扫所有 t_agent 解绑 + system 禁卸 + 不调 `npx clawhub uninstall`。Mid 档，需求包待真做时建 |
| WAITING-SUBAGENT-UX | SubAgent / TeamCreate 异步派发后，主 agent loop 结束 → `runtime_status=idle`，但 sub-agent 还在 running。UI 没有"等待 N 个 sub-agent"指示，用户误以为卡住会重发消息（session `2ad5da4d` 现场） | 多次用户反馈"主 agent 不动了"补消息后才发现 sub-agent 还在跑 / collab 页面信息不够 | 主旨：session 级 `pending_subagent_count` 字段（从活跃 collab_run 计算，per-turn refresh）+ chat header 显示"等待 N 个 sub-agent" badge + hover 展开看 handle / status / duration。语义类似 MSG-1 waiting_user 但对应 waiting_subagent。Mid 档，需求包待真做时建 |
| **SUBAGENT-APPROVAL-FORWARD** | `ChatService.java:557-562` `s.setRuntimeStatus("waiting_user")` + `broadcaster.sessionStatus(sessionId, ...)` 把审批 card 推送目标固定为当前 session id —— subagent 触发 install / dangerous tool confirmation 时 card 只推到 subagent sessionId，主 agent chat 看不到，用户必须跳到 subagent chat 才能审批，常被误以为 subagent 卡死（2026-05-04 用户现场报告） | 用户多次反馈"subagent 卡住没反应"（实际是等审批）/ 接 channel gateway 后陌生用户场景增加 / [GAP-PRETOOL-HOOK-PERMISSION](#) 推进时一并设计 | 主旨：ChatService 触发 confirmation 处加分支 `if (parentSessionId != null)` 同时往主 agent session 推 ws + 持久化 ask_user message 到主 agent session（MSG-1 payload jsonb 加 `sourceSessionId=child session id`，**不改 schema**）+ 前端 chat ask_user card 渲染检测 sourceSessionId 加 "From subagent X" 头部标签 + Approve 按钮调 sourceSessionId 的 confirm endpoint。决策：Q1(a) 仅推直接父 / Q2(b) subagent chat 也保留 card / Q3(a) 主 agent 状态不变 / Q5(a) 多 card 时间序堆叠。**Mid 档**（触碰 ChatService 核心文件但仅延伸推送路径不改不变量），~80-130 行（30 BE + 40 FE + 测试），需求包待真做时建 |
| ~~**GAP-PROMPT-CACHE**~~ | **superseded by [P13 PROMPT-CACHE-MVP](requirements/archive/2026-05-08-PROMPT-CACHE-MVP/)（2026-05-08 已交付）**。5 provider 全链路 + Claude 3 breakpoint + dashboard hit rate 闭环。本条 GAP 当时分析了 ClaudeProvider 没结构化 system prompt + cacheReadTokens/cacheCreationTokens 字段 stub 的问题；P13 完整解决（system 改 structured array + cache_control / UsageNormalizer 跨 5 provider / V62 migration / dashboard CacheStatsRow） | — | — |
| **GAP-PRETOOL-HOOK-PERMISSION** | `HookEvent.java:14-18` 枚举只 5 个事件（SESSION_START / USER_PROMPT_SUBMIT / **POST_TOOL_USE** / STOP / SESSION_END），**无 PRE_TOOL_USE**；`Tool.java:13-41` 接口仅有 `isReadOnly()`，**无 `checkPermissions()`**；`AgentLoopEngine.java:1053` 的 `isInstallRequiringConfirmation` 是**硬编码** `npx clawhub install / npm install` 几个特定命令不可配置；无 alwaysAllow / alwaysDeny / alwaysAsk 三级规则；危险命令（Bash `rm -rf` / FileEdit `/etc/**`）目前直接执行无 backstop。**可吸收 BUG-27** | 接 channel gateway 后陌生用户场景 / BUG-27 类反馈累积 / 真实 prod agent 误执行破坏命令 | 主旨：`HookEvent` 加 `PRE_TOOL_USE` 事件 + `Tool` 接口加 `checkPermissions(input, ctx) → allow/ask/deny` 默认 ALLOW 向后兼容 + 三级规则来源（session/agent/global）含 pattern 匹配 `Bash(git *)` / `FileEdit(/etc/**)` + DENY 时 stderr 拼给 LLM 重 plan + ASK 走 MSG-1 inline confirmation card 复用。**P1 Full**（核心 AgentLoopEngine + Tool 接口 + 规则 schema + dispatcher，触碰核心文件清单）（来源：Claude Code 02 §7 三层权限 + 07 Hooks PreToolUse + Hermes 02 approval callback），见 `/tmp/skillforge-gap-analysis-r2.md` Gap 2 |
| ~~**GAP-MCP**~~ | **MCP Client 部分 superseded by [P11 MCP-CLIENT-MVP](requirements/archive/2026-05-07-MCP-CLIENT-MVP/)（2026-05-07 已交付）**：MCP stdio transport + per-agent `mcp_server_ids` + dashboard CRUD + dogfood time server 闭环。**MCP Server 部分（把 SkillForge 暴露给 VS Code / Claude Desktop / Cursor）尚未做** —— 如果未来需要可独立提需求包，命名 MCP-SERVER-MVP | 用户想反向把 SkillForge 当 MCP server 暴露给外部 IDE / Desktop | MCP Server 端实施清单（待真做时建独立需求包）：HTTP-SSE transport + `/mcp` endpoint + tool/resource/prompt 暴露 + auth + 跨 OS bundling |
| **GAP-FILEEDIT-MULTIMODAL** | `FileEditTool.java:64-120` `execute()` 不验"本 session 在该 file_path 上 Read 过且 mtime 未变"，agent 跨 turn edit 陈旧文件不报错（external 修改被 silent 覆盖）；`FileReadTool.java:1-60` 不识别 image / PDF / Jupyter notebook（无 magic bytes 分支）；**`OpenAiProvider.java:537` 注释承认 "image / tool_use blocks ... silently dropped"**，即便 `mimo-v2-omni` / Claude Vision 等多模态模型也用不上 | 用户报 stale edit 覆盖外部修改难调 / 想上传截图 / PDF 给 agent 看图 / 用了 omni 模型但 image 不通 | **保留 4a，吸收 4b**：(4a) **Read-before-Edit precondition** (P2 Mid)：`SkillContext` 加 `readFileState` LRU + FileEdit 验 "this session 读过该 path + mtime 未变"；原 (4b) **Multimodal Read** 不再单独做，已并入 `MULTIMODAL-MVP Phase 1`，因为没有 Chat 上传入口就无法端到端验证图片/PDF 能力。见 `/tmp/skillforge-gap-analysis-r2.md` Gap 4 |
| **OBS-2-ADMIN-RUN-ETL** | OBS-2 M2 / OBS-1 R__migrate_legacy_llm_call 等 ETL 当前都通过 `application.yml etl.<x>.mode: off \| flyway` placeholder + Flyway hash 切换 → **必须重启 server 才能跑**。重启是重操作（影响 chat 在线用户 + WS 连接断开 + 状态机要恢复）。运维流程"切 mode → 重启 → 切回 → 再重启"= 4 次重启 | 实际跑 OBS-2 M2 / 后续任何 ETL 类回填时（频率 = 数月一次但每次都重）/ 其他 schema 演进引入更多 Repeatable migration | 主旨：admin endpoint `POST /api/admin/observability/run-etl` body `{etl: 'legacy_llm_call' \| 'legacy_trace_span', dryRun: boolean}` → 内部直接 execute 对应 R__ SQL（绕过 Flyway hash + placeholder 切换） + ownership/role guard（只允许 admin user）+ dryRun 走 transaction-rollback 看影响行数 + 真跑返回 INSERT/UPDATE 行数。后续也可加其他维护性 SQL（dual-write 一致性核对 / orphan 清理 / blob TTL 等）。**P2 Mid**（不触碰核心文件，纯新 controller + service + auth），需求包待真做时建 |
| ~~**EVAL-SKILL-EVOLVE-USE-FAILURES**~~ | **superseded by [SKILL-EVOLVE-LOOP](requirements/archive/2026-05-08-SKILL-EVOLVE-LOOP/)（2026-05-08 已交付 Phase 4）**。`EvalTaskRepository.findRecentFailuresForSkill` 已加 + `SkillEvolutionService.callLlmForImprovement` 已拼最近 5 个 composite_score < 60 失败进 LLM prompt | — | — |
| **EVAL-PARALLEL-SCENARIO** | `EvalOrchestrator.runEval()`（`skillforge-server/.../eval/EvalOrchestrator.java:124,136-337`）注释 `// 5. Run each scenario sequentially (v1.0)`，scenario 全串行执行 LLM 调用。AbEvalPipeline / SkillAbEvalService 同样串行。EVAL-V2 M0+M1 已为 >50 case throttle progress = dataset 增长是预期。100 个 scenario 串行 ≈ 10-20 分钟 | dataset 大到 eval 任务 >10 分钟用户开始抱怨 / 想做 nightly auto-eval 但跑不完一晚 | 主旨：scenario 级**有界并发**（`Semaphore` 限 2-4 worker，不是 `parallelStream` 一把梭）+ LLM provider rate-limit 处理（429 backoff）+ 结果聚合线程安全（concurrent collection 收集 case 后顺序写 t_eval_task_item）+ EvalJudgeTool 同步并发，sandbox session 互不干扰。**P3 Mid**（约 100-150 行 + 并发测试），需求包待真做时建 |
| **EVAL-SCENARIO-EXTRACT-DEDUP** | `SessionScenarioExtractorService.extractFromSessions()` 批量提取后直接 `saveAll`，没检查是否已有相同 task 的 scenario draft；`extractFromSession` 单 session 版本同样无去重。随时间推移同一用户的重复操作会产生大量语义相同 draft，污染 dataset 库 | 用户反馈 dataset 出现明显重复 task / draft 数量爆炸但有效 scenario 没增加 | 主旨：保存前 embedding fuzzy match（`EmbeddingService` 基础设施已存在）+ 兜底 task 文本归一化后 hash 去重；命中已有 draft 时合并 source_session_ids 而不是新建。**P3 Light**（约 30-50 行 + 测试），需求包待真做时建 |
| ~~**EVAL-LATENCY-DIM-EXPLICIT-NULL**~~ | **已交付（2026-05-11 Mid pipeline，详见"最近完成"区第一行 + delivery-index）**：M4_V2 latency not_measured + 综合分按 measured 维度（quality+efficiency+cost 权重 0.85）重归一 + dimensionStatus 字段透传 wire + FE chip/header/cell/chart 全显示 "not measured"。1187 测试全绿 + npm build 0 error + 无 schema migration。实际改动 ~1040 行（比预估 40-60 行大，因 reviewer W1 dimensionStatus 透传契约完整性 fix） | — | — |
| ~~**SKILL-FROM-SESSION**~~ | **superseded by skill-creator 系统 skill**（2026-05-08 用户决策）。用户在某 session 里想沉淀直接跟 agent 说"把这个 session 提炼成 skill"，`skill-creator` skill 会读 session 写 SKILL.md 注册，**自然语言入口已覆盖**。专门 button + review drawer = 多余 UX 包装，不创造新能力。如果未来 skill-creator 不够用、用户开始反馈"想沉淀但不知道跟 agent 怎么开口"再考虑做 button | — | — |
| **EVAL-DYNAMIC-USER-SIM** | 与当前 `SKILL-AB-MULTITURN-FIX` 对齐：Phase 1（`SkillAbEvalService` multi-turn fallback 修复）已拆成当前队列 Mid 包；本 backlog 只保留 Phase 2/3。SkillForge 已有 skill 生成/优化闭环，但 `SessionScenarioExtractorService` 仍只是机械抽 turns，不抽业务目标/约束/成功标准；也没有 LLM user simulator 来验证优化后的 skill 在真实多轮业务流程里是否减少轮数、耗时、绕路和失败 | skill 优化后需要判断“本次 `SKILL.md` 改动是否真的提升多轮业务完成率”；用户不想每次手工模拟对话；skill 使用中出现轮询次数长、耗时长、任务完成率低，需要把 session 分析建议转成可复测评测 | Phase 2/3 待 Phase 1 实测后独立开 Full 需求包：增强 session→scenario 抽取，输出 `businessGoal` / `successCriteria` / `userPersona` / `userConstraints` / `failureSignals` / `expectedOutcome`；新增 LLM `UserSimulatorAgent`，按 goal/context/persona 与 skill agent 动态对话；process-level judge 对完整 transcript 评分（任务完成、约束满足、重复解释、工具调用、轮数/loops/tokens/latency）。不并入 P14 tau-bench；字段进 column vs JSON、runner 抽取、动态结果是否进入 promote gate 均留待新包 ratify。 |
| **STREAM-TOOL-EARLY-DISPATCH** | 当前 AgentLoopEngine 等 LLM 流式 [DONE] 后才 parse tool_use 调 tool，latency 累积每轮 1-3 秒。Claude code v2.1.88 在流式过程中识别 tool_use block 完整 (`input_json_delta` 累计成完整 JSON 后) 立即 dispatch，多 tool_use 还能并行。**2026-05-08 调研后延后理由**：(1) 项目 default-provider=bailian（OpenAI 系），ClaudeProvider 当前**无 API key**，整个 ClaudeProvider 是空架子；(2) Claude SSE 有 `content_block_stop` 显式事件适合 early-dispatch，OpenAI 协议无该事件，要做需启发式判断 `tool_calls.index` 转跳 / `finish_reason`，加 ~300-500 行；(3) 若先做 Claude-only ~480 行 shelf warming code 当前 0 benefit。**用 Tool.isReadOnly() 分类调度**（read-only 并行 / write 串行）替代加锁，无需 SkillContext audit。完整调研报告 `/tmp/stream-tool-research.md`（718 行）。**触发重启**：SkillForge 接入 Claude API key / 决定主推 Claude provider / OpenAI 也要早 dispatch（V2 一并做）| 用户感知 agent 响应延迟 / multi tool_use 想并行执行 / 长 stream 体感卡 / **接入 Claude API 后立刻有 1-3s 节省** | 主旨：调研已完成（参考 `/tmp/stream-tool-research.md` Section D Option 1）。Phase 1 只做 Claude-only StreamingToolExecutor + AgentLoopEngine.handler.onToolUse 立刻 dispatch + read/write 分类调度 + feature flag 默认 off，~480 行 + 测试。**P1 Full**（核心 AgentLoopEngine + LlmStreamHandler 触碰核心文件清单），需求包待真做时建 |
| ~~**MEMORY-DREAM-CONSOLIDATION**~~ | **已交付并归档（2026-05-08，`05d0593` `6468fe3` `118f887`）**：Dream Consolidation cron 每天 03:30 跑 `MemoryConsolidator.consolidate(userId)`；支持 `skillforge.memory.consolidation.scheduled-enabled` yaml gate、`POST /api/admin/memory/consolidation/run-once?userId=` 手动触发、dashboard Memory 页 Run Consolidation 按钮、V66 `t_memory.archived_reason` 归档原因字段，以及 detailed totals 返回。需求包已移到 [archive](requirements/archive/2026-05-08-MEMORY-DREAM-CONSOLIDATION/index.md)，完整事实见 delivery-index。 | — | — |
| **AUDIO-INPUT-MVP** | 音频输入与图片/PDF不是同一链路：需要 ASR/transcription provider，而不是 vision provider。MULTIMODAL-MVP Phase 1/1.5/2 已交付图片+PDF，音频独立列。 | 用户需要上传语音、会议录音、访谈音频，期望 agent 基于 transcript 继续工作 | 主旨（独立需求，待真做时建包）：Chat 上传 `mp3/wav/m4a/ogg/flac` 等音频 → 存 attachment（复用 `t_chat_attachment` + 加 kind='audio'）→ 调 transcription provider 转文本 → transcript 注入当前 user message，可选保存原音频链接和转写元数据。Provider 候选：Whisper（本地或 OpenAI）/ Qwen-Audio / DashScope ASR / 字节 Volc ASR。MVP 不做实时语音 / 不做 speaker diarization / 不做音频直接喂模型 / 不做逐字时间戳编辑。**Full TBD ~300-500 行**（新 provider abstraction + 成本控制 + 长音频限制 + attachment 生命周期 + FE upload type allowlist 扩 + ChatAttachmentService 加 transcribe 路径）。 |
| **MULTIMODAL-OBSERVABILITY-COLUMNS** | 现状：`t_chat_attachment` 只有 `status` (`uploaded` / `bound`)；PDF 抽取失败 / image processing 失败 / MIME-magic mismatch 等都没结构化记录在 DB，运维只能翻 BE 日志找原因。MULTIMODAL-MVP Phase 1 tech-design 原本规划了 4 个观测字段但未实施。 | 客户增多后 PDF / image 处理问题反复出现，没字段就只能 grep server log 逐行查；用户报"这个 PDF 没识别出来"无快速诊断手段 | 主旨：(a) V73 migration 加 `error_code VARCHAR(80)` / `error_message TEXT` / `processing_mode VARCHAR(50)` / `extracted_text_chars INT` 四列；(b) `ChatAttachmentService.upload` + `materializeForProvider` + `pdfTextBlock` 失败路径 set 错误码（候选码：`MIME_MAGIC_MISMATCH` / `PDF_PARSE_FAILED` / `PDF_TEXT_EMPTY_NEEDS_VISION` / `IMAGE_TOO_LARGE` / `PDF_PAGE_LIMIT_EXCEEDED`）；(c) 成功路径 set `processing_mode` (`IMAGE_BLOCK` / `PDF_TEXT` / `PDF_PAGE_IMAGE` 待 PDF-SCAN-FALLBACK 落地)；(d) admin REST `GET /api/admin/chat-attachments?errorCode=...` filter。**Light ~50-100 行**（V73 + entity 加字段 + service set 调用点 + 1 admin query endpoint + 测试）。先做完 IMAGE-COMPRESSION + PDF-SCAN-FALLBACK 后再做最有价值（那时才有 processing_mode 真分支）。 |
| **MULTIMODAL-TRACE-ATTR-EFFECTIVE-MODEL** | ~~Phase 1 tech-design 原本规划在每条 LLM call 的 `t_llm_span.attributes_json` 加 `llm.effective_model` + `llm.model_source` 两个 attr，让 admin 不读业务日志就能知道这次 call 用了哪个 model + model id 来自 (`agent.modelId` / `runtimeModelOverride` / `agent.multimodalModelId`)。~~ **Phase 1.5 redesign (2026-05-14) 后 ROI 急剧下降**：effective model 现在就是 `agent.modelId` 或 `/model` runtime override，跟 `t_llm_span.model` 列本身重复；`/model` override 出错率低，diagnostic 增益接近 0。**不做**（除非未来引入 multi-model fan-out 或 model A/B 实验路径才有意义） | — | — |
| **MULTIMODAL-PDF-SCAN-FALLBACK** | 现状：`ChatAttachmentService.pdfTextBlock` 用 `PDFTextStripper.getText(doc)` 抽文字（限 20 页）→ 文本不足时 LLM 收到 placeholder "[PDF attachment: filename]"。扫描型 PDF / 图片型 PDF 无可抽文本 → LLM 等于看不到内容。来源：MULTIMODAL-MVP Phase 1 PRD Ratify 但未实施。 | 用户上传扫描件 / 图片型 PDF 后 LLM 回答类似"PDF 看起来是空的" / 商业场景多扫描件合同 / 学术场景多扫描论文 | 主旨：`ChatAttachmentService.pdfTextBlock` 抽文字长度 < 阈值（如 200 字符）时，用 `PDFRenderer.renderImageWithDPI(page, 150)` 渲前 N 页（默认前 5 页）为 PNG，每页生成一个 `image` ContentBlock 喂 vision provider；ratify 决策（页数上限 / 渲染 DPI / 触发文本阈值）写 PRD。Provider 不支持 vision → 显式 `PDF_TEXT_EMPTY_NEEDS_VISION` 错误 (PRD Ratify 已写)，FE 提示用户切 vision 模型。**Mid ~150-250 行**（PDFBox 已是 dep；FE 错误码处理 + tooltip + chip 显示"扫描 PDF（已转图）"标签）。 |
| **MULTIMODAL-IMAGE-COMPRESSION** | 现状：`ChatAttachmentService.materializeForProvider` 把整张原图 base64 喂 LLM。上传 5MB iPhone HEIC 截图 → 6.6MB base64 → ~880K tokens 单图 → DashScope/xiaomi-mimo 直接拒（context 超）或 token 飙升。来源：MULTIMODAL-MVP Phase 1 PRD 列为 Phase 2 follow-up 未实施。 | 用户上传大图 / 高分辨率截图后 LLM 拒绝或返"图片过大" / 想批量传 multi-image 但 token 超限 | 主旨：image_ref → image materialize 时如果长边 > 2048px 或字节 > 1MB → 用 Java `ImageIO` + `BufferedImage.getScaledInstance` (Lanczos) + JPEG quality 0.85 写新 byte[] 再 base64。长边目标 2048px 是大多数 vision provider sweet spot（Claude Vision / GPT-4o / Qwen-VL）。原图保留在 storage_path 不动（用户下载完整原图仍可），仅 provider 请求用压缩版。**Mid ~80-150 行**（新 ImageScaler util + materializer 接 + 测试 PNG/JPEG/WebP 三 path + DPI 元数据保留）。 |
| **MULTIMODAL-ATTACHMENT-CLEANUP** | 现状：`t_chat_attachment` 有 `status='uploaded' AND seq_no IS NULL` 的孤儿行（上传成功但消息发送失败 / 用户切走 session 没发送）。tech-design 当时接受 24h orphan 作为 Phase 1 trade-off，**实施侧没有 cron job 真清理**。物理文件同理：DB FK CASCADE 只清 row，磁盘文件 `~/.skillforge/data/chat-attachments/<sessionId>/...` 残留。来源：MULTIMODAL-MVP Phase 1 已知 trade-off。 | server 长时间不重启 / 多人 dev 测试 / 客户演示频繁上传 → 磁盘膨胀（GB 级）/ DB row 累积 | 主旨：(a) `@Scheduled(cron = "0 0 4 * * *")` daily 4:00 跑 `ChatAttachmentService.cleanupOrphans()` 查 `seq_no IS NULL AND created_at < now() - INTERVAL '24 hours'` → 删 row + 物理文件；(b) 加 `t_chat_attachment.deleted_at` 软删字段（防误删后恢复）；(c) startup recovery 一次 + admin endpoint `POST /api/admin/chat-attachments/cleanup` 手动触发；(d) metrics：清理行数 / 磁盘释放字节数。**Light ~80-120 行**（无 schema migration 必需，加 `deleted_at` 可选）。 |
| **MULTIMODAL-VIDEO-FILE** | 现状：视频文件（mp4/mov/webm）从未规划。要做需要：(1) frame 抽帧（ffmpeg 或 Java 库 jcodec）→ N 张关键帧 image；(2) 音轨提取 → 走 AUDIO-INPUT-MVP 的 ASR；(3) 元数据（时长 / 分辨率）；(4) 选帧策略（均匀 / scene-change 检测）；(5) provider capability（vision + audio 都要）；(6) FE 加 mp4 type allowlist + 上传进度条（视频文件普遍 >10MB）。来源：用户 2026-05-14 询问。 | 用户上传产品 demo 视频 / 短视频 / 屏幕录制要求 agent 总结 | 主旨（待真做时建独立需求包）：依赖 `AUDIO-INPUT-MVP` 先落地。技术栈：ffmpeg-cli 子进程 / `jcodec` 纯 Java（部署简单但性能差）/ `Bytedeco JavaCV` (FFmpeg JNI)。MVP scope：1 video → 5-10 帧 image + audio transcript → 拼成 user message。**MVP 不做**：实时视频流 / 长视频（> 5min 直接拒）/ scene-change 智能选帧（用均匀采样）/ 视频内 OCR / 物体识别。**Full TBD ~600-1000 行**（依赖 AUDIO + 新 ffmpeg 调用 + storage 优化大文件 + provider capability 检 vision+audio）。 |
| **MULTIMODAL-VIDEO-LINK** | 现状：视频链接（B 站 / YouTube / 抖音 / Twitter / Vimeo）从未规划。要做需要：(1) URL pattern 检测 + 平台路由；(2) yt-dlp 或平台 API 抓 stream URL；(3) 下载到本地 → 走 `MULTIMODAL-VIDEO-FILE` 链路；(4) caption / subtitle 抓取（如果平台提供，可跳 ASR）；(5) 法务 / 版权问题（不在本平台代抓视频内容流通）。来源：用户 2026-05-14 询问。 | 用户给视频 URL 期望 agent 不下载就分析（"看下这个 B 站视频讲了啥"） | 主旨（待真做时建独立需求包）：依赖 `MULTIMODAL-VIDEO-FILE` 先落地（链接走相同的"下载+解析"流水线）。技术栈：yt-dlp 子进程 / RapidAPI / 各平台 official SDK（B 站需 cookie + sign，门槛较高）/ 字幕优先（caption track 比 ASR 快 100x）。MVP scope：YouTube + B 站 + 单 video URL（不做 playlist）。**法务先判**：是否允许下载第三方平台视频到本平台 server。**MVP 不做**：playlist 批量 / live stream / 私域视频 / DRM 内容。**Full TBD ~800-1500 行**（依赖 VIDEO-FILE + yt-dlp 集成 + 字幕抓取 + 法务边界）。 |
| **MULTIMODAL-WORD-EXCEL** | 现状：Word / Excel / CSV 从未规划。Phase 1 PRD 明确排除。FE upload allowlist 限 `image/* + application/pdf`。来源：MULTIMODAL-MVP Phase 1 已知排除项。 | 用户上传业务文档 / 财报 / 数据表期望 agent 读懂 | 主旨（待真做时建独立需求包）：Word 走 Apache POI (`HWPFDocument` / `XWPFDocument`) 抽文本 + 表格转 markdown；Excel 走 POI / EasyExcel sheet by sheet 转 markdown table（限单 sheet 行数 + 列数）；CSV 直接读 + auto-detect delimiter；保留 inline 图片 / 公式 / 注释作为单独 text block。MVP 不做：编辑文档 / 复杂样式还原 / 公式重算 / pivot table / VBA / 图表渲染。**Full TBD ~400-800 行**（POI 依赖 ~15MB / EasyExcel 轻量 / FE upload allowlist 扩 / chip 显示 sheet 数 / page 数）。 |
| **SKILL-VERSION-MAIN-DETAIL-REFACTOR** | 当前 `t_skill` 单表多行 + `enabled` flag + V64 partial unique 同时表达"业务 skill 标识"（owner+name）和"版本"（semver / parent_skill_id）和"在线状态"（enabled），违反主从表设计原则。同 owner 同 name 多行让 list / permissions / audit 都得 group + filter。来源：2026-05-08 用户提出 | 多 owner / 多版本数据膨胀让单表 row 数量过大 / Audit 需要清晰 "skill 实体" vs "版本快照" 分离 / 加 multi-tenant 后权限模型靠 (owner_id, name) tuple 反查难做 / 出现需要 "未发布的开发版本" 这种第三态 | 主旨：拆为 `t_skill_main`（id / owner_id / name UNIQUE / active_version_id / created_at / archived_at）+ `t_skill_version`（id / main_id FK / semver / content_hash / skill_path / source / status enum 'draft'\|'candidate'\|'active'\|'archived' / created_at / promoted_at）。Migration 把现有每个 (owner_id, name) 族折成 1 个 main + N 个 version row，active_version_id 指向 enabled 那一行。删 V64 partial unique（main 的 UNIQUE(owner_id, name) 替代）。**Full 红灯改动** ~600-1000 行：30+ call sites（SkillService / SkillRepository / SkillController / SkillAbEvalService / SkillEvolutionService / SkillDraftService / agent.skill_ids 反查 / SkillRegistry 注册路径 / FE 列表去 group 改直接拉 main）+ V65 + V66 数据迁移 migration。**风险**：迁移 in-place 改动 ~7 个 entity / promote / rollback / fork 全部要双表事务 / FE 多个 endpoint shape 都变。需求包待真做时建 + 完整 mrd/prd/tech-design 流程。**当前接受**：单表方案在 SKILL-EVOLVE-LOOP / DASHBOARD-POLISH 双 commit 后已工作良好，FE 聚合解决可见性问题，partial unique 解决 race —— 没到痛点不重构 |
| **MEMORY-DEDUP-COSINE-ACTIVATION** | Memory dedup 4 层中 L2 (写入时 cosine ≥0.95 update / ≥0.85 merge) + L4 (后台 cron 兜底 cosine dedup) **代码全就绪**（`MemoryService.saveMemory` line 261-283 + `MemoryConsolidator.deduplicateByEmbedding`），但**当前 inactive**：(1) `embedding.enabled=false` 没 embedding API key；(2) PG 没装 pgvector extension（V7 migration silent skip）→ embedding 列不存在；(3) MemoryEmbeddingWorker 没人触发。当前实际只 L1 (exact-title) + L3 (LLM prompt 引导) 工作，**语义相同但 title 不同的 memory 会漏**（empirical 验证 0 同 title 重复，但语义近似没数据）。来源：2026-05-08 用户问题 + MEMORY-DREAM-CONSOLIDATION 落地后发现 | SkillForge 接入 OpenAI / DashScope / Qwen embedding API / 用户反馈 memory 检索质量降 / dashboard 显示同义 memory 堆积 | 主旨：纯配置激活，**0 行新代码**：(a) `application.yml` `embedding.enabled: true` + `EMBEDDING_API_KEY` env / `base-url` 选 OpenAI 或 DashScope；(b) PG 装 `CREATE EXTENSION vector;`；(c) V67 migration 重新加 `embedding vector(1536)` 列 + HNSW idx（V7 silent skip 过了）；(d) backfill worker 给现有 memory 算 embedding；(e) restart server。**P2 Light**（纯运维 + 1 migration），需求包待激活时建 |
| ~~**OBS-3 跨 session unified trace tree**~~ | **superseded by [OBS-4 root_trace_id](requirements/archive/2026-05-03-OBS-4-root-trace-id/)（2026-05-03 已交付）**。OBS-3 v1（UI 层 nested rendering + 启发式 DFS 拼装）2026-05-03 上午尝试后回滚（commit `2d1a7bf`）。教训：数据模型不动 UI 层硬拼凑无法表达"一次完整调研"语义，nested 全展开把 child spans 淹没父主线。OBS-4 用 schema 一等公民字段 `root_trace_id` 把跨 agent / 跨 session trace 串联起来，UI 改成二级折叠 inline group（默认折叠），完整解决该需求 | — | — |
| **REMINDER-MVP-PARTIAL-PATHS** | REMINDER-MVP Phase A（commit `bdb0453`）只在 `ChatService.chatAsync` 主路径注入 `<system-reminder>` 进 user msg content blocks。其它 3 条 user-msg 路径未覆盖：(1) `enqueueUserMessage`（用户在 loop 跑着时插话进队列）(2) `answerAsk`（agent 抛 ask 卡片用户回答）(3) `answerConfirmation`（用户点 yes/no 按钮）。这些路径的 user msg 持久化时直接走 `Message.user(text)` String content。**影响**：cache 命中**完全 OK**（持久化什么 replay 什么、bit-identical），仅 reminder feature 在这 3 类入口的那一 turn 静默不发；下一条 main-path msg 又恢复。最坏后果：context 临近 70% 时用户恰好走 enqueue/ask/confirmation，那一 turn LLM 没收到"该收尾"提醒可能多生成一点；framework auto-compact 兜底无数据丢失 | 用户报告"context 接近溢出但 LLM 没收敛"且能回追到 enqueue/ask/confirmation 路径 / reminder feature 用户感知覆盖率痛点 | 主旨：把 `ChatService.buildUserMessageWithReminder()` helper 推广到 enqueue path（line ~191/207）+ answerAsk + answerConfirmation 调用点，每处 ~10 行替换。**Light 档**（FE 不动，BE ~30-50 行 + 测试），需求包待真做时建 |
| ~~**REMINDER-FE-CONTENT-TYPE-DRIFT**~~ | **已交付（commit `67d50bb`，2026-05-10）**：新增 `src/types/messages.ts` 含 `RawMessage` + `ContentBlock`，BE→FE ingress 类型显式（`content: string \| ContentBlock[] \| undefined` + index signature 兼容未来字段）；useChatMessages / useChatSession / useChatWsEventHandler / ChildAgentFeed / SessionList 5 处接入 `RawMessage`，删 `as any[]` cast。零 runtime 行为变更（strip + collapse 仍由 messageContent.ts 处理，ChatMessage 仍是 `content: string`）；tsc / build / vitest 21/21 全过 | — | — |
| **GEN-UI-HTML-RENDERING** | 现状：dashboard chat / tool 输出 / skill 输出 / reminder / recovery payload **全是文本**（markdown 由 `MarkdownRenderer` 渲，message content_json 仅 `text` / `tool_use` / `tool_result` 三类 ContentBlock），无 HTML 渲染路径，安全模型建在"输出全文本 XSS=0"假设上。**趋势观察**（2026-05-10 用户提）：(a) Claude Artifacts / ChatGPT Canvas 让模型直接产出可交互 HTML/CSS/JS；(b) Anthropic MCP Apps SDK 让 MCP server 推 UI widget resources 到 client 渲染（已有 `mcp-server-dev:build-mcp-app` skill 文档）；(c) Vercel / LangChain "Generative UI" 让 LLM 输出 React 组件 JSON 树客户端渲染；(d) Tool 输出富化（WebFetch 返 sanitized HTML preview / CodeSandbox 返截图嵌入 / EvalScenario 返可视化卡片）；(e) Skill 视觉化输出（图表 / 表格 / mini-app 而非纯文字）。**9 个维度的影响**：(1) 渲染层 + 安全 — XSS 攻击面打开，要 DOMPurify sanitize / iframe sandbox / CSP；(2) ContentBlock 形态扩展 — 加 `'html'` / `'ui_resource'` / `'artifact'` type，BE 持久化不变 FE renderer 按 type 分支；(3) Tool 输出协议升级 — `SkillResult` 加 html factory + sanitize 内置 / `Tool.execute` 返 ContentBlock[] 不止 String；(4) MCP UI 互通 — 跟 P11 MCP-CLIENT 联动，加 resources fetch + ui_resource→ContentBlock 转换；(5) Cache 命中 — HTML token 数比 markdown 多 30-60%，context 满更快、reminder/compact 触发更频繁；(6) Eval 比对策略 — `EvalJudgeTool` 现在文本相似度，HTML 后要 DOM diff / LLM judge 判"视觉/交互等价"；(7) Channel gateway 兼容 — 飞书/Telegram 不能渲 HTML iframe/script，必须给降级路径（strip 标签 / puppeteer 截图 / 拒推送）；(8) 持久化 + replay 一致性 — `t_session_message.content_json` 存 HTML 字面量，下次 LLM call 喂 history 时给 HTML 还是 stripped 文本？前者 token 浪费，后者破 cache 字节一致触发 b2c7039 mid-prefix divergence guard；(9) Skill 输出形态生态 — SKILL.md 加输出形态元数据，`SkillSecurityScanner` 必须扫 HTML 模板 XSS 风险，marketplace 标注 skill 信任度 | 用户报"想要 chart / 可视化输出"频繁 / 接 Claude Desktop / Cursor 等 MCP UI client 时（互通需要）/ 竞品（Cursor / Claude Code chat / Cherry Studio）chat 全都支持 HTML 后压力上来 / 接公网 marketplace + 多用户场景（HTML 安全反而要先成熟）/ Anthropic MCP Apps SDK 在 client 端普及 | 主旨（待真做时建独立需求包，需用户决定 scope）：**短期（3 个月）不必动** —— 当前文本 markdown 覆盖 95% 用户需求，安全模型简单。**中期（半年）做"只读 HTML 卡片"小步**：① ContentBlock 加 `'html'` type；② FE `MarkdownRenderer` 旁加 `HtmlCardRenderer`（DOMPurify allowlist 仅基础 tag 不允 script/iframe）；③ Tool 接口加 SkillResult.htmlCard helper + sanitize 内置；④ MarkdownRenderer 自动识别 ` ```html ` 代码块升级渲染（向后兼容）；⑤ Channel gateway 双轨：dashboard 渲 HTML，channel 自动降级文本；⑥ Eval 加 `htmlEqualSemantic` LLM judge 维度。覆盖 80% 可视化需求且 XSS 风险可控（无 JS）。**互锁 backlog**：与 `SKILL-SECURITY-SCAN`（HTML 输出模板扫 XSS）/ `GAP-MCP` Server 部分（MCP Apps SDK 一并）一起设计；不与 `MULTIMODAL-MVP` 绑定，前者是输出渲染安全，后者是输入附件和模型理解。**P2-P3 Full**（跨 BE/FE/eval/channel/persistence 多路径，~600-1000 行 + 完整 mrd/prd/tech-design），先调研选型再实施 |
| **LSP-TOOL** | 现状：SkillForge 的 code-aware tool 全是文件粒度（Read / Edit / Write / Glob / Grep / Bash），**没有 symbol 粒度的代码导航能力**。Agent 想"找 `SessionService.updateSessionMessages` 所有 caller" 只能 `grep -rn "updateSessionMessages"`，命中含字符串字面量 / 注释 / 测试 mock 等 false positive；想知道某变量的类型推断、import 来源、函数签名也只能 ad-hoc 文本搜。同类工具（Cursor / Claude Code / GitHub Copilot）都接 Language Server Protocol（LSP）拿 IDE 级语义信息（go-to-definition / find-references / hover-info / document-symbols / workspace-symbols / diagnostics / rename）；SkillForge agent 缺这条捷径。来源：用户口头 / 之前讨论提过 LSP-tool 需求，本次（2026-05-10）补到 backlog | 用户在重构 / 大型 codebase 调研场景频繁报"agent 找不全引用" / "改个函数签名 agent 漏改 caller" / 接更多大型项目 codebase 后 grep 噪声成本 N²-级累积 | 主旨（待真做时建独立需求包，需要用户决定 scope）：① **transport 选型**：参考 P11 MCP-CLIENT-MVP 模式实现 LSP stdio transport（JSON-RPC 2.0 with Content-Length header 而非 NDJSON）+ ProcessBuilder 起 language server（Java: `jdtls` / Python: `pyright` / TypeScript: `typescript-language-server` / Go: `gopls` / Rust: `rust-analyzer`）+ workspace folder 配置；② **协议层**：`initialize` / `initialized` / `textDocument/didOpen` / `definition` / `references` / `hover` / `documentSymbol` / `workspaceSymbol` / `prepareRename` 等核心方法；③ **Tool wrapper**：暴露 `LspGoToDefinition(file, line, col)` / `LspFindReferences(file, line, col)` / `LspHover(file, line, col)` / `LspDocumentSymbols(file)` / `LspWorkspaceSymbol(query)` 给 SkillRegistry，复用 `mcp_*` 前缀模式（如 `lsp_<lang>_*`）；④ **per-agent** 启用配置（`lsp_server_ids` 类似 `mcp_server_ids`）；⑤ **dashboard CRUD**（类似 `/mcp-servers` 页面 `/lsp-servers`）；⑥ **多 language server 并存**（一个 workspace 多语言）。**P1 Full**（新 module + 跨核心 SkillRegistry / Tool 接口 + 外部协议 + 持久化 server 配置 + dashboard CRUD），与 P11 MCP-CLIENT-MVP 工作量类似（~3-5 day）。**MVP 不做**：rename / code action / 自动 didChange 同步（先 didOpen 一次性即可）/ semantic tokens / inline diagnostics 推 LLM。**实施前澄清**：(a) 启动哪些 language server 默认（dogfood：jdtls 最契合 SkillForge 自身 Java 代码？或先 typescript-language-server 调 dashboard？）/ (b) 是否复用 MCP server CRUD UI 共用一张表 vs 独立 t_lsp_server 表 / (c) workspace 路径配置（单一 SkillForge 项目 vs 用户 attached 任意路径） |
| ~~**REMINDER-TODOWRITE-SOURCE**~~ | **已交付（commit `ecbd775`，2026-05-12）**：新增 `TodoListSource implements ReminderSource`，从 `TodoStore` 读取 session 当前 todos，按 in_progress / pending / completed 分组注入 `<system-reminder>`；`SkillForgeConfig` 注册顺序为 ContextUsage → TodoList → MemoryAge → FileActivity；`application.yml` 默认 `todo-list.enabled=true`、`interval-turns=1`、`max-todos=20`；`TodoListSourceTest` 覆盖分组 emit / 安全 skip / 截断 / debounce。 | — | — |
| **SKILL-EXTRACT-MODEL-PICKER** | 现状：`SkillDraftService.extractFromRecentSessions` 通过类常量 `EXTRACT_PROVIDER_NAME="xiaomi-mimo"` + `EXTRACT_MODEL="mimo-v2.5-pro"` 硬编码 provider/model（2026-05-13 替换 bailian 401 token 故障时引入）。换模型必须改代码 + 重启 server，无 runtime 配置面；多 provider 对比同一批 session 的抽取效果也只能改一次跑一次 | 用户想试不同 provider/model（claude / deepseek / glm-5）对同一批 session 的抽取效果 / 不想每次重启 server / 接 channel 后多用户场景需要每用户或团队配 | 主旨：① BE `POST /skill-drafts/extract` 接受可选 query `?providerName=&model=`，覆盖类常量；DTO 校验 provider 必须在 `LlmProperties.providers` 已注册名单内、model 在该 provider `models[]` 内（防注入未注册名）；② FE Skills/Drafts 页加 model picker（数据源复用 `GET /api/providers` 或类似 endpoint），点 Extract 时透传选择；③ localStorage 缓存上次选择避免每次重选。**Mid 档** ~80-150 行（30 BE controller/DTO + 50-80 FE picker + 测试），需求包待真做时建。**不做**：每用户偏好持久化 / provider 健康度自动检测 / 用 default-provider 之外的 setProvider 全局影响其它功能 |
| **SKILL-EXTRACT-AND-AB-VIA-AGENT** | 现状：两条 skill 编排路径都是**直接 LLM call**，不走 agent loop。**(a) 抽取**：`SkillDraftService.extractFromRecentSessions` line 175 `provider.chat(request)` 拿 10 个 session truncated（4000 char/session）concat 一次性输出 JSON 数组，无中间工具调用、不能 fan-out 看具体 session 详情、不能查相邻 session 上下文、不能跨 session 找 pattern。**(b) A/B Eval**：`SkillAbEvalService.runAbEval` 跑 baseline + candidate 多 scenario 也是机械循环 LLM call，无智能的"先抽 scenario 共性 → 决定哪些指标重测 → 适配性裁剪"。两者准确度都受限于单 prompt 信息量 | LLM 单 prompt 抽得不够准 / 想让 agent 用 tool 查 t_session_message 详情 / 用 SubAgent 并行分析每个 session 后再 consolidate / A/B Eval 想根据 candidate skill 性质动态决定要测哪些 scenario / 接 user feedback 闭环（"这条抽得不对，重新看 session X"）| 主旨（待真做时建独立需求包，需用户决定 scope）：① 内置 2 个 system agent —— `skill-extractor` + `skill-ab-evaluator`（类似 `memory-curator` 模式），各自 prompt 工程描述任务 + 给 SessionRead / SessionListMessages / MemoryRecall / ScenarioRead / RunSkillOnScenario 等 tool；② `SkillDraftService` 改成 `SubAgentDispatchService.dispatch("skill-extractor", ...)`，`SkillAbEvalService.runAbEval` 改成 `SubAgentDispatchService.dispatch("skill-ab-evaluator", ...)`，结果通过专门的 dispatch handler 回收（agent 用 `CreateSkillDraft` / `RecordAbScore` tool 写 DB 而不是 LLM 直接输出 JSON）；③ 复用 P12 ScheduledTask 跑日常 cron；④ FE TaskPanel（2026-05-13 已落地）自然显示 agent 进度；⑤ 失败 fallback 到当前 single-LLM-call 实现（feature flag）。**Full ~500-800 行**（2 个新 system agent prompts + 4-6 新 tool + 2 处 dispatch 改造 + handler + fallback），触碰 SubAgent dispatch 核心路径，与 `memory-curator` 工作量同量级 × 2。**实施前澄清**：(a) 一次抽取派发 1 个 agent 看 N session 还是 N 个 sub-agent 各看 1 个 session 后 consolidate；(b) agent 是否能 cross-reference 别的 agent 的 session（隐私边界）；(c) 用户 feedback 入口（"这条抽错了"按钮触发 agent 重看）是否同期做；(d) A/B Eval agent 能否自主决定要不要跑全 scenario set 还是 sample 子集（节省 token） |
| **DATA-LAYER-ORIGIN-SPAN-LINK** | 现状：会话数据 3 视图分散且无显式 backlink。(1) **原始数据**（tool 50KB FileRead 输出 / LLM 完整请求-响应字节）存在 `t_llm_span.output_summary` + `/data/llm-payloads/<traceId>/<spanId>-{request,response}.json` blob，**immutable 审计**。(2) **Agent 视图**（下次 LLM 看的 history）存在 `t_session_message.content_json`，light compact 会就地 truncate（50KB → 5KB）、full compact 会加 boundary 把老行隐藏（getContextMessages 跳过 pre-boundary）。(3) **User 视图**派生自 agent 视图 + FE `stripSystemReminderBlocks` 过滤，零独立存储。**Gap**：Agent 视图 seq N 截断版的 tool_result 跟 t_llm_span 原始输出之间**没有 explicit `origin_span_id` 列**，要追溯只能 trace_id + 时间戳 + 启发式匹配；FE 看到 `[12345 chars truncated]` marker 时**没有"展开看原文"按钮**调试 50KB+ tool 时痛点。讨论 2026-05-10。来源：用户问"我们 DB 需要记录下原始 / agent / user 三种视角"  | 用户在 chat 页面想点开看 50KB tool_result 原文 / debug 时频繁切到 t_llm_span 表查询 / 大 tool 输出场景增多 | 主旨（最小有效修法）：① `t_session_message` 加 nullable column `origin_span_id varchar(36)`，tool_result 行写入时填该 row 关联的 tool span_id（compact 改 content 时不动这列）；② FE markdown 渲染识别 truncate marker 时渲染成"展开看原文"按钮，调现有 `GET /api/observability/spans/<spanId>/blob?part=output` endpoint。**Light 档**：1 migration + ~30 行 BE（FileTool / SubAgentTool / Bash 等 tool dispatch 写 message 时填 span_id） + ~50 行 FE 按钮+modal。**不推荐**重做成 3 独立表 / ContentBlock 加 visibility flag（ripple 太大、ROI 低）。当前 SkillForge 用户实际不大频繁碰 50KB+ tool 输出的 chat 场景，碰到痛点再触发实施 |

## 阅读规则

实现任务时，先打开链接的需求包。实现前读 `prd.md` 和 `tech-design.md`；只有原始产品意图或限制不清楚时才读 `mrd.md`。
