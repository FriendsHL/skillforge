# SkillForge ToDo

> 更新于：2026-05-08(P13 Prompt Cache MVP 完成)
> 规则：这里只放当前执行状态；需求和方案细节放在链接的需求包中。
> 旧版：重整前长版 ToDo 已保留在 [references/legacy-todo-2026-04-28.md](references/legacy-todo-2026-04-28.md)。

## 当前队列

| 顺序  | ID        | 标题                                | 模式   | 状态        | 优先级 | 风险   | 文档                                                        | 下一步                                   |
| --- | --------- | --------------------------------- | ---- | --------- | --- | ---- | --------------------------------------------------------- | ------------------------------------- |
| 5   | P9-4/P9-5 | Partial compact + post-compact 恢复 | Full | prd-draft | P2  | Full | [需求包](requirements/active/P9-4-P9-5-compaction-recovery/index.md) | 先决定"最近文件"数据来源                         |

## 阻塞 / 待决策

| ID | 待决策 | 负责人 | 阻塞项 |
| --- | --- | --- | --- |
| P9-4/P9-5 | compact 后“最近文件”的数据来源 | youren + agent | P9-5 设计 |

## 最近完成

| ID | 完成日期 | Commit | 交付 |
| --- | --- | --- | --- |
| **P13 Prompt Cache MVP（5 provider 全链路：稳定 prefix + Claude 3 breakpoint + Usage normalize + dashboard hit rate）** | 2026-05-08 | `<P13 commit>` | 5 个 LLM provider 启用 prompt cache 全链路。**核心改动**：(1) `skillforge-core/.../llm/cache/` 新建 6 文件协议层 — SystemPromptParts (record stable+dynamic) / CacheBoundary marker `<!-- SKILLFORGE_CACHE_BOUNDARY -->` / CacheControlMarker / ToolNormalizer (trim + sort by name + ORDER_MAP_ENTRIES_BY_KEYS + per-tool SHA256 hash + cached canonical mapper r2) / UsageNormalizer (按 ProviderProtocolFamily 分支 5 provider，r3 OpenAI-family `input = Math.max(0, input - cacheRead)` 让 inputTokens 跨 provider 统一"非 cached"语义)；(2) ToolHashTracker / CacheBreakDetector @Component (5% 容差 + 2K 阈值算法 + per-session prevCacheRead state)；(3) SystemPromptBuilder.buildWithBoundary(claudeMd) 拆 stable / dynamic 两段，stable 含 agent.systemPrompt + soulPrompt + toolsPrompt + behaviorRules（不含 Instant.now / userId / sessionId）—— SHA256 跨调用严格相等 (INV-1)；(4) ClaudeProvider.buildRequestBody 改 system structured array + 3 breakpoint cache_control（system stable 末 + tools 末 + 最后 user message 末 block，参考 claude code 模式，用 4 上限的 3）；(5) OpenAiProvider 不动 request body (INV-5)，parseResponse 用 UsageNormalizer 统一解析；(6) LlmResponse.Usage 加 cacheReadInputTokens + cacheCreationInputTokens 字段（保留 2-arg constructor backward compat），所有 provider parseResponse 走 UsageNormalizer；(7) V62 migration `t_llm_span.cache_creation_tokens INTEGER` nullable + LlmSpan domain / Entity / DTO / PgLlmTraceStore attributes_json round-trip / TraceLlmCallObserver doWrite 写 cache 字段 + cache_break attr + 双层 try/catch 隔离 (INV-10)；(8) AgentLoopEngine 用 buildWithBoundary 拼接 stable + MARKER + dynamic（r2 W3 fix：promptSuffix 非空但 systemPrompt 没 MARKER 时强制插入 MARKER 防 dynamic 漏进 stable cache）；(9) FE LlmSpanDetail 加 cacheCreationTokens + metadata 字段 + 新建 CacheStatsRow (cache 数字 + hit rate 徽章颜色阈值 + break event 红徽章 + Tooltip + 千分位 + 1 位小数) + LlmSpanDetailView 接入。**13 INV ratify** 详见 [tech-design.md](requirements/archive/2026-05-08-PROMPT-CACHE-MVP/tech-design.md) 含跨 provider inputTokens 语义统一定义。**Full pipeline r1+r2+r2.5+r3 对抗循环** PASS：r1 BE PASS / 0 blocker / 4 warning + FE NEEDS_FIX_R2 / 2 blocker（B1 hit rate denominator 算 16000% / B2 wire shape mismatch silent failure 红徽章不渲染 - P11 教训重演）+ 2 warning → r2 fix BE B2 dto metadata Map + W1/W2/W3/W4，FE B1 prop 重命名内部聚合 + W1 label `prompt` + W2 6 wire-level integration test + r1 B1 broken example regression → **FE r2 push-back 抓 cross-provider 语义 architectural flaw**（FE 读 UsageNormalizer.java:53 发现 OpenAI-family `prompt_tokens` 是 TOTAL 含 cached，DeepSeek 真实场景 hit rate 偏低）→ r3 BE mini-fix UsageNormalizer 4 个 OpenAI-family case 加 `input - cacheRead` + dirtyWireDataClampedToZero edge case（cached > total → 0 不负）。**Phase Final 修 Maven 多模块 jar 一致性 bug**：spring-boot:run 加载 ~/.m2 旧 SNAPSHOT jar 触发 NoSuchMethodError，需 `mvn install -pl core/tools/observability` 安装新 jar 后再启 backend。验证：`mvn test` Tests run: 1060, Failures: 0 (baseline 1057 + r1 38 新增 r2/r3 4 新增) → BUILD SUCCESS；`tsc --noEmit` + `npm run build` EXIT=0；FE 25 cache tests + BE 38 cache tests 全绿；端到端 Phase Final：用户用 OpenAI-family provider (DeepSeek/Qwen/mimo) e2e 验证，dashboard cache stats 行正常显示（无 ANTHROPIC_API_KEY 暂不验 Claude 手动 cache_control 真命中，留下次有 key 时验证）。**MVP 不做** (V2)：Qwen 手动 cache_control / Anthropic 1h TTL / Messages sliding window / cost 美刀估算 / xiaomi-mimo `cache_write_tokens` / cache hit rate 告警 / LRU eviction CacheBreakDetector / ToolHashTracker 持久化。Migration: V62。 |
| **P11 MCP Client MVP（stdio + per-agent + dashboard CRUD + dogfood time server）** | 2026-05-07 | `c4f7102` | 引入 Model Context Protocol 客户端能力。**核心改动**：(1) `skillforge-tools/.../mcp/` 协议层（McpStdioTransport NDJSON 帧 / McpServerSession initialize 状态机+tools cache+lazy 重连 / McpToolAdapter inputSchema 透传 / McpServerSessionRegistry / JSON-RPC records）+ skillforge-server/.../mcp/ 服务端（Entity / Repo / Service / Lifecycle @TransactionalEventListener(AFTER_COMMIT, REQUIRES_NEW) reload / McpToolRegistrar 命名前缀 + 冲突拒绝 / 6 REST endpoints / DTO + events + exceptions）+ `LoopContext.allowedMcpServerNames` 双层防护（INV-4 collectTools 暴露过滤 + dispatch NOT_ALLOWED 短路）；(2) V61 schema (`t_mcp_server` + `t_agent.mcp_server_ids VARCHAR(512)` comma-list + idx + 默认 seed time uvx) + ChatService 默认空集（agent 必须显式 enable）；(3) FE `/mcp-servers` 页面 + AgentDrawer multi-select + Layout MCP 入口 + 编辑 drawer Form.List args/env + name regex `[a-z0-9_]+` ≤32 + edit name 禁用 + 测试连接 + 删除 409 双 shape；(4) env var `${VAR}` 严格 regex + Matcher.quoteReplacement 防注入 + ProcessBuilder array form 防 shell injection；(5) DELETE 引用校验 split+equals 防 substring 误匹配（INV-12）。**13 INV ratify**（详见 [tech-design.md](requirements/archive/2026-05-07-MCP-CLIENT-MVP/tech-design.md)）含新增 INV-13: env mask sentinel `"***"` FE/BE 两端同步严格定义。**Full pipeline r1+r2+r2.5+r3 对抗循环** PASS：r1 BE 0 blocker / 4 warning（W1 W4 accept；W2 stderr WARN leak / W3 env plaintext API 暴露）+ FE 1 blocker（实测证伪 main pre-existing）+ 2 warning（W1 edit 仍发 name 隐患 / W2 listMcpServers 静默 fallback）→ r2 fix BE W2/W3 + FE B/W1/W2/W3-FE → r2.5 BE 加 update name 兼容 + env preserve-on-***（FE concern 联动）→ r2 reviewer 抓 architectural flaw（FE *** filter 让 BE preserve never fires）→ r3 FE 撤销 filter 让两边防御 compound。**Phase Final 修 2 个 unit test 漏抓的 startup bug**（reviewer 都没抓到）：SkillForgeApplication EntityScan/EnableJpaRepositories 漏 mcp package + McpServerLifecycle.onUpserted @TransactionalEventListener+@Transactional 在 Spring 6.1+ 必须 propagation=REQUIRES_NEW。**MVP 不做**（V2）：MCP Server 反向 / HTTP-SSE transport / OAuth flow / 用户级 server / Firecrawl/Postgres/Notion/Calendar 等具体 server（dashboard CRUD 自助加）/ 监控告警 / resources/prompts / dedicated mcpExecutor。**默认 dogfooding**: time MCP server (Anthropic 官方 uvx mcp-server-time) 提供 `mcp_time_get_current_time` + `mcp_time_convert_time` 2 tools。**协议测试**走 Mockito 单测（不写 Java IT fixture，用户 ratify）+ Phase Final 真 time server e2e 兜底。验证：`mvn test` Tests run: 1057, Failures: 0 (baseline 1009 + 48 新增) → BUILD SUCCESS；`tsc --noEmit` + `npm run build` EXIT=0；FE 26 + BE 48 tests 全绿；端到端 Phase Final：uvx mcp-server-time 进程 pid=50548 connected → 2 tools 注入 → dashboard `/mcp-servers` status=connected → test-connection 返完整 inputSchema。Migration: V61。 |
| **P10 聊天斜杠命令 MVP（8 命令 + dashboard popup + channel BE 拦截）** | 2026-05-07 | `e069057` | 8 命令（/new /compact /model /models /skill /tool /context /help）+ V60 schema (`t_session.runtime_model_override` 字段)+ ChatService 两处 model 解析改造（runLoop / completeConfirmedTool）+ `SlashCommandService` registry 模式 + 8 个 `SlashCommandHandler` + `CommandResult` DTO 三 displayMode (redirect/toast/modal) + `SlashCommandController` REST `POST /api/commands/execute` (record `{sessionId, command, args, userId}` + @Valid + IT 真过 Jackson + LocalValidatorFactoryBean) + `ModelCatalog` 用 LlmProperties YAML（INV-8 校验 modelId 存在性）+ `ChannelSessionRouter` 拦截 `/` 前缀早于 chatAsync + `ChannelConversationRebindService` (r2 抽 @Transactional 防 close+insert 非原子)+ `chatLoopExecutor.submit` 异步触发 /compact (B2 fire-and-forget 0.008s) + FE `CommandPopup` (8 命令 fuzzy match + 上下箭头 + Enter/Tab/Esc)+ `CommandResultModal` (markdown 渲染) + `ChatInput` 改造 (`COMMAND_NAME_REGEX = /^\/[A-Za-z]*$/` 仅首字符 `/` 触发) + `commands.ts` API + Chat.tsx `slashCommandConfig` useMemo + `refreshCompactStats` useCallback (r2 修 W1 破坏 React.memo)。**9 决策 ratify**：见 [tech-design.md §"Ratified Decisions"](requirements/archive/2026-05-07-P10-slash-commands/tech-design.md)。**Full pipeline r1+r2 对抗循环 PASS**：r1 BE 2 blocker (B1 silent failure FE/BE contract mismatch + B2 /compact sync 阻塞) + 2 warning + FE 0 blocker + 2 warning → team-lead 仲裁 must-fix-r2: BE B1/B2/W1/W2 + FE W1/W2；defer: BE C1 t_model_provider 不存在 (用 LlmProperties) / BE C2 SYSTEM_TOOL_NAMES cosmetic / BE C5 broadcaster mock noise / FE C1 model header chip (V2) → r2 reviewer 复审 PASS。验证：`mvn -pl skillforge-server test` 1009 tests 0 failures (baseline 991 + 18 新增) + `cd skillforge-dashboard && npm run build` EXIT=0 + 7 命令 curl e2e 全过 (/help /models /context /model /compact 0.008s 异步 /unknown 等) + INV-15 验证 (/model qwen-32b 路由到 ModelCommandHandler 不被 ModelsCommandHandler 拦截)。**MVP 不做** (V2)：自定义命令 / `/agent` `/session` `/cancel` `/branch` `/cost` 边缘命令 / chat header model chip / `Tool.isSystem()` 接口 / 专用 compactExecutor / 命令参数 fuzzy 补全。Migration: V60。 |
| **P12 定时任务 MVP（user-type cron + one-shot + 5 Tool + dashboard）** | 2026-05-07 | `72b8980` | V59 schema (`t_scheduled_task` + `t_scheduled_task_run`) + `ScheduledTaskService` (CRUD + 互斥校验 + 互转) + `UserTaskScheduler` (ThreadPoolTaskScheduler + per-task ReentrantLock + skip-if-running ConcurrentHashMap.newKeySet) + `ScheduledTaskExecutor` (session new/reuse + onSessionLoopFinished hook 监听 → channel push) + `SchedulerChannelDispatcher` (ReplyDeliveryService.deliver + catch Throwable + INV-9 严格化) + 5 Agent Tool (Create/Update/Delete/List/GetScheduledTask 均 silent + creator_user_id 隔离) + REST 7 endpoints (`/api/schedules` CRUD + manual trigger + run history) + `ChatService.SessionLoopFinishedEvent` 在 finally 内发布（核心文件改动）+ FE `/schedules` 页面（列表 + 编辑 drawer + cron↔one-shot radio 互转 + per-row triggeringId state + run history drawer + Tag 颜色状态展示）+ `app.dashboard-url` 配置化（waiting_user 推绝对 URL）。**11 决策 ratify** 见 [archive/2026-05-07-P12-scheduled-tasks/tech-design.md §"Ratified Decisions"](requirements/archive/2026-05-07-P12-scheduled-tasks/tech-design.md)。**Full pipeline r1 + r2 对抗循环 PASS**：r1 BE 6 warnings / FE 2 blocker + 2 warning → team-lead Judge 仲裁 must-fix-r2: BE W1/W3/W4/W5 + FE B-1/W-1/W-2；defer: B-2 cron next-5-fires (FE 简化) / W2 listRuns 分页 / W6 sync listener → r2 reviewer 复审 PASS。验证：`mvn -pl skillforge-server test` 934 tests 0 failures (含 49 IT skipped)、`cd skillforge-dashboard && npm run build` EXIT=0。Migration: V59。 |
| **EVAL-V2 M5+ Smart trace/session dataset drafts** | 2026-05-07 | `a06bd3d` | 管理端 trace 列表新增 Smart Import：`GET /api/eval/traces/suggestions` 根据 `agent_error / tool_failure / span_error / high_token / multi_turn / has_tool_calls` 等原因推荐适合沉淀为评测集的 trace/session；用户选择后调用 `POST /api/eval/scenarios/batch-import` 批量生成 `draft` 场景，后续沿用 Scenario Drafts approve 流程入 dataset。前端 `TraceImportSuggester.tsx` 接真实建议接口，`Traces.tsx` 支持单条 Add to dataset 和多选 smart import；同时清理 eval UI 的 TS/build 问题。验证：`mvn -pl skillforge-server -Dtest=TraceScenarioImportServiceTest,EvalControllerTraceImportTest test` 7/7 通过；`cd skillforge-dashboard && npx tsc --noEmit` 通过；`cd skillforge-dashboard && npm run build` 通过。运行侧 follow-up：重建并重启 server，补齐 launchctl LLM provider env；本地 DB 热修 Code Agent `owner_id NULL -> 1`。 |
| **EVAL-V2 M6 apply suggestion → PromptImprover** | 2026-05-06 | `99ad06f` | 新增 `POST /api/eval/tasks/{id}/apply-improvement`，从 task 读取 `improvementSuggestion` 并触发 `PromptImproverService.startImprovement(...)`；`PromptImproverService` 新增 suggestion-aware 入口，把分析建议写入 `PromptVersion.improvementRationale`，并在 candidate prompt 生成时把该建议注入 LLM prompt，避免“Apply suggestion”只是空转已有 auto-improve 流程；前端 `Eval.tsx` 的 task drawer 在 `Improvement suggestion` 区块新增 `Apply suggestion` 按钮，调用后返回 `abRunId` 并提示 improvement 已启动。验证：`mvn -pl skillforge-server -Dtest=EvalTaskControllerTest,PromptImproverServiceTest,EvalScoreFormulaTest test` 通过；`cd skillforge-dashboard && npm run build` 通过。 |
| **EVAL-V2 M4 score 多维度** | 2026-05-06 | `99ad06f` | 新增 `V58__add_eval_task_item_dimension_scores.sql`，给 `t_eval_task_item` 增加 `quality_score / efficiency_score / latency_score / cost_score / cost_usd / score_formula_version / score_breakdown_json`；后端新增 `EvalScoreFormula`，按 `0.55 quality + 0.20 efficiency + 0.15 latency + 0.10 cost` 聚合总分，使用全局 cost threshold，并在 `quality < 30` 时将总分封顶到 39；`EvalOrchestrator` 从 `rootTraceId` 聚合整条 trace tree 的 `total_cost_usd`，写回 item 与 scenarioResults；`EvalTaskController` / compare 接口补齐多维字段；前端 `Eval.tsx` 增加 metric selector，task cases 与 compare modal 支持按 `composite / quality / efficiency / latency / cost` 切换查看，并按当前维度实时计算 delta。验证：`mvn -pl skillforge-server -Dtest=EvalTaskControllerTest,EvalScoreFormulaTest test` 通过；`cd skillforge-dashboard && npm run build` 通过。 |
| **EVAL-V2 M5 trace 数据回流 dataset** | 2026-05-06 | `2d10edf` | 新增 `POST /api/eval/scenarios/from-trace`，从 `rootTraceId` 反查 root trace 的首条 user message 与末条 assistant message，自动生成 `EvalScenarioEntity`（默认 `category=trace_import`、`split=held_out`、`oracleType=llm_judge`，agentId/sourceSessionId 从 trace 继承）；前端 `Traces.tsx` 详情头部新增 `Add to dataset` 按钮，直接透传 `rootTraceId`，成功后刷新 eval dataset 查询。验证：server 定向测试新增 `EvalControllerTraceImportTest`、`TraceScenarioImportServiceTest` 并与 scenario versioning 测试一起通过；dashboard `npm run build` 通过。下一步：M4 score 多维度。 |
| **EVAL-V2 M3c 归因闭环 + M3e case → trace** | 2026-05-06 | `2d10edf` | M3c：新增 `V55__create_eval_analysis_session.sql`、`EvalAnalysisSessionEntity/Repository/Service`、`EvalAnalysisController`、`POST /api/eval/scenarios/{id}/analyze`、`POST /api/eval/tasks/{id}/analyze`、`POST /api/eval/tasks/{id}/items/{itemId}/analyze`、`GET /api/eval/tasks/{id}/analysis-sessions`，前端 `AnalyzeCaseModal` 统一支持 `scenario/task/item` 三种上下文；新增 `AnalyzeEvalTaskTool` 让 analysis chat 可把 `attributionSummary / improvementSuggestion` 回写到 `t_eval_task`。M3e：task item 卡片新增 `View trace`，跳到 `/traces?traceId=...`；`Traces.tsx` 加 query-param 深链接选中逻辑。验证：server 定向测试 32/32 通过；dashboard `npx tsc --noEmit` + `npx vite build` 通过。下一步：M3d compare runs。 |
| **EVAL-V2 M3d compare runs + M3f annotation queue** | 2026-05-06 | `2d10edf` | M3d：新增 `POST /api/eval/tasks/compare?ids=a,b`，返回共享 `scenarioId` 交集下两侧 task 的 `status / compositeScore / attribution / latency / loops / tools / rootTraceId / agentFinalOutput`，并计算 `scoreDelta / outputDiffers`；前端 `Eval.tsx` 新增 2-task compare 选择和 compare modal。M3f：新增 `V56__create_eval_annotation.sql`、`EvalAnnotationEntity/Repository/Controller`、`POST /api/eval/annotations`、`GET /api/eval/annotations`、`PATCH /api/eval/annotations/{id}`；前端 `Eval.tsx` 新增 Annotation Queue，支持从 task item 发起人工标注、录入 `correctedScore/correctedExpected`、查看 pending/applied、标记 applied，并继续跳 `View trace`。当前边界：annotation queue 只负责人工修分/修 expected 的暂存与状态流转，真正 scenario versioning / re-judge 留给 M3g。验证：server 定向测试 37/37 通过；dashboard `npx tsc --noEmit` + `npx vite build` 通过。下一步：M3g dataset versioning。 |
| **EVAL-V2 M3g dataset versioning（M3 收尾）** | 2026-05-06 | `2d10edf` | 新增 `V57__add_eval_scenario_versioning.sql`，`t_eval_scenario` 加 `version` + `parent_scenario_id`；后端新增 `EvalScenarioVersionService`、`GET /api/eval/scenarios/{id}/versions`、`POST /api/eval/scenarios/{id}/version`，并把 Agent dataset 列表收口为“每个 scenario family 只展示最新版本”。前端 `ScenarioDetailDrawer` 新增 version selector / history，`DatasetBrowser` agent 卡片展示版本号；同时把 `AnnotationQueue` 里的 `correctedExpected` 接到“Create version”动作，DB 场景可直接从人工标注创建新版本并自动标记 annotation applied。验证：server 定向测试 43/43 通过；dashboard `npx tsc --noEmit` + `npx vite build` 通过。下一步：M4 或 M5。 |
| **EVAL-V2 M3a-b2 task/item 双写收尾 + r2 review fix** | 2026-05-06 | `2d10edf` | M3a 第二段收尾：落地 `EvalTaskEntity` / `EvalTaskItemEntity`、`EvalTaskController`、`RunEvalTaskTool`、V52/V53/V54 三个 migration，以及 reviewer r2 fix 5 项：`ScenarioRunnerTool` 单/多轮统一在 `finally` 做 `rewriteTraceOriginToEval`，V52 rename 加 idempotent guard，V54 task-level idempotent guard 防重复迁移，V53 `idx_task_item_session` 改 partial index，`EvalOrchestrator` dual-write warn log 补 throwable。新增 `ScenarioRunnerToolTest` 回归锁 early-return origin rewrite；`EvalTaskMigrationIT` 保留但改为 `-Dskillforge.runMigrationIT=true` 显式 opt-in，避免本地日常回归受 Docker/Testcontainers 干扰。下一步：M3c 归因闭环；M3a-b3 FE 切换并入后续任务面板演进。 |
| **EVAL-V2 M3a-b1 origin 字段** | 2026-05-05 | `cd69350` | M3a 三段拆分第一段：V50 (server: t_session + t_llm_trace 加 origin VARCHAR(16) NOT NULL DEFAULT 'production' + partial index) + V51 (observability: 同步加 origin to t_llm_trace) + ORIGIN_PRODUCTION/ORIGIN_EVAL 常量 + 5 处过滤 (SessionService / TracesController / DashboardService LEFT JOIN preserves channel-only / CompactionService / Recovery) + 3 处 spawn 复制 + GetTraceTool 6th caller 加 filter（reviewer 抓的 BLOCKER）。Full pipeline 2 reviewer + r2 fix 4 件。**已知 trade-off**：b1 部署后 EvalOrchestrator 仍未改造（b2 scope），eval 流量当前仍 origin='production' 直到 b2 落地。781/766 server +1 + 25/25 observability + V50/V51 实跑 |
| **EVAL-V2 M3b Dataset UI 完整化（Gap A）** | 2026-05-05 | `b349f6f` | EvalController.toBaseScenarioMap 补 surface 5 字段（toolsHint / tags / setupFiles / maxLoops / performanceThresholdMs）+ FE EvalDatasetScenario / BaseScenario 类型扩展 + ScenarioDetailDrawer 加 5 段（Description / Tools / Tags / Setup files / Technical info，全 null/empty guard）+ DatasetBrowser 卡片加 description preview（line-clamp 2 + max-height 3em fallback）+ 前 3 tag chip + "+N" overflow chip（title hover 显隐藏 tag 名）。Mid 简化：1 dev → 1 ts-reviewer (Sonnet) PASS (0 blocker / 4 warning) → r2 fix (W1 +N title hover / W2 max-height 2.8em→3em fallback)。766/766 mvn 无 regression / tsc 0 / vite build 成功。无 schema migration |
| **EVAL-V2 M2 多轮 case** | 2026-05-05 | `5608cd0` | EvalScenarioEntity 加 conversation_turns（V49）+ EvalOrchestrator multi-turn 分支 + EvalJudgeTool.judgeMultiTurnConversation（per-turn 30% + overall 70% weight 配置化）+ ScenarioRunnerTool.runScenarioMultiTurn（单 sandbox 累积 history，90s budget）+ SessionScenarioExtractor 多轮抽取（≥2 user→multi-turn）+ AbEvalPipeline / SkillAbEvalService R5 fallback warning + FE ScenarioDetailDrawer 多轮气泡渲染 + AddBaseScenarioModal multi-turn turns editor（advanced disclosure，stable id key 防 stale state）。Mid pipeline：1 dev → 2 reviewer (java + ts Sonnet) 并行 → r2 fix（FE 2 blocker：silent task 验证死锁 + key={i} stale 漏洞）。766/766 mvn + V49 migration 实跑 |
| **EVAL-V2 PRD v2 重写** | 2026-05-05 | `74c096b` | 完整闭环 7 milestones (M2 / M3a-g / M4-6) + D1-D14 决策（D10 origin 字段 + D9 t_eval_analysis_session 独立表 + D6 rename t_eval_run + D7 复用 OBS trace）+ Gap A/B/C/D 整合 |
| **EVAL-V2 Q1+Q2+Q3** | 2026-05-05 | `3554569` | EVAL-V2 M0+M1 follow-up 3 件：Q1 SessionEntity 加 `source_scenario_id VARCHAR(64)` (V48) + AnalyzeCaseModal 传 + ScenarioDetailDrawer 反查"Analysis sessions"段；Q2+Q3 ScenarioLoader 双路径（classpath + home dir）+ POST `/eval/scenarios/base` 写 home dir JSON + DatasetBrowser `[Base][Agent]` tab + `+ 新增` modal + 中文 chip 系统内置/用户新增 + AddEvalScenarioTool（agent 通过 tool 加 scenario）。**已知 trade-off**：scenario 持久化双轨（per-agent → DB / global → JSON），M3 dataset versioning 时统一。Mid pipeline：1 dev → 2 reviewer (java + ts Sonnet) 并行对抗 1 轮 → r2 fix (FE 2 blocker AddBaseScenarioModal + 中文 chip / BE W1 length 36→64) → 749/749 mvn test |
| **EVAL-V2 M0+M1 MVP** | 2026-05-04 | `47b331c` | Datasets browser tab + Eval 详情卡片化（big score + attribution badge 仅 fail 显示 + 折叠 task/judgeRationale/agentFinalOutput）+ ScenarioDetailDrawer + AnalyzeCaseModal（"分析这个 case" D4 stretch）+ EvalOrchestrator WS push case_running/case_passed/case_failed/eval_run_completed + throttle (>50 case 按 5%) + FE progress bar |
| **SUBAGENT-CONTINUATION** | 2026-05-04 | `ee3dd53` | SubAgentTool 加 `continue runId task`（reuse idle child session 继续聊）+ `terminate runId`（强制结束 child + 释放并发额度）两个 action；preserveActiveRoot=true 让 continue 内部 trace 继承同 root。`onSessionLoopFinished` + `ChatService.runLoop` teardown 双 TERMINATED guard 让"父显式终止"成 sticky 终态。Mid pipeline 走完：r1 dev → DONE_WITH_CONCERNS → r2 fix (terminate race + userId) → java-reviewer 对抗 1 轮 (NEEDS_FIX 2 warning) → r3 fix (ChatService guard + markRunResumed defensive) → 主会话目检 PASS。717/717 全量回归绿 |
| **P12-PRE** | 2026-05-04 | `a27cc39` | Sprint 4 前置决策闭环：Cost Dashboard 复用现有 `pages/ModelUsage`；PG 备份 + 多用户/权限模型均"暂不实现，accept current risk"。需求包归档；P12 解除 blocker |
| **TOOL-CC-NAME-ALIAS** | 2026-05-04 | `be5788b` | 内置 file 工具 LLM-facing 名 `FileRead/FileWrite/FileEdit` → `Read/Write/Edit`（3 Tool + 2 Sandboxed* 改 `getName()`；46 文件 string literals + V47 migration backfill `t_agent.skill_ids/tool_ids`）。**实施时改方案**：原 todo 写"加别名重定向"，实测后用户决定走"直接改名"（避免长期维护双名歧义）。Reviewer PASS，`mvn test` 695 pass / 0 fail |
| **OBS-4 全交付（M0-M4）** | 2026-05-03 | `eda937d`(M0) + `e89c593`(M1) + `877495b`(M2) + `a496a7e`(M3) + 本 commit (M4 收尾归档)；root_trace_id 一等公民字段 + 跨 agent / 跨 session trace 串联 + FE 二级折叠瀑布流；session `cf4202a6` 7 traces / 5 sessions / 60 spans 真实场景 INV-1/3/4/5/6 全验证 | [交付索引](delivery-index.md)；需求包归档到 archive |
| **OBS-2 M4** | 2026-05-03 | M2 ETL 实跑（33 trace + 920 tool + 4 event 回填）+ M4 关旧轨写入 `b31cfaf` | [交付索引](delivery-index.md)；M5/M6 待跟进 |
| **OBS-2 Trace 数据模型统一（M0-M3）** | 2026-05-02 | `cf9123c` + `06b2372` + `edd5a21`(M0) + `4412729`(M1) + `26023c5`(M2) + `69ee35b`(M3) | [交付索引](delivery-index.md)；M3.5/M4/M5/M6 待跟进，PRD 留 active |
| **xiaomi-mimo provider** | 2026-05-02 | `08a6d4d` + `afa8cf4` + `f6be1ab` | [交付索引](delivery-index.md) |
| SKILL-IMPORT-BATCH | 2026-05-01 | `478e135` | [交付索引](delivery-index.md) |
| SKILL-IMPORT | 2026-05-01 | `a09a17a` + `4490e14` | [交付索引](delivery-index.md) |
| MSG-1 + BUG-30 + BUG-31 | 2026-04-30 | `543a60e` | [交付索引](delivery-index.md) |
| P9-2 + BUG-32 | 2026-04-30 | `fe0404c` | [交付索引](delivery-index.md) |
| SKILL-LOAD | 2026-04-30 | `8d1fb45`(merged via `d74e01c`) | [交付索引](delivery-index.md) |
| P1-D | 2026-04-30 | `6b19439` + `223e5a8`(V33→V38 fix) | [交付索引](delivery-index.md) |
| CTX-1 | 2026-04-30 | `1de1fe1` | [交付索引](delivery-index.md) |
| OBS-1 | 2026-04-29 | 见交付索引 | [交付索引](delivery-index.md) |
| P1-C | 2026-04-28 | `555bdba` | [交付索引](delivery-index.md) |
| Memory v2 | 2026-04-27 | `9f36b59`, `8330d32`, `86703ed`, `96676b9` | [交付索引](delivery-index.md) |
| BUG-F | 2026-04-26 | `e9b48f3` | [交付索引](delivery-index.md) |
| SEC-2 | 2026-04-25 | 见交付索引 | [交付索引](delivery-index.md) |
| thinking-mode-v1 | 2026-04-25 | `55969db` | [交付索引](delivery-index.md) |

## 暂缓

| ID              | 原因                                                      | 重评触发条件                                                | 文档                                                                      |
| --------------- | ------------------------------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------- |
| BUG-G           | 根因已修，剩余 sanitizer / 尾部不变量属于防御性补强                        | 再次出现 dangling assistant `tool_use` / 缺失 `tool_result` | [需求包](requirements/deferred/BUG-G-defensive-hardening/index.md)         |
| SEC-1           | 当前本地 / 单用户使用下重要但不紧急                                     | 多端部署、共享环境或 P12 正式上线                                   | [需求包](requirements/backlog/SEC-1-channel-config-encryption/index.md)    |
| P14             | 暂无真实多轮 benchmark 需求                                     | 出现 tau-bench 类真实业务评测需求                                | deferred package TBD                                                    |
| P3-2/P3-4       | 依赖 P14 eval 基础设施                                        | P14 基础设施就绪                                            | deferred package TBD                                                    |
| P15-3/P15-6     | Analyzer MVP 暂不需要 eval run 读取或分析 tab 落库                 | Analyzer MVP 证明有持续价值                                  | deferred package TBD                                                    |
| P12-3/P12-6     | 超出 P12 MVP 范围                                           | 需要 system job UI 或高级可靠性                               | [P12](requirements/active/P12-scheduled-tasks/index.md)                 |
| P10-4/P10-5     | 四条命令 MVP 已覆盖主要场景                                        | 出现明确自定义命令或 help menu 需求                               | [P10](requirements/active/P10-slash-commands/index.md)                  |
| OBS-1-4/OBS-1-5 | OBS-1 MVP 上线后再看 compact 验证视角 + provider quirk 自动诊断的真实使用 | 看到真实 raw payload 后用户提出明确需求                            | [OBS-1 PRD](requirements/archive/2026-04-29-OBS-1-session-trace/prd.md) |
| **P10-INLINE-CMD-RESULT** | P10 5 只读命令（/skill /tool /context /help /models）当前用 modal 弹窗显示 result。用户 2026-05-07 反馈：modal 切窗口不自然，希望在 chat 列表里 inline 渲染（不污染 LLM 上下文 = BE 不写 session_message 已实现，前端只需加 per-session local React state） | 用户实际用 5 只读命令时觉得 modal 体验差 | 主旨：FE only 改造（BE 不动）。ChatWindow.tsx 加 `localCommandResults: Map<sessionId, CommandResult[]>` state，命令 result 不存 DB 仅 React state，chat 列表渲染时按时间顺序混入 messages，新消息类型 `command_result` 特殊样式（灰背景 + 🔧 角标 + markdown body 渲染）。删 CommandResultModal.tsx + 引用。切 session / 刷新页面 result 消失（接受）。Mid 档，约 80-150 行 FE 改动 + 测试反断言。类似 Claude Code / Cursor 的 /help 设计 |
| SKILL-UNINSTALL | dashboard delete 按钮已能用但不清 `t_agent.skill_ids`（silent dangling）；agent 没对应卸载 Tool；本期 SKILL-IMPORT 后浮现的对称缺口 | agent 主动需要卸载 skill / 用户报告删 skill 后 skill_ids 残留 stale 名字 | 主旨：UninstallSkill Tool（dry-run + confirm 双调用）+ SkillService.deleteSkill 内部加 unbind 修复（dashboard 同步受益）+ 全局扫所有 t_agent 解绑 + system 禁卸 + 不调 `npx clawhub uninstall`。Mid 档，需求包待真做时建 |
| WAITING-SUBAGENT-UX | SubAgent / TeamCreate 异步派发后，主 agent loop 结束 → `runtime_status=idle`，但 sub-agent 还在 running。UI 没有"等待 N 个 sub-agent"指示，用户误以为卡住会重发消息（session `2ad5da4d` 现场） | 多次用户反馈"主 agent 不动了"补消息后才发现 sub-agent 还在跑 / collab 页面信息不够 | 主旨：session 级 `pending_subagent_count` 字段（从活跃 collab_run 计算，per-turn refresh）+ chat header 显示"等待 N 个 sub-agent" badge + hover 展开看 handle / status / duration。语义类似 MSG-1 waiting_user 但对应 waiting_subagent。Mid 档，需求包待真做时建 |
| **SUBAGENT-APPROVAL-FORWARD** | `ChatService.java:557-562` `s.setRuntimeStatus("waiting_user")` + `broadcaster.sessionStatus(sessionId, ...)` 把审批 card 推送目标固定为当前 session id —— subagent 触发 install / dangerous tool confirmation 时 card 只推到 subagent sessionId，主 agent chat 看不到，用户必须跳到 subagent chat 才能审批，常被误以为 subagent 卡死（2026-05-04 用户现场报告） | 用户多次反馈"subagent 卡住没反应"（实际是等审批）/ 接 channel gateway 后陌生用户场景增加 / [GAP-PRETOOL-HOOK-PERMISSION](#) 推进时一并设计 | 主旨：ChatService 触发 confirmation 处加分支 `if (parentSessionId != null)` 同时往主 agent session 推 ws + 持久化 ask_user message 到主 agent session（MSG-1 payload jsonb 加 `sourceSessionId=child session id`，**不改 schema**）+ 前端 chat ask_user card 渲染检测 sourceSessionId 加 "From subagent X" 头部标签 + Approve 按钮调 sourceSessionId 的 confirm endpoint。决策：Q1(a) 仅推直接父 / Q2(b) subagent chat 也保留 card / Q3(a) 主 agent 状态不变 / Q5(a) 多 card 时间序堆叠。**Mid 档**（触碰 ChatService 核心文件但仅延伸推送路径不改不变量），~80-130 行（30 BE + 40 FE + 测试），需求包待真做时建 |
| **GAP-PROMPT-CACHE** | `ClaudeProvider.buildRequestBody` (line 384-419) `root.put("system", request.getSystemPrompt())` 把 system prompt 当**裸字符串**赋值，不是 `[{type:"text", text:..., cache_control:{type:"ephemeral"}}]` 结构化数组；OBS-1 的 `cacheReadTokens` / `cacheCreationTokens` 字段是 stub（`LlmSpanDetailDto.java:25` 显示用但**无 provider 写入路径**）。每次 LLM 调用 input 全量计费，5-90% input token 浪费 | 当前主用 provider 不支持 prompt caching（2026-05-04 用户决策暂缓）；等多数在用 provider 都支持 prompt caching 后再统一开启 | 主旨：改 `ClaudeProvider.buildRequestBody` 把 system + tools 改成 structured array 标 `cache_control: ephemeral` + `LlmResponse.Usage` 解析 `cache_creation_input_tokens` / `cache_read_input_tokens` 写到 LLM_CALL trace + OBS-1 dashboard 加 cache hit rate 列。**P0 Mid**（来源：Anthropic Prompt Caching API + Claude Code 02 工具系统 §6 line 110），见 `/tmp/skillforge-gap-analysis-r2.md` Gap 1（含 grep evidence 3 路 + ClaudeProvider:384 文件读 + delivery-index 检索 0 命中）|
| **GAP-PRETOOL-HOOK-PERMISSION** | `HookEvent.java:14-18` 枚举只 5 个事件（SESSION_START / USER_PROMPT_SUBMIT / **POST_TOOL_USE** / STOP / SESSION_END），**无 PRE_TOOL_USE**；`Tool.java:13-41` 接口仅有 `isReadOnly()`，**无 `checkPermissions()`**；`AgentLoopEngine.java:1053` 的 `isInstallRequiringConfirmation` 是**硬编码** `npx clawhub install / npm install` 几个特定命令不可配置；无 alwaysAllow / alwaysDeny / alwaysAsk 三级规则；危险命令（Bash `rm -rf` / FileEdit `/etc/**`）目前直接执行无 backstop。**可吸收 BUG-27** | 接 channel gateway 后陌生用户场景 / BUG-27 类反馈累积 / 真实 prod agent 误执行破坏命令 | 主旨：`HookEvent` 加 `PRE_TOOL_USE` 事件 + `Tool` 接口加 `checkPermissions(input, ctx) → allow/ask/deny` 默认 ALLOW 向后兼容 + 三级规则来源（session/agent/global）含 pattern 匹配 `Bash(git *)` / `FileEdit(/etc/**)` + DENY 时 stderr 拼给 LLM 重 plan + ASK 走 MSG-1 inline confirmation card 复用。**P1 Full**（核心 AgentLoopEngine + Tool 接口 + 规则 schema + dispatcher，触碰核心文件清单）（来源：Claude Code 02 §7 三层权限 + 07 Hooks PreToolUse + Hermes 02 approval callback），见 `/tmp/skillforge-gap-analysis-r2.md` Gap 2 |
| **GAP-MCP** | grep "MCP / mcp / model.context" 在 codebase / `application.yml` / `CLAUDE.md` / `delivery-index.md` / 暂缓表全 0 命中；`skillforge-tools/` 11 个 Tool 全 Java 内置（FileRead/FileEdit/FileWrite/Bash/Browser/CodeReview/CodeSandbox/Glob/Grep/WebFetch/WebSearch）无 MCP 桥接；用户想接 GitHub MCP / Slack MCP / Notion MCP / 内部 service catalog MCP 只能写 Java 编译进 `skillforge-tools` jar 重启服务 | 用户开始拼图把 SkillForge 接到 GitHub / Notion / Linear / 内部 service / 想让 Claude Desktop / Cursor 反向调 SkillForge 当 MCP server | 主旨：① **MCP Client** (更紧迫)：MCP stdio/SSE/HTTP transport 适配层 + `McpProperties` config + `McpToolRegistry` 注册到 SkillRegistry + `mcp_*` tool prefix 命名规则 + OAuth/auth；② **MCP Server**：把 SkillForge 暴露给 VS Code / Claude Desktop。**P1 Full**（新 module + 跨核心 SkillRegistry / Tool 接口 + 外部协议）（来源：Hermes 08 ACP-MCP + Claude Code 02 工具系统 §6 MCP 集成），见 `/tmp/skillforge-gap-analysis-r2.md` Gap 3 |
| **GAP-FILEEDIT-MULTIMODAL** | `FileEditTool.java:64-120` `execute()` 不验"本 session 在该 file_path 上 Read 过且 mtime 未变"，agent 跨 turn edit 陈旧文件不报错（external 修改被 silent 覆盖）；`FileReadTool.java:1-60` 不识别 image / PDF / Jupyter notebook（无 magic bytes 分支）；**`OpenAiProvider.java:537` 注释承认 "image / tool_use blocks ... silently dropped"**，即便 `mimo-v2-omni` / Claude Vision 等多模态模型也用不上 | 用户报 stale edit 覆盖外部修改难调 / 想上传截图 / PDF 给 agent 看图 / 用了 omni 模型但 image 不通 | **拆 2 个 sub-gap**：(4a) **Read-before-Edit precondition** (P2 Mid)：`SkillContext` 加 `readFileState` LRU + FileEdit 验 "this session 读过该 path + mtime 未变"；(4b) **Multimodal Read** (**P1 Mid**)：FileReadTool 加 image / PDF / notebook 分支 (扩展名 + magic bytes 判断 + image resize 到 token 预算 + PDF 按页 extract) + 修 `OpenAiProvider:537` 让 image blocks 真送给 vision-capable provider（含 mimo-v2-omni / Claude / GPT-4o）（来源：Claude Code 02 工具系统 §FileReadTool readFileState + 多模态分支），见 `/tmp/skillforge-gap-analysis-r2.md` Gap 4 |
| **OBS-2-ADMIN-RUN-ETL** | OBS-2 M2 / OBS-1 R__migrate_legacy_llm_call 等 ETL 当前都通过 `application.yml etl.<x>.mode: off \| flyway` placeholder + Flyway hash 切换 → **必须重启 server 才能跑**。重启是重操作（影响 chat 在线用户 + WS 连接断开 + 状态机要恢复）。运维流程"切 mode → 重启 → 切回 → 再重启"= 4 次重启 | 实际跑 OBS-2 M2 / 后续任何 ETL 类回填时（频率 = 数月一次但每次都重）/ 其他 schema 演进引入更多 Repeatable migration | 主旨：admin endpoint `POST /api/admin/observability/run-etl` body `{etl: 'legacy_llm_call' \| 'legacy_trace_span', dryRun: boolean}` → 内部直接 execute 对应 R__ SQL（绕过 Flyway hash + placeholder 切换） + ownership/role guard（只允许 admin user）+ dryRun 走 transaction-rollback 看影响行数 + 真跑返回 INSERT/UPDATE 行数。后续也可加其他维护性 SQL（dual-write 一致性核对 / orphan 清理 / blob TTL 等）。**P2 Mid**（不触碰核心文件，纯新 controller + service + auth），需求包待真做时建 |
| **EVAL-SKILL-EVOLVE-USE-FAILURES** | `SkillEvolutionService.callLlmForImprovement()`（`skillforge-server/.../improve/SkillEvolutionService.java:206-225`）只把 skill 的 usageCount/successCount/successRate 和当前 SKILL.md 喂给 LLM，**不传具体失败 scenario**。对照 EVAL-V2 M6 已为 PromptImprover 接通 eval failures（`PromptImproverService.generateCandidatePrompt` 把 task / attribution / score 传给 LLM），SkillEvolution 是**对称缺口**——skill 自动改进闭着眼瞎改。来源：2026-05-07 外部 review spot-check 验证 TRUE | 真实跑 skill auto-evolve 后用户反馈"改了等于没改"/ EVAL 跑出 SKILL_EXECUTION_FAILURE 归因有量 / 主动想拉齐 PromptImprover 闭环 | 主旨：在 SkillEvolutionService 里 query EvalTaskRepository 取最近 N 个该 skill 相关失败 scenario（task / attribution / 失败原因 / agent_final_output），拼进 LLM prompt。**P1 Mid**（SkillEvolutionService 单文件 + EvalTaskRepository 加查询方法，约 50-80 行 + 测试），需求包待真做时建 |
| **EVAL-PARALLEL-SCENARIO** | `EvalOrchestrator.runEval()`（`skillforge-server/.../eval/EvalOrchestrator.java:124,136-337`）注释 `// 5. Run each scenario sequentially (v1.0)`，scenario 全串行执行 LLM 调用。AbEvalPipeline / SkillAbEvalService 同样串行。EVAL-V2 M0+M1 已为 >50 case throttle progress = dataset 增长是预期。100 个 scenario 串行 ≈ 10-20 分钟 | dataset 大到 eval 任务 >10 分钟用户开始抱怨 / 想做 nightly auto-eval 但跑不完一晚 | 主旨：scenario 级**有界并发**（`Semaphore` 限 2-4 worker，不是 `parallelStream` 一把梭）+ LLM provider rate-limit 处理（429 backoff）+ 结果聚合线程安全（concurrent collection 收集 case 后顺序写 t_eval_task_item）+ EvalJudgeTool 同步并发，sandbox session 互不干扰。**P3 Mid**（约 100-150 行 + 并发测试），需求包待真做时建 |
| **EVAL-SCENARIO-EXTRACT-DEDUP** | `SessionScenarioExtractorService.extractFromSessions()` 批量提取后直接 `saveAll`，没检查是否已有相同 task 的 scenario draft；`extractFromSession` 单 session 版本同样无去重。随时间推移同一用户的重复操作会产生大量语义相同 draft，污染 dataset 库 | 用户反馈 dataset 出现明显重复 task / draft 数量爆炸但有效 scenario 没增加 | 主旨：保存前 embedding fuzzy match（`EmbeddingService` 基础设施已存在）+ 兜底 task 文本归一化后 hash 去重；命中已有 draft 时合并 source_session_ids 而不是新建。**P3 Light**（约 30-50 行 + 测试），需求包待真做时建 |
| **EVAL-LATENCY-DIM-EXPLICIT-NULL** | `EvalScoreFormula.normalizeThresholdMetric()` 当 `threshold == null \|\| threshold <= 0` 时**直接返回 100.0**（满分）。多数 scenario 没配 `performanceThresholdMs`，导致 latency 维度永远满分掩盖真实性能问题。M4 加了多维度评分但这个分支让 latency 维度信号失真 | 用户做 A/B 比对发现"两边 latency 都 100 分"但实际 B 慢一倍 / 想做 perf regression 报警但 latency 维度无效 | 主旨：threshold 未配置时**不给满分**，改为标 `dimensionStatus="not_measured"` 该维度**不参与综合分加权**（综合分按已测维度权重重归一），FE 显示 "not measured" 而不是 100。**反对**外部分析师建议的"全局默认 30s"，那会让没配置的 scenario 集体掉分制造噪音。**P3 Light**（EvalScoreFormula + DTO + FE 一处显示，约 40-60 行 + 测试），需求包待真做时建 |
| **SKILL-FROM-SESSION** | SkillForge 当前 skill 创建只有 dashboard 手写 SKILL.md + 第三方 marketplace 装包导入两条路径，**没有 "用户跟 agent 走过一个有价值的 session → 提炼成可复用 skill" 的路径**。grep `createSkillFromSession\|sessionToSkill\|distillSkill` 全 0 命中确认。这是 SkillForge 心智的天然缺口——SKILL-IMPORT（外部）+ SkillEvolutionService（自动改进现有）已闭环，缺这一条"内部沉淀"的路径就把 skill lifecycle 最重要的入口堵上了 | 用户多次表达"这个 session 走完发现这种问题反复出现，希望能沉淀复用" / 真实跑 EVAL 后想用 eval-passing 的 session 直接做模板 | 主旨：（1）`POST /api/skills/from-session` 接受 sessionId + 用户写的 skill 元信息（name / description / 适用场景）；（2）BE 抽 session 的 user goal / 关键工具调用序列 / 成功路径生成 SKILL.md 草稿（LLM 辅助）；（3）FE chat header 加"沉淀为 skill"按钮（idle session 才可用）+ 草稿 review drawer 让用户改完确认；（4）skill 落 `t_skill` 标 `source_type=session_distilled` + `source_session_id` 反查；（5）跟 #4 EVAL-SKILL-EVOLVE-USE-FAILURES 联动：从 session 来的 skill 在 SkillEvolutionService 改进时优先取**该原始 session 之后的同类 session 失败数据**作 process trace。**P1 Mid**（约 200-300 行：1 service + 1 controller + 1 LLM prompt template + 1 entity 字段 migration + FE drawer），需求包待真做时建。**故意不带 dynamic user-sim**——评测用现有 SkillAbEvalService A/B 比对（disable vs enable 新 skill）即可，user-sim 走独立条目 |
| **EVAL-DYNAMIC-USER-SIM** | EVAL-V2 当前 multi-turn 是**脚本化** `conversation_turns`，agent 改进后 user turn 与 agent 新回复语义脱节，导致 `AbEvalPipeline` 和 `SkillAbEvalService` 都 `log.warn(... falling back to single-turn ... R5 follow-up)` —— 实际只跑 single-turn。新 skill cold start 期没有真实失败数据时也没法做 process-level 迭代优化，只能等真实使用积累。设计：A agent 扮演 user（anchor 在原 session 抽出的 userGoal/Constraints/Persona 而**不是脚本 turn**）↔ B agent 跑被测 agent ↔ Judge agent 看完整 transcript 出 process-level 优化建议（具体哪轮哪步出错 + SKILL.md 怎么改）→ 写回 `SkillEvolutionService` | 用户开始大量做 conversational agent / multi-turn fallback warning 实际困扰评测准确性 / SKILL-FROM-SESSION 跑了一段时间后出现 "新 skill 想立刻迭代但还没真实数据" cold-start 痛点 | 主旨：（1）`SessionScenarioExtractor` 输出层加 `userGoal/userConstraints/userPersona` 字段（独立于 `conversation_turns`，新 schema 字段）；（2）`UserSimulatorAgent` 新建（system prompt anchor 在三个字段，不允许跑题）；（3）`ScenarioRunnerTool.runScenarioWithUserSim()` 新分支用 TeamCreate 起 A/B 双 session；（4）`EvalJudgeTool` 新增 `judgeWithProcessTrace()` 出 turn-by-turn 优化建议；（5）`AbEvalPipeline` / `SkillAbEvalService` 把 fallback warning 替换成真正的 multi-turn 路径。**已知风险**：3x token cost / 重现性弱（user-sim 非确定性，需 seed 但效果有限）/ user-sim 自身需要校准（不像目标 agent 才是 bug）。**P2 Mid**（~400-500 行：1 schema migration + 1 service + 1 agent role + 4 tool 方法 + 测试），需求包待真做时建。**当前 SkillForge 偶尔跑 EVAL，cold-start 痛点未实证；P1/P2 之间动态调整等 SKILL-FROM-SESSION 落地后看真实信号** |
| ~~**OBS-3 跨 session unified trace tree**~~ | **superseded by [OBS-4 root_trace_id](requirements/archive/2026-05-03-OBS-4-root-trace-id/)（2026-05-03 已交付）**。OBS-3 v1（UI 层 nested rendering + 启发式 DFS 拼装）2026-05-03 上午尝试后回滚（commit `2d1a7bf`）。教训：数据模型不动 UI 层硬拼凑无法表达"一次完整调研"语义，nested 全展开把 child spans 淹没父主线。OBS-4 用 schema 一等公民字段 `root_trace_id` 把跨 agent / 跨 session trace 串联起来，UI 改成二级折叠 inline group（默认折叠），完整解决该需求 | — | — |

## 阅读规则

实现任务时，先打开链接的需求包。实现前读 `prd.md` 和 `tech-design.md`；只有原始产品意图或限制不清楚时才读 `mrd.md`。
