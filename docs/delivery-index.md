# SkillForge 交付索引

> 更新于：2026-05-02(docs governance 补回写：MSG-1 / xiaomi-mimo provider / P9-2 commit hash)
> 目的：集中维护已完成交付事实。`docs/todo.md` 只记录当前执行状态。

---

## 维护规则

1. **新功能先有需求入口**：进入实施前，优先创建或复用一个 requirement package，路径为 `docs/requirements/<state>/<ID>-<slug>/`。复杂需求使用 `mrd.md` / `prd.md` / `tech-design.md` 三件套。
2. **完成时同步两处**：任务完成后，同一个收尾 commit 必须更新 `docs/todo.md` 和本文档。`todo.md` 只保留短完成摘要；本文档补完整索引行。
3. **索引行字段固定**：每个已完成交付至少记录 `完成日期`、`交付项`、`技术方案`、`辅助文档`、`验证/commit/migration`。
4. **无独立方案也要显式写清**：小修、热修或补丁没有技术方案时，`技术方案` 写“无独立方案”，并在备注里说明原因。
5. **Flyway migration 不回改**：已应用的 `Vxx__*.sql` 不再修改；后续 DB schema/data 变更新增下一个版本号，并在索引里记录 migration 编号。
6. **日期用完成日期**：表里的日期是代码/文档完成并验证的日期，不是方案创建日期。

---

## 已完成交付索引

| 完成日期 | 交付项 | 技术方案 | 辅助文档 | 验证 / commit / migration |
| --- | --- | --- | --- | --- |
| 2026-05-02 | **xiaomi-mimo LLM provider**（Solo 配置改动）：`application.yml.skillforge.llm.providers` 新增 `xiaomi-mimo` (type: openai, base-url: `https://token-plan-cn.xiaomimimo.com`, api-key: `${XIAOMI_MIMO_API_KEY:}`)，注册 8 个官方 lowercase model：`mimo-v2.5-pro`(default) / `mimo-v2.5` / `mimo-v2.5-tts-voiceclone` / `mimo-v2.5-tts-voicedesign` / `mimo-v2.5-tts` / `mimo-v2-pro` / `mimo-v2-omni` / `mimo-v2-tts`。`ModelConfig.KNOWN_MODEL_WINDOWS` 加 6 个 MiMo prefix（startsWith 匹配 lowercase normalize 后）：`mimo-v2.5-pro` / `mimo-v2.5-tts` / `mimo-v2.5` → 1,048,576 (1M tokens)；`mimo-v2-pro` / `mimo-v2-tts` → 1,048,576；`mimo-v2-omni` → 262,144 (256K omni-modal)。**两个 footgun fix**：(1) 用户给的 mixed-case `MiMo-V2.5-Pro` 等被 Xiaomi `/v1/models` registry 拒绝（`HTTP 400 "Not supported model"`），改全 lowercase 对齐 official names；(2) base-url 错误带 `/v1` 后缀导致 `OpenAiProvider` 硬编码 `baseUrl + "/v1/chat/completions"` 拼出双 `/v1` → openresty `HTTP 404`，改 base-url 为 root host (`https://token-plan-cn.xiaomimimo.com`)，与 deepseek / dashscope / claude / openai 既有 provider pattern 一致。Live 验证：直接 curl `mimo-v2.5-pro` chat completion 通（含 `reasoning_content` thinking mode）；现场 session `2ad5da4d` 用 Main Assistant `xiaomi-mimo:mimo-v2.5-pro` 跑多 sub-agent 协作（TeamCreate × 5 / 整合报告）成功；`/api/llm/models` 返回 8 个 lowercase MiMo IDs。本期发现一个测试 design 修正（dev self-check 修正 brief 给的"`skill-hub` → SKILLHUB"测试 case 逻辑 bug —— `SKILLHUB` 是单词无下划线 `replace('-','_')` 得不存在的 `SKILL_HUB`，改用 `SkillHub` 大小写 + dash 输入由 internal-source-rejection 路径覆盖）。**TTS variants** (`-tts` / `-tts-voiceclone` / `-tts-voicedesign`) 配置用于 catalog 可见性，不应通过 chat-completion path 调用；待 TTS-aware 调用 path 真做时再启用。无 schema migration | 无独立方案；Solo 配置改动 | 无 PRD（Solo 直接配置）；env var 在 `~/.zshrc` 加 `export XIAOMI_MIMO_API_KEY=<token>` | `08a6d4d`(初版加 mixed-case + /v1) + `afa8cf4`(lowercase fix) + `f6be1ab`(drop /v1 from base-url) + `474f794`(docs todo SKILL-IMPORT-BATCH commit hash 修正) + `6284217`(WAITING-SUBAGENT-UX 暂缓条目)；无 schema 改动；`mvn -pl skillforge-core test -Dtest=ModelConfigTest` 2/2 + `mvn -DskipTests install` 全 reactor SUCCESS；live curl 直接验通 + SkillForge `/api/llm/models` 返回 8 个 lowercase MiMo + session `2ad5da4d` 真实流量验证 |
| 2026-05-01 | **SKILL-IMPORT-BATCH 扫描 marketplace whitelist batch import**（Mid Pipeline）：补 SKILL-IMPORT 上的 batch 路径，给 dashboard 一个 endpoint / 后续 UI 用。新增 `POST /api/skills/rescan-marketplace?source=<marketplace>&userId=<uid>` REST endpoint（受 `AuthInterceptor` Bearer token 拦截）+ `SkillImportService.batchImportFromMarketplace(source, ownerId)` 扫所有 `allowedSourceRoots` 第一层子目录批量调既有 `importSkill()` 流程（**外层方法不带 `@Transactional`** 避免一个失败回滚整批）+ `BatchImportResult` record 4 桶分类（imported / updated / skipped / failed）+ marketplace source whitelist `EnumSet.of(CLAWHUB, GITHUB, SKILLHUB, FILESYSTEM)` 防止 internal source（UPLOAD / SKILL_CREATOR / DRAFT_APPROVE / EVOLUTION_FORK）通过此 endpoint 污染 catalog；TS 类型 `BatchImportResult` / `BatchImportResultItem` 导出到 `skillforge-dashboard/src/api/index.ts` 让后续前端设计 agent 直接 import。**关键决策**：(1) 失败一个不中断整批（continue + 4 桶分类，partial-success 风格）；(2) Files.list 走 try-with-resources 确保 fd 释放；(3) source 白名单 valueOf 后 contains 校验，错误消息分两类（"invalid source" 枚举不存在 / "source must be one of" 枚举存在但非 marketplace）；(4) 多 source mapping config 留 follow-up（当前 `allowed-source-roots` flat list 不区分 source，多 source 时升级为 `Map<SkillSource, List<Path>>`）；(5) DECIDE-1(A) 主会话决策接受 SkillController userId query param pattern（pre-existing 全 controller 同款），PRD F1/F4/AC-7/AC-8 描述对齐 codebase + 加 follow-up 到 P12-PRE "auth model upgrade" scope（多用户决策定型后统一从 token extract userId）。Mid Pipeline 走完：1 backend dev → 2 sonnet reviewer 对抗 1 轮（A: 2 blocker / 3 warning；B: 2 blocker / 2 warning，**双 reviewer 独立 converge** 在 source whitelist 缺失 + auth pattern deviation 两条 blocker）→ opus judge 重判 → NEEDS_FIX (1 必修 + 1 主会话决策项) → 主会话拍 DECIDE-1(A) + dev 一次 fix（MUST-1 source whitelist + MUST-2 IT mock 零风险防御 + DECIDE-1(A) PRD doc 4 项对齐 + P12-PRE follow-up note）→ phase final 主会话独立 mvn test 20/18 pass + 2 IT skip 无 Docker pre-existing baseline + spot-check 3 关键风险点全落地。无 schema migration（V33/V38 字段已齐）。**附加 follow-up**：6 条（含 reviewer 已列 nits + Judge 增补 4 项）+ Judge 附加观察 SkillController 全 controller auth model upgrade（已写到 P12-PRE 决策行）；测试 design 修正：dev self-check 发现 brief 给的"`skill-hub` → SKILLHUB"测试用例逻辑 bug（`SKILLHUB` 是单词无下划线 `replace('-','_')` 得不存在的 `SKILL_HUB`），改用 `SkillHub` 大小写 + dash 输入由 internal-source-rejection 路径覆盖 | [SKILL-IMPORT-BATCH 技术方案](requirements/archive/2026-05-01-SKILL-IMPORT-BATCH-rescan-marketplace/tech-design.md) | [SKILL-IMPORT-BATCH PRD](requirements/archive/2026-05-01-SKILL-IMPORT-BATCH-rescan-marketplace/prd.md)、Reviewer/Judge/Nit 报告在 `/tmp/review-batch-{a,b}-r1.md` + `/tmp/judge-batch-r1.md` + `/tmp/nits-followup-skill-import-batch.md`（保留作审计） | 待 commit；无 schema 改动；`mvn -pl skillforge-server -am test -Dtest='SkillImportServiceBatch*,SkillControllerRescanMarketplace*,SkillRescanMarketplaceIT,SkillControllerTest'` 20/18 pass + 2 IT skip + 0 fail/error，含 `SkillImportServiceBatchTest` 7 + `SkillControllerRescanMarketplaceTest` 5（+1 fix 后新加 internalSource_returns400）+ `SkillRescanMarketplaceIT` 2（IT skip）+ `SkillControllerTest` 6（既有 + 1 新 ctor mock 不破坏）；前端 `npx tsc --noEmit` 通过；**Phase Final live curl AC-1 暴露生产 bug：self-invocation 跳 Spring proxy → `@Transactional` 失效 → `@Modifying` 抛 `InvalidDataAccessApiUsageException`（同 P9-2 / SKILL-IMPORT MUST-2 "测试假阳性"footgun）。修法 D9：抽 `SkillBatchImporter` orchestrator（inject `SkillImportService` 走 Spring proxy → `@Transactional` 自然生效），不动 D2 partial-success 行为；测试 rename `SkillImportServiceBatchTest` → `SkillBatchImporterTest`，`SkillRescanMarketplaceIT` 同样 rewire，2 个 controller test 改 mock `SkillBatchImporter`。Phase Final 重测 live curl AC-1 imported=3 + AC-2 updated=3 + t_skill 落库 owner_id=1 + 文件落盘三方一致** |
| 2026-05-03 | **OBS-3 跨 session unified trace tree（Mid Pipeline）**：在 OBS-2 数据模型基础上做 UI 层 unified trace tree — 父 trace waterfall 内嵌套显示 child trace 的 spans（缩进 + 折叠 + child status badge），跨 session 边界但 schema 不动（child trace 仍独立 `t_llm_trace` 行）。**BE**：新 endpoint `GET /api/traces/{traceId}/with_descendants?max_depth=3&max_descendants=20&user_id=X`；新 service `TraceDescendantsService` (DFS via `t_session.parent_session_id` + cycle detection visited set + `resolveDispatchSpan` 复用 OBS-1 SubagentSessionResolver 的 `childSessionId:` regex 模式扫父 trace tool spans)；新 4 DTO record；`LlmTraceStore` 接口加 `listTracesBySessionAsc`；`SessionRepository` 加 `findByParentSessionIdOrderByCreatedAtAsc`；`ChatEventBroadcaster.traceFinalized` (default no-op 向后兼容)；`AgentLoopEngine.finalizeTraceSafe` 集中挂载点 push WS（10 finalize 路径单一覆盖，未来扩展自动继承）；`ChatWebSocketHandler` 发 `type=trace_finalized` payload；`ObservabilityOwnershipGuard` 复用 OBS-1 模式。**FE**：新 `NestedWaterfallRenderer.tsx` 共享组件（`mode: 'full'\|'mini'` props）；full = 缩进 16px / vertical guide line / 颜色暗化 / ▶ icon button；mini = 缩进 8px / 紧凑单行 / 状态点；orphan descendants (parentSpanId=null) 渲染 standalone child-header；truncated → "Show more" 按钮 lazy load + react-query setQueryData merge dedup；3 处 waterfall 改造（`SessionWaterfallPanel` + `pages/Traces.tsx` + `RightRail.tsx` mini-waterfall）；`expandedSubtrees: Set<string>` 折叠状态切 trace 自动清空。**Multi-session WS subscribe（关键设计决定）**：fe-dev 实现 `wsSessionIdsKey` + `wsListRef` 模式 — 每个页面对 parent + 每个 descendant `sessionId` 各开一个 socket（FE-side 解决 BE Reviewer 提的 W2 channel mismatch，避免 BE 多 broadcast）；WS handler `setQueryData` 直接 mutate `descendants[i].status / totalDurationMs / toolCallCount / eventCount`（不 refetch）；ref guard `wsListRef.current === sockets` 防 race。SubagentJumpLink 不动（OBS-3 是 enhancement 不是 replacement）。**Pipeline trail**：Plan 跳过（PRD 完整唯一）→ BE+FE 并行 Dev → 双 Reviewer 对抗 1 轮（BE 0 blocker / 3 warnings：W1 resolveDispatchSpan N+1 / W2 channel mismatch 已被 FE multi-subscribe cover / W3 缺 IT；FE 0 blocker / 5 warnings：W1 重复 type / W2 dead memo / W3 WS handler ref churn / W4 loading state / W5 Unicode │ 在 1px 框溢出）→ 主会话 Quick wins inline fix W2 dead memo + W5 Unicode │（清空 span，纯 CSS background 画 guide line），剩余 6 warnings 留 follow-up。**关键决策（Q1-Q8）**：DFS 深度 ≤3 / descendants ≤20 同步 + lazy load 第二层 / 默认折叠 / endpoint snake_case / 缩进 16px+8px / WS push trace_finalized / SubagentJumpLink 保留 / 3 处 waterfall 全覆盖（Q8 用户提：SessionWaterfallPanel + Traces.tsx + RightRail mini） | [OBS-3 PRD](requirements/active/2026-05-03-OBS-3-unified-trace-tree/prd.md) | [OBS-3 技术方案](requirements/active/2026-05-03-OBS-3-unified-trace-tree/tech-design.md)、Code Review 文档 in `/tmp/obs-3-be-review-r1.md` + `/tmp/obs-3-fe-review-r1.md`（保留作审计） | OBS-3 commit (hash 待补)；`mvn test` BUILD SUCCESS（core 160 / tools 14 / observability 22 / server 698 / cli 31）；新增测试 29 cases：`TraceDescendantsServiceTest` (12) + `TraceWithDescendantsControllerTest` (4) + `AgentLoopEngineTraceFinalizedBroadcastTest` (5) + `NestedWaterfallRenderer.test.tsx` (8)；`tsc --noEmit` clean (0 new errors)；`vite build` 成功；现场 session `6f18ecca`（3 个 TeamCreate child）作为开发 + 验证 fixture |
| 2026-05-03 | **OBS-2 M4 关闭 t_trace_span 写入路径 + M2 ETL 实际触发**（Full Pipeline 简化版）：M3.5 观察期确认双写一致后，主会话直接 SQL 跑 M2 ETL（仿 Phase A 模式，绕过 Flyway placeholder + restart 流程）：33 legacy trace 转 ok/error + 920 tool spans + 4 event spans (2 ASK_USER + 1 COMPACT + 1 INSTALL_CONFIRM) 回填到 t_llm_trace + t_llm_span (source='legacy')，所有缺失检查归零。然后 M4 关闭旧轨写入：BE Dev 删 `AgentLoopEngine` 16 处 `traceCollector.record(...)` 调用 + 删 7-arg `recordConfirmationTrace` dead helper（保留 rootSpan 对象 + `setId(loopCtx.getTraceId())` OBS-2 不变量 + `traceCollector` field/setter 保留 noop 化 + TraceCollector 接口 / TraceSpan 类 / TraceSpanRepository / TraceSpanEntity 都不删，M5/M6 才 cleanup）；`GetTraceTool` (LLM-callable) 切到 `LlmTraceStore` + `LlmTraceRepository`（输出 shape 跟 M3 TracesController 对齐保兼容 LLM prompt 习惯）；新增 `AgentLoopEngineNoTraceCollectorRecordIT` 锁不变量（tool-using + text-only 双 chat run + `verify(traceCollector, never()).record(...)`）。**主会话 reviewer W2 inline fix**（review verdict PASS 后扩大范围）：`LifecycleHookDispatcherImpl:451` (LIFECYCLE_HOOK trace_span) + `TeamSendTool:172` (PEER_MESSAGE trace_span) 仍调 `traceCollector.record(span)` 是 M4 范围漏洞（PRD §Q2 "关写后只读再 drop" 要求所有写路径关闭），主会话直接注释这 2 处 + 修 `LifecycleHookDispatcherTest` 7 个失败测试断言（hasSize(N) → isEmpty + collected.get 多行 statement 注释，by Python regex sub）。**关键决策（Q1-Q5 全推荐）**：保留代码 noop 化 / 不加 feature flag / M2 ETL 不强制再跑 / 保留 R__migrate_legacy_trace_span.sql / 不加 DB enforcement。Pipeline trail：Plan 跳过（PRD §M4 完整唯一）→ 1 BE Dev 中途 user-pause 后续 → 1 java-reviewer 对抗 1 轮 PASS（0 blocker / 3 warnings：W1 GetTraceTool N+1 是 pre-M4 继承的已知 / W2 已 inline 修 / W3 死参数留 M5/M6） | [OBS-2 PRD](requirements/active/2026-05-02-OBS-2-trace-data-model-unification/prd.md) | [OBS-2 技术方案](requirements/active/2026-05-02-OBS-2-trace-data-model-unification/tech-design.md)、Code Review 文档 in `/tmp/obs-2-m4-review-r1.md` + `/tmp/nits-followup-obs-2-m4.md`（保留作审计） | M2 ETL 直跑（无 commit，纯数据动作）+ M4 commit `b31cfaf` ；`mvn test` 全过 904/904（core 160 / tools 14 / observability 22 / server 677 / cli 31）+ `AgentLoopEngineNoTraceCollectorRecordIT` 2 IT cases 全过；grep `traceCollector\.record` production 代码 = 0 命中（除 2 处 OBS-2 M4 注释） |
| 2026-05-02 | **OBS-2 Trace 数据模型统一（M0-M3 已交付，M3.5+M4+M5+M6 待跟进）**（Phase A 数据修复 + 4 阶段 Full/Mid Pipeline）：把 trace/span 数据模型从 OBS-1 双轨（旧 `t_trace_span` 单表 + 新 `t_llm_trace`/`t_llm_span` 仅 LLM 维度）收敛到单一来源。**Phase A**（前置数据修复）：直接 SQL 等价 R__migrate_legacy_llm_call ETL 把 OBS-1 上线前 4-27/4-28 的 33 trace + 176 LLM 调用回填到 `t_llm_trace`/`t_llm_span` (`source='legacy'`)；**M0**（schema migration，Solo）：`V41__add_trace_id_to_session_message.sql`（server）+ `V42__llm_trace_aggregate_fields.sql`（observability，加 `total_duration_ms`/`tool_call_count`/`event_count`/`status`/`error`/`agent_name`）+ `V43__llm_span_kind_event_type_name.sql`（observability，加 `kind`/`event_type`/`name` + `(session_id\|trace_id, kind, started_at)` 复合索引）；**M1**（写入路径双写，Full）：`ChatEventBroadcaster.messageAppended` 加 `traceId` 参数（9 处 production 调用点）+ `LlmTraceStore` 新 5 方法（`upsertTraceStub` 同步 + `writeToolSpan`/`writeEventSpan`/`finalizeTrace` 异步 via TransactionTemplate 规避 self-invocation AOP + `listSpansByTrace`）+ `AgentLoopEngine` 改造（`rootSpan.setId(traceId)` ID 对齐 + `AtomicInteger` 计数器修 lambda effectively-final + 多线程 race + 4 类 event span 双写 + tool span 双写 + 10 finalizeTrace 路径全覆盖 + `WHERE status='running'` 幂等 SQL guard）+ `ChatService` 改造（构造器注入 `LlmTraceStore` + chatStream/answerAsk/answerConfirmation 入口生成 UUID traceId + runLoop 6-arg overload + catch 块保底 finalize 0/0 fallback counts）+ 新增 `TraceLifecycleSink` 接口 + `LlmTraceLifecycleSinkAdapter` 桥接（避免 core ↔ observability 循环依赖）；**M2**（历史数据迁移，Mid）：`R__migrate_legacy_trace_span.sql` Repeatable migration（仿 R__migrate_legacy_llm_call placeholder 模式，`etl_trace_span_mode: off\|flyway` 控制，UPDATE `t_llm_trace` + INSERT `t_llm_span` kind='tool'/'event'，含 `source='legacy' AND status='running'` filter 不动 live + ON CONFLICT DO NOTHING + NOT EXISTS guard ±1s + iteration_index 防 shadow live + deployment runbook 写在 SQL 文件头说明 ordering 依赖）+ `ObservabilityFlywayConfig` 加 `etl_trace_span_mode` placeholder + `application.yml` 加配置；**M3**（API + 前端切读，Full）：`/api/traces` 切到 `t_llm_trace`（`countByTraceIdsAndKind` GROUP BY 单查询消除 N+1，30 trace 从 30+1 → 2 SQL）+ `/api/traces/{id}/spans` 切到 `t_llm_span` 扁平列表（去 BFS）+ `/api/observability/sessions/{id}/spans` 加 `traceId` 过滤参数（去两表 merge）+ `/api/observability/tool-spans/{id}` 切到 `t_llm_span where kind='tool'` + 新增 `/api/observability/event-spans/{id}` + `EventSpanSummaryDto`/`EventSpanDetailDto` records；前端 `SpanSummary` union 加 `EventSpanSummary` + `VALID_EVENT_TYPES` derived type + `normalizeEventType` runtime guard + `SessionDetail.tsx` `sessionSpansQuery` 改 per-trace（移除 M0 临时 `limit=1000` hack） + `SessionWaterfallPanel` 加 event filter chip + amber 颜色 + `EventSpanDetailView` 新建 + `SessionStatsBar` **移除 Messages 字段**（Q7 C1 决策）；M1 留下的 W1（`StoredMessage` 加 `traceId` + boundary preservation 用 7-arg `AppendMessage` 保留历史 trace_id）/ W2（`listSpansByTrace` 用 `Pageable` SQL LIMIT 替代内存全量加载）/ W3（`TraceIdPropagationIT` + `DualWriteToolSpanIT` + `LlmSpanCountByTraceIdIT` 共 6 IT cases）切读前必修；**B-1 主会话决策**（FE 揭示 UX regression：M3 后 `trace.input/output` 为 null → TraceDetailPanel "User query"/"Assistant result" 块变 "—"）：`SessionMessageDto` 加 `traceId` 字段 + `SessionService.getFullHistoryDtos` 透传 + 前端 `selectedTraceOverview` 从 `messages.filter(m.traceId === selectedTraceId)` 派生 first user/last assistant + 删除 `selectedTraceFullQuery` dead code。**Pipeline trail**：M0 Solo（dev DB transaction-rollback dry-run）；M1 Full（Plan r1 FAIL 3 blocker + r2 FAIL 1 blocker §D.8 ChatService 依赖 → 主会话 inline fix 进 Dev → Code Review r1 PASS 0 blocker / 3 warnings 留 follow-up）；M2 Mid（Plan 跳过 + Dev DONE_WITH_CONCERNS C1-C4 + Review PASS 全部 ACCEPT/ACCEPT-with-runbook + 主会话 inline 加 deployment runbook 注释 W1）；M3 Full（Plan 跳过 + BE+FE 并行 Dev + B-1 主会话 inline + Code Review r1 BE FAIL 1 blocker N+1 / FE PASS → r2 fix LlmSpanRepository batch GROUP BY + LlmTraceStore 接口提升 + null-kinds 默认填 ALLOWED_KINDS + FE eventType runtime guard → r2 双双 PASS）。**关键决策（PRD 11 Q）**：A1 event 统一 kind='event' / B2-a stats bar per-trace / Q3 kind 不含 agent_loop / Q4 加通用 name 列 / Q5 tool blob 留 OBS-3 / Q6 t_session_message.trace_id 不 backfill / Q7 移除 stats bar Messages 字段 / Q8 M2 Repeatable migration / Q9 broadcaster A.1 加显式参数 / Q9b ChatService 提前生成 traceId + A.3 孤儿 fallback / Q1+Q2+Q10 双写过渡期 2-3 天 + M3 完成 1-2 周后 M4 关旧写 + M5 观察 4 周 + M6 drop。**已识别 known issue**（commit message 记录 + 留 follow-up）：BE W2 `getTraceSpans` 无 Pageable / BE W3 `findAll()` admin 路径无边界 / FE pre-existing useMemo setState（不是 OBS-2 引入）/ FE pre-existing tsc -b errors（同）/ FE messages-loading flash 短暂 "—" / FE amber light-mode 3.65:1 contrast 低于 WCAG AA / FE-r2 normalizeEventType 缺 vitest unit tests / FE-r2 N1 unused eslint-disable | [OBS-2 PRD](requirements/active/2026-05-02-OBS-2-trace-data-model-unification/prd.md) | [OBS-2 MRD](requirements/active/2026-05-02-OBS-2-trace-data-model-unification/mrd.md)、[OBS-2 技术方案](requirements/active/2026-05-02-OBS-2-trace-data-model-unification/tech-design.md)、Plan/Review 文档系列 in `/tmp/obs-2-{m1,m2,m3}-{plan-r{1,2},review-r{1,2},be-review-r{1,2},fe-review-r{1,2}}.md` + `/tmp/nits-followup-obs-2-*.md`（保留作审计） | `cf9123c`(M0 临时 hotfix limit=1000)+`06b2372`(PRD)+`edd5a21`(M0 schema)+`4412729`(M1 写入)+`26023c5`(M2 ETL)+`69ee35b`(M3 API+前端)；`V41`/`V42`/`V43` + `R__migrate_legacy_trace_span.sql`(默认 mode=off)；`mvn test` BUILD SUCCESS：core 全绿 / observability 22 tests（5 OBS-1 + 2 PgLlmSpanEntityIT + 3 PgLlmTraceLifecycleIT + 12 PgLlmTraceSpanMigrationIT）/ server 671 tests / cli 31 tests，0 fail / 0 err / 29 docker-skipped；新增 IT：`PgLlmTraceLifecycleIT`(3) + `PgLlmTraceSpanMigrationIT`(12) + `TraceIdPropagationIT`(2) + `DualWriteToolSpanIT`(2) + `LlmSpanCountByTraceIdIT`(2) + `SessionSpansMergedIT` (+2 null/empty kinds) + `SessionSpansAuthIT` (+event auth + kind mismatch)；同时**修 M1 commit fake positive**：M2 改 `skillforge-observability/pom.xml` 加 surefire `**/*IT.java` include，让 M1 commit 当时被 silently skip 的 IT（PgLlmTraceLifecycleIT + PgLlmSpanEntityIT）真正运行；**M3.5 / M4 / M5 / M6 后续**（按 PRD §10 timeline）：观察期 1-2 周 → 关闭 t_trace_span 写入 → 4 周观察 → drop t_trace_span |
| 2026-05-01 | **SKILL-IMPORT 第三方 marketplace 装包导入 SkillForge**（Mid Pipeline）：现场 bug 是 agent 调用 `npx clawhub install <slug>` 把 skill 装到 ClawHub CLI 自己的 workspace（`~/.openclaw/workspace/skills/`），SkillForge `t_skill` 表 0 user 行 / `data/skills/` 空 / dashboard 不可见。修法是补 P1-D 留下的 import 缺口：新增 `ImportSkill` Java Tool（agent 入参 `{sourcePath, source}`），后端 `SkillImportService` 校验白名单（`application.yml` 配 `skillforge.skill-import.allowed-source-roots: [~/.openclaw/workspace/skills]` + `~` 展开 + `toRealPath()` 解析符号链接）→ 解析 `SKILL.md` frontmatter + `_meta.json`（version 缺失 fallback `latest`）→ `SkillStorageService.allocate(SkillSource.<source>, ctx)` 用 P1-D 留好的路径分配（CLAWHUB → `clawhub/{slug}/{version}`）→ 递归 cp 文件 → `SkillCatalogReconciler.hashSkillMd(target)` 算 sha256 of SKILL.md（D6：在 reconciler 加 public 入口暴露内部已有 sha256 算法，与 reconciler 启动 reconcile 算法 byte-for-byte 一致避免下次启动误判 hash 漂移）→ 原生 `INSERT t_skill ... ON CONFLICT (COALESCE(owner_id, -1), name) DO NOTHING`（与 V31 `uq_t_skill_owner_name` 索引精度一致）→ 注册到 SkillRegistry 让 agent 后续 turn 立即可用；改 `system-skills/{clawhub,github,skillhub}/SKILL.md` 加 "After Install: Register to SkillForge" 段引导 agent 装完调 ImportSkill；新增 `ImportResult` record + `SkillImportProperties` config bean。**关键决策**：(1) Tool name `ImportSkill`（中文 description 让 agent 理解），(2) 同 slug 直接 UPDATE 不报错（用户拍板），(3) 旧版本路径变 orphan log.warn 不删（沿用 P1-D 风格），(4) D8 shared path 不带 ownerId（避免触动 SkillStorageService 核心文件升 Full）。Mid Pipeline 走完：Phase 1 Backend Dev → Phase 2 双 Sonnet Reviewer 对抗 1 轮（A: 1 blocker / 2 warning / 5 nit；B: 0 blocker / 3 warning / 3 nit）→ Phase 3 Judge Opus 重判（NEEDS_FIX：1 维持 blocker = F2 orphan log.warn 漏；1 warning 升 blocker = 并发 retry 是 Mockito 假阳性 + tech-design 明文要求 `ToolResultArchiveService.insertIgnoreConflict` 模式没沿用；1 warning 升 must one-liner = `def.getName()` → `meta.slug()` 消除 frontmatter rename 重复行 corner case）→ Phase 3.5 Dev 一次性 fix（MUST-1 加 orphan log.warn 含 row.id + oldPath + newPath + "orphan"+"not deleted" 关键词 + 双向 log capture 断言；MUST-2 改用 native `INSERT ... ON CONFLICT DO NOTHING` + 18 列字段映射 + 重写 concurrent test 改 mock `insertIgnoreConflict` 返回 0 + 新加 IT method `insertImportedSkillIgnoreConflict_realPg_returnsOneOnFresh_thenZeroOnDup`；MUST-3 `meta.slug()` 替换 `def.getName()` 在 row.name + lookup key）→ Phase Final 主会话独立 mvn test 26/22 pass + 4 IT skip + 0 failed/errors / spot-check 3 MUSTs 全落地 / hash 算法一致性 / SQL ON CONFLICT 与 V31 索引精度匹配确认。**附加 follow-up**：UNIQUE 索引只保 (owner_id, name) 不含 source 列，dev 防御代码 fail-loud 不会 silent overwrite，用户决策方向 (a) 改错误信息 + 专属 exception 等真遇到重名再做（**不升 Full 不改 schema**）；W2 Registry 在 @Transactional 内 ghost entry 风险极低 + restart 自愈，留 follow-up | [SKILL-IMPORT 技术方案](requirements/archive/2026-05-01-SKILL-IMPORT-third-party-marketplace/tech-design.md) | [SKILL-IMPORT PRD](requirements/archive/2026-05-01-SKILL-IMPORT-third-party-marketplace/prd.md)、Reviewer/Judge/Nit 报告在 `/tmp/review-be-{a,b}-r1.md` + `/tmp/judge-r1.md` + `/tmp/nits-followup-skill-import.md`（保留作审计） | 待 commit；无 schema 改动；`mvn -pl skillforge-server -am test -Dtest='SkillImport*,ImportSkillTool*'` 26/22 pass + 4 IT skip（无 Docker pre-existing baseline）+ 0 failed/0 errors，含 `SkillImportPropertiesTest` 5 + `SkillImportServiceTest` 9（+1 fix 后新加 slug-vs-frontmatter）+ `ImportSkillToolTest` 8 + `SkillImportIT` 4（+1 MUST-2 native PG 真跑回归保险）；现场 session `f73cba8f-4a01-466d-bb24-e9de7cd69438` 触发本需求；commit `a09a17a` 主交付 + `4490e14` 顺手修 ChannelCardActionController 启动 bug |
| 2026-04-30 | **MSG-1 消息类型化 + ask_user 持久化 + 非阻塞 waiting_user**（Full Pipeline）：把"字符串 marker + 内存 latch"散落非常规消息正规化进消息流。`t_session_message` 加 `message_type` (32) + `answered_at` (TIMESTAMPTZ) + `control_id` 列 + `idx_session_message_control` 索引（**V40 migration**）。Core 引擎 `AgentLoopEngine` (+361 行) 加 `InteractiveControlRequest.java`（104 行 control 抽象）+ `LoopResult.controlRequest`；server `ChatService` (+221 行) 实现非阻塞 `waiting_user` + worker 释放 + continuation 恢复，`ChatController` / `ChannelCardActionController` / `DefaultConfirmationPrompter` 配套；前端 `ChatWindow` (+123 行) + `Chat.tsx` 加 inline 卡片渲染分发，`PendingAskCard` 改造，`useChatWsEventHandler` 加 control message 重建（rawMessages 写路径，ask_user / confirmation 复活）；测试 `AgentLoopEngineToolUseInvariantTest`。**关键 message_type 取值**：`normal`（默认）/ `team_result`（TeamCreate 子 agent 返回）/ `subagent_result`（非 collab 的 subagent 返回）/ `ask_user`（agent 发起询问卡片）/ `confirmation`（框架审批卡片）。**关键决策**：(1) ask_user / confirmation 触发后当前 loop 结束 + worker 释放（不再 `CountDownLatch.await()` 占线程），用户点击卡片提交答案后补齐对应 `tool_result` + 开新 trace loop；(2) 自然语言新消息触发 `superseded` 当前未回答 ask 卡片 → 按普通 user turn 继续；(3) 审批卡片必须显式 approve / deny，自然语言不能隐式 approve；(4) 新 row 默认 `message_type='normal'`，前端 normalizeMessages 默认 normal 分支保持向后兼容；(5) 历史 `[TeamResult ...]` 不全量回写，新消息写 `message_type` + 前端对老 marker 兼容识别。**解决 BUG-30**（subagent 返回被渲染成用户消息 → 通过 `message_type='team_result'/'subagent_result'` + 前端按 type 分发渲染）+ **BUG-31**（ask_user 卡片 WS 重连后丢失 + 占 chatLoop worker → 持久化 + 非阻塞）。触碰核心文件清单：`ChatService` / `SessionMessageRepository` / `PendingAskRegistry` / `ChatWebSocketHandler` / `Chat.tsx` / `useChatWsEventHandler.ts`。**docs governance 注**：commit 当时未及时回写 status 字段（design-draft 一直挂在 active 表），2026-05-02 cleanup commit 一并补写归档 | [MSG-1 技术方案](requirements/archive/2026-04-30-MSG-1-message-typing/tech-design.md) | [MSG-1 PRD](requirements/archive/2026-04-30-MSG-1-message-typing/prd.md)、[MSG-1 MRD](requirements/archive/2026-04-30-MSG-1-message-typing/mrd.md) | `543a60e`；`V40__session_message_ui_types.sql`；mvn 编译 + 测试见 commit；解 BUG-30 + BUG-31 |
| 2026-04-30 | **P9-2 Tool Result 归档 + BUG-32 修复**（Full Pipeline）：归档超大单轮 tool result（>200K chars/message 聚合）+ request-time 聚合裁剪（每次发 LLM 前对副本裁，不动持久化历史）+ `max_tokens` 透明续写恢复（continuation 语义，单 turn 1 次防护对应 Claude Code `hasAttemptedReactiveCompact` per-413-event 语义）+ 砍掉默认 `max_input_tokens=500000` 累计硬停（改 opt-in `enforce_max_input_tokens`，业界标杆 Claude Code / OpenClaw 均无此累计闸门）。新增 `t_tool_result_archive` 表（V39 migration，UNIQUE(session_id, tool_use_id) + UNIQUE(archive_id) 兜底幂等不翻转不变量）、`ToolResultArchiveEntity` / `ToolResultArchiveRepository`（PG-safe `INSERT ... ON CONFLICT DO NOTHING`）/ `ToolResultArchiveService`（lookup → insertIgnoreConflict → re-lookup 三步 idempotent upsert，参考 Claude Code `partitionByPriorDecision` 三状态）、core 内 `ToolResultRequestBudgeter`（深拷贝裁剪不 mutate 原 messages）；改造 `AgentLoopEngine` 接入 budgeter + `hasAttemptedMaxTokensRecovery` per-iteration reset + continuation request 不带 tools + 砍默认硬停 + LLM_CALL trace 暴露 trim 元数据；`SessionService.getContextMessages` 集成 `applyArchiveSafely`（失败 fallback 不破坏 chat loop）；`ChatService.isSilentExit` 新增 `max_tokens_exhausted`。Full Pipeline 走完：Phase 1 Dev → Phase 2 双 Reviewer 对抗 1 轮（A: 0 blocker / 6 warning / PASS；B: 0 blocker / 4 warning / FIX）→ Phase 3 Judge Opus 重判（0 blocker / 10 warning，3 项必修 / 7 项 follow-up）→ Phase 3.5 Dev 一次性 fix（FIX-1 per-iteration reset / FIX-2 改 PG-safe ON CONFLICT DO NOTHING 修复"测试 Mockito 走通 PG 上跑不通的路径" / FIX-3 补 compaction interaction 测试）→ Phase Final 主会话 mvn 全量回归。**关键决策**：(1) "turn" 语义=单次 LLM 调用（不是 per-`run()`，对应 Claude Code per-413-event 一致）；(2) `ToolResultRetrieveSkill` 排除（与 Claude Code 一致，preview 对模型即是最终视图）；(3) Judge 抓出"测试假阳性"（Sonnet reviewer 标 warning，Opus Judge 升判到接近静默失败边界）是本轮最有价值的 catch | [P9-2 技术方案](requirements/archive/2026-04-30-P9-2-tool-result-archive/tech-design.md) | [P9-2 PRD](requirements/archive/2026-04-30-P9-2-tool-result-archive/prd.md)、[P9-2 MRD](requirements/archive/2026-04-30-P9-2-tool-result-archive/mrd.md)、Reviewer/Judge 报告在 `/tmp/review-be-r1-{a,b}.md` + `/tmp/judge-r1.md` + `/tmp/nits-followup-p9-2-tool-result-archive.md`（保留作审计） | `fe0404c`；`V39__tool_result_archive.sql`；`mvn -pl skillforge-core test` 158/158（含 `ToolResultRequestBudgeterTest` 7 + `AgentLoopEngineMaxTokensRecoveryTest` 5 共 12 P9-2 新增 cases）；`mvn -pl skillforge-server test` 602/602 unit 全绿（6 errors 全是 `ContainerFetchException` 本地无 Docker 的 IT，pre-existing baseline，与 P9-2 无关）；`ToolResultArchiveServiceTest` 7/7（6 既有 + 1 新 compaction interaction）；BUG-32 现场 session `919c2bda-1867-40eb-8c7c-4bb49d341ee2` 同形 fixture 进入 budgeter 路径覆盖 |
| 2026-04-30 | SKILL-LOAD Skill Loader Tool:在另一个 worktree(`feat/skill-loader-tool`)开发完成,本 session merge 进 main。改动:`AgentLoopEngine` 加 `skillLoaderToolSchema(visibleSkillDefs)` 静态方法 + `skill_loader` tool(让 agent 显式 load skill,**不再启动就把所有 skill schema 全送给 LLM,节省 input tokens**);`SystemPromptBuilder` 加 skill loader 描述列表逻辑;`ContextBreakdownService` 配合更新 tool_schemas 估算路径;新需求包 4 文件 | [SKILL-LOAD 技术方案](requirements/archive/2026-04-30-SKILL-LOAD-skill-loader-tool/tech-design.md) | [SKILL-LOAD PRD](requirements/archive/2026-04-30-SKILL-LOAD-skill-loader-tool/prd.md)、[SKILL-LOAD MRD](requirements/archive/2026-04-30-SKILL-LOAD-skill-loader-tool/mrd.md) | `8d1fb45`(主 commit,在 feat/skill-loader-tool worktree)+ `d74e01c`(merge 进 main,解 docs/README + docs/todo 冲突);新增测试 `AgentLoopEngineSkillLoaderToolTest` 4 cases + `SystemPromptBuilderTest` 3 cases 全绿;merge 后跟 P1-D / CTX-1 集成测试 58/58 全绿 |
| 2026-04-30 | **P1-D Follow-up(reconciler 4 元组同名处理)**:Phase Final 实测 reconciler Rescan 后发现 3 个 orphan 磁盘文件夹(`filesystem/legacy/clawhub/github/latest`、`1/690041a8-...`(code-review)、`1/d482137c-...`(skill-creator))未建 t_skill row 且报 `duplicate key value violates unique constraint "uq_t_skill_owner_name"`。根因:`SkillCatalogReconciler` 4 元组 compat match `(is_system, owner_id, name, source)` 在 (a) 旧 path 没 source segment 时推 source=NULL 跟旧 row 具体 source 不一致 → match 失败 → INSERT 撞 unique;(b) 同 owner 同 name 但不同 uuid 的两个磁盘文件夹(evolution fork v2 副本)第二次扫到时 4 元组 match 没找到第一次 INSERT 的 row(可能 EM cache / flush 问题)→ 又 INSERT 撞 unique。**临时修复**:SQL 删 5 个旧 user t_skill row(id 33/65/66/129/161,artifact_status=missing/shadowed,skill_path 指向旧路径)+ Rescan API 触发 reconciler 重建 → 4 个新 row 落地(2 个 active / 2 个 shadowed by system),同 owner 同 name 重复的 3 个仍 orphan。**真正修复**(转 follow-up commit):reconciler 4 元组 match 改成"宽松 source"(同 owner+name 同 type 仍 match,UPDATE 而不是 INSERT)+ 同名重复时走 conflict resolution 标 disabled/shadowed 而不是 INSERT 撞 unique | 无独立方案;治标修复直接执行 SQL,治本待 follow-up commit | [P1-D 技术方案](requirements/archive/2026-04-30-P1-D-skill-root-catalog-convergence/tech-design.md) | 无 commit;`PGPASSWORD=postgres psql -p 15432 -U postgres -d skillforge -c "DELETE FROM t_skill WHERE id IN (33,65,66,129,161)";` 后 dashboard Rescan 触发 reconciler RescanReport[created=5,updated=5,shadowed=2];3 个 orphan 文件夹保留(可手动 rm 或留 P2 follow-up reconciler 修复时一起处理) |
| 2026-04-30 | P1-D Skill Root 与 Catalog 收口:把 Skill 物理存放、启动加载、DB catalog 和冲突治理收口。新增 `SkillForgeHomeResolver`(env var 优先 + Java 启动期向上找项目根 + skillforge-server 双 anchor + fail-fast)、`SkillStorageService`(8 SkillSource enum + 路径分配 + path traversal 防御)、`SkillCatalogReconciler`(扫盘 + 4 元组 compat 匹配 + RescanReport)、`SkillConflictResolver`(独立 @Service + REQUIRES_NEW per-row 隔离)、`SkillSource` / `AllocationContext` / `RescanReport` / `LlmContextLengthExceededException` 等;V33 migration 加 4 字段(content_hash / last_scanned_at Instant / artifact_status / shadowed_by);改造 4 个 server 入口(`SkillService.uploadSkill` / `SkillCreatorService` / `SkillDraftService.approveDraft` / `SkillEvolutionService` fork)统一走 SkillStorageService;`SystemSkillLoader` / `UserSkillLoader` 注入 HomeResolver + Reconciler 接管;`POST /api/skills/rescan` API + Skills API 暴露 governance 字段 + DELETE system 403 拦截;前端 SkillTable / SkillDrawer / SkillList 加 governance 字段 + Rescan 按钮 + isSystem 禁删 UI(double-disabled fail-secure);**删除 dead 的 ClawHub Java 链路**(整 package + ClawHubTool + application.yml 配置块);**一次性数据迁移**(grill-me 迁 system-skills + 双 data/skills 合并);Full Pipeline 走完(Plan 1 轮 NEEDS_FIX + Phase 3 r1 NEEDS_FIX 2 blocker + r2 双 PASS + Judge r2 PASS);新增测试 16 个(SkillForgeHomeResolverTest 5 + SkillStorageServiceTest 11 + SkillCatalogReconcilerTest 8 + SkillConflictResolutionTest 5 + SkillControllerTest 6 + RescanReportTest 3 + SkillTable.test.tsx 6 + SkillDrawer.test.tsx 2 + SkillList.test.tsx 5 新增 case + AgentLoopEngineSkillViewFailSecureTest 4 既存回归)| [P1-D 技术方案](requirements/archive/2026-04-30-P1-D-skill-root-catalog-convergence/tech-design.md) | [P1-D PRD](requirements/archive/2026-04-30-P1-D-skill-root-catalog-convergence/prd.md)、[P1-D MRD](requirements/archive/2026-04-30-P1-D-skill-root-catalog-convergence/mrd.md)、Plan/Review/Judge 文档系列在 `/tmp/plan-p1-d-r1.md` + `/tmp/review-{be1,be2,fe1,be-r2,fe-r2}-r{1,2}.md` + `/tmp/judge-r{1,2}.md`(保留作审计) | `6b19439`(主 commit)+ `223e5a8`(V33→V38 rename fix,因勘查阶段漏勘 skillforge-observability module 的 V33,backend 启动 Flyway 报"more than one migration with version 33",rename 为 V38 后启动成功);`V38__skill_root_catalog_convergence.sql`;`mvn test -pl skillforge-server -Dtest=<P1-D 9 个测试类>` 53/53 全绿;dashboard `vite build` 通过 1.60s;`vitest run skills/__tests__/ pages/__tests__/SkillList.test.tsx` 18/18 全绿;Phase Final user dashboard 实测:Skills 页 governance UI(type/source/artifactStatus/shadowedBy/lastScannedAt 列)、Rescan 按钮(返回 RescanReport)、system skill 禁删 UI(disabled + Tooltip)均工作正常;但发现 P1-D 暴露的旧数据 noise(7 个 orphan SKILL.md 路径,reconciler 4 元组 compat match 因 source 不一致 → INSERT 撞 unique),临时 SQL `DELETE FROM t_skill WHERE id IN (33,65,66,129,161)` 后 Rescan 触发 reconciler RescanReport[created=5,updated=5,shadowed=2],dashboard 显示恢复正常;3 个磁盘 orphan(`filesystem/legacy/clawhub/github/latest` + 2 个 P1-C evolution fork 副本)留 follow-up;`/tmp/nits-followup-p1-d.md` 含 7 条 follow-up(reconciler 4 元组 match 改宽松 / native SQL IN 参数化 / rescan auth / 双重 reconcile / 并发 rescan 幂等 / Tooltip 可见性断言强化 / axios.isAxiosError guard 等)|
| 2026-04-30 | CTX-1 三档触发接全量估算 + 阈值配置化 + 撞窗 retry：新建 `RequestTokenEstimator`(共享估算算法,覆盖 system+messages+tools+maxTokens)、`CompactThresholds`(per-provider 配置)、`LlmContextLengthExceededException`(撞窗错误识别);改造 `AgentLoopEngine` 三档 compact 触发(B1/B2/preemptive 共用 `RequestTokenEstimator`,阈值改读 `provider.getCompactThresholds()`)+ chatStream 撞窗 retry 单次(while + retried flag);`ContextBreakdownService` 加 `output_reserved` 段并复用 `RequestTokenEstimator.estimateToolSchemas` 与 engine 算法对齐;`Claude/OpenAiProvider` 加 `detectContextOverflow()` + thresholds 注入;`LlmProperties` 嵌套 `CompactThresholds` YAML;Mid Pipeline(用户授权降档)走完 1 轮 review 对抗 + Judge NEEDS_FIX 一次 fix;新增 9 个测试文件(单元 + 集成共 25 tests),含 `EngineEstimateMatchesBreakdownIT` 跨路径比对 AC-1 regression guard | [CTX-1 技术方案](requirements/archive/2026-04-30-CTX-1-context-token-accounting/tech-design.md) | [CTX-1 PRD](requirements/archive/2026-04-30-CTX-1-context-token-accounting/prd.md)、[CTX-1 MRD](requirements/archive/2026-04-30-CTX-1-context-token-accounting/mrd.md)、Reviewer/Judge 报告在 `/tmp/review-be{1,2}-r1.md` + `/tmp/judge-r1.md`(保留作审计) | `1de1fe1`;无 schema 改动;`mvn test -pl skillforge-core` 141/141 + `mvn test -pl skillforge-server -Dtest=<critical>` 70/70 全绿;known minor:dashboard segment-of-parts vs engine whole-string BPE boundary noise(±5 tokens/边界,segment 模型固有副作用,follow-up 不阻塞);AC-7 阈值校准跑改长会话观察 `/tmp/nits-followup-ctx-1-token-accounting.md` 5 条 follow-up |
| 2026-04-29 | OBS-1 Session × Trace 合并详情视图：新建 `skillforge-observability` module（`LlmTraceStore` / `BlobStore` / `TraceLlmCallObserver` / `ObservabilityJacksonConfig` / `LegacyLlmCallEtlService`）+ 4 个 server controller（`SessionSpansController` / `LlmSpanController` / `ToolSpanController` / `LlmTraceController`）+ `SessionSpansService` / `SubagentSessionResolver` / `ObservabilityOwnershipGuard` + dashboard `SessionDetail` / `SessionTimelinePanel` / `SpanDetailPanel` / `LlmSpanDetailView` / `ToolSpanDetailView` / `PayloadViewer` / `SubagentJumpLink`；同步修 6 处 plan 外补丁（JSONB `@JdbcTypeCode`、Cycle dependency split、ETL placeholder、V36/V37 dedup、Message.getTextContent tool_result、4 controller user_id 过滤） | [OBS-1 技术方案](requirements/archive/2026-04-29-OBS-1-session-trace/tech-design.md) | [ToDo](todo.md)、Plan/Review/Judge 文档系列在 `/tmp/plan-r{1,2,3}.md` + `/tmp/review-{be,fe,judge}-r{1,2}.md`（保留作审计） | `ffa2248`(主 feature commit) + `f768a7b`(session-detail 视图样式与交互 polish) + `9fe87d6`(docs archive + delivery index + todo) + `21e4561`(close BUG-25/BUG-26 via OBS-1)；`V33` / `V35` / `V36` / `V37` + `R__migrate_legacy_llm_call.sql`；core 111 tests + observability 5 tests + server 539+ tests；用户 dashboard 实测 cross-user 403 + 数据展示 + Tool output 渲染 |
| 2026-04-27 | Memory v2（PR-1 ~ PR-5）：`V29` schema、L0/L1 task-aware recall、增量抽取 cursor、embedding add-time dedup、状态机/淘汰/UI 收口 | [Memory v2 技术方案](requirements/archive/2026-04-27-MEMORY-v2/tech-design.md) | [ToDo](todo.md) | `9f36b59` / `8330d32` / `86703ed` / `96676b9` + 当前 workspace PR-5 收尾；`V29`; `mvn -pl skillforge-server -am -Dtest=MemoryConsolidatorTest,MemoryServiceTest -Dsurefire.failIfNoSpecifiedTests=false test` 24/24；`skillforge-dashboard npm run build` |
| 2026-04-26 | P15-4 Agent 配置自省 + UpdateAgent：`GetAgentConfig`、`AgentDiscovery` 增强、带一次确认的 `UpdateAgent` | 无独立方案；属于 P11/P15 自省工具补齐，并复用 [SEC-2 技术方案](requirements/archive/2026-04-25-SEC-2-hook-source-protection/tech-design.md) 的 hook 来源隔离边界 | [ToDo](todo.md) | 本次提交；`mvn -pl skillforge-server -am -DskipTests compile`；`UpdateAgentToolTest` + `AgentLoopEngineInstallConfirmationTest` 16/16；server 重启确认工具注册 |
| 2026-04-26 | Agent 管理与内置模板 SQL 化收口：`CreateAgent` Tool、Code/Main Agent Flyway seed、Main Assistant leader config | 无独立方案；确认流复用 [Install Confirmation 技术方案](requirements/archive/2026-04-26-INSTALL-confirmation-flow/tech-design.md) 的 human confirmation pattern | [ToDo](todo.md) | `e22609c` / `3243759` / `d9d82a3` / `3542fa6`；`V26` / `V27` / `V28`；418 non-IT server tests；本地 DB 已到 v28 |
| 2026-04-26 | BUG-F Compact 摘要存储重构，修复 mixed user/tool_result 导致 OpenAI-compatible provider 400 | 无独立方案；关联 [P6 消息行存储技术方案](requirements/archive/2026-04-21-P6-session-message-storage/tech-design.md)、[P9 Tool 输出裁剪技术方案](requirements/archive/2026-04-22-P9-tool-result-compaction/tech-design.md) | [Bug 列表](bugs.md)、[ToDo](todo.md) | `e9b48f3`；370 unit tests；server 重启验证 |
| 2026-04-26 | Sprint 1：P9-7 token 估算、P3-1/P3-3 memory snapshot、P13-3 清理 | [P9 Tool 输出裁剪技术方案](requirements/archive/2026-04-22-P9-tool-result-compaction/tech-design.md) 覆盖 P9 方向；其余为防腐补丁，无独立方案 | [ToDo](todo.md) | `621f417` / `f4773c3`；397 non-IT server tests |
| 2026-04-26 | Sprint 2 PR1/PR2：AgentDiscovery、name resolver、visibility、custom rule severity、GetTrace、GetSessionMessages | 无独立方案；属于 P11/P15 收窄实现 | [ToDo](todo.md) | `f48b61d`；413 non-IT server tests |
| 2026-04-25 | SEC-2 Hook Source Protection + Agent-authored Hook Binding V1 | [SEC-2 技术方案](requirements/archive/2026-04-25-SEC-2-hook-source-protection/tech-design.md) | [N3 Lifecycle Hooks 技术方案](requirements/archive/2026-04-17-N3-lifecycle-hooks/tech-design.md) | `V24`; SEC-2 tests 27/27；frontend build |
| 2026-04-25 | thinking-mode-v1：per-agent thinkingMode/reasoningEffort/provider protocol family | 无独立方案；实现记录在 [ToDo](todo.md) | [ToDo](todo.md) | `55969db`；357 backend tests；13 frontend tests |
| 2026-04-24 | `Message.reasoning_content` 持久化，修复 thinking + tool_use 下一轮回放 | 无独立方案 | [ToDo](todo.md) | `V22`; 310 non-IT server tests |
| 2026-04-24 | 引擎稳定性：Compact breaker 误触、LLM stream 抗抖、provider HTTP client split | 无独立方案；问题记录归档在 [Bug 列表](bugs.md) | [ToDo](todo.md) | `121e8dc`；364 non-IT server tests |
| 2026-04-22 | P13-2 Hook 编辑器重写 | [N3 Lifecycle Hooks 技术方案](requirements/archive/2026-04-17-N3-lifecycle-hooks/tech-design.md) | [ToDo](todo.md) | 38 tests；browser e2e |
| 2026-04-22 | P13-5/7/8/10/11 follow-up batch | 无独立方案；前端/后端体验补丁 | [ToDo](todo.md) | 见 todo 完成记录 |
| 2026-04-22 | P9-1 Compactable 工具白名单 | [P9 Tool 输出裁剪技术方案](requirements/archive/2026-04-22-P9-tool-result-compaction/tech-design.md) | [ToDo](todo.md) | 25 tests |
| 2026-04-22 | P9-3 Time-based 冷清理 | [P9 Tool 输出裁剪技术方案](requirements/archive/2026-04-22-P9-tool-result-compaction/tech-design.md) | [ToDo](todo.md) | 12 tests |
| 2026-04-22 | P9-6 Session Memory 零 LLM 压缩 | [P9 Tool 输出裁剪技术方案](requirements/archive/2026-04-22-P9-tool-result-compaction/tech-design.md) | [ToDo](todo.md) | 8 tests |
| 2026-04-21 | P8 记忆 LLM 提取 | 无独立方案；与 memory 系统相关 | [N1 Memory 向量检索技术方案](requirements/archive/2026-04-16-N1-memory-vector-search/tech-design.md)、[ToDo](todo.md) | 19 unit tests |
| 2026-04-21 | Session 批量删除 | 无独立方案 | [ToDo](todo.md) | Full Pipeline |
| 2026-04-21 | Session 来源渠道可视化 | 无独立方案；关联 channel gateway | [P2 Channel Gateway 技术方案](requirements/archive/2026-04-20-P2-channel-gateway/tech-design.md)、[ToDo](todo.md) | 前后端实现记录见 todo |
| 2026-04-21 | Context breakdown API + 实时 token 面板 | 无独立方案 | [ToDo](todo.md) | Reviewer pass；前后端修复记录见 todo |
| 2026-04-21 | Session 详情 drawer 整体打磨 | 无独立方案；前端体验补丁 | [P5 前端体验技术方案](requirements/archive/2026-04-19-P5-frontend-optimization/tech-design.md)、[ToDo](todo.md) | 前端实现记录见 todo |
| 2026-04-21 | P7 飞书 WebSocket 长连接模式 | [P7 飞书 WebSocket 技术方案](requirements/archive/2026-04-21-P7-feishu-websocket/tech-design.md) | [P2 Channel Gateway 技术方案](requirements/archive/2026-04-20-P2-channel-gateway/tech-design.md) | Feishu WS dispatcher / push manager tests；E2E 验证 |
| 2026-04-21 | P6 消息行存储重构 | [P6 消息行存储技术方案](requirements/archive/2026-04-21-P6-session-message-storage/tech-design.md) | [P6 灰度手册](operations/p6-rollout-playbook.md) | `V18`; rollout toggle / checkpoint APIs |
| 2026-04-20 | P2 多平台消息网关：飞书 + Telegram + ChannelAdapter SPI | [P2 Channel Gateway 技术方案](requirements/archive/2026-04-20-P2-channel-gateway/tech-design.md) | [P2 Channel Gateway Plan A 存档](requirements/archive/2026-04-20-P2-channel-gateway/reviews/plan-a.md) | `V17`; Full Pipeline |
| 2026-04-20 | P1 Skill 自动生成 + 自进化 | [P1 Self-Improve 技术方案](requirements/archive/2026-04-20-P1-self-improve-pipeline/tech-design.md) | [Eval 方法论](references/design-eval-methodology.md) | Skill versioning / A-B / evolution run migrations |
| 2026-04-19 | P4 Code Agent | [P4 Code Agent 技术方案](requirements/archive/2026-04-19-P4-code-agent/tech-design.md) | [SEC-2 技术方案](requirements/archive/2026-04-25-SEC-2-hook-source-protection/tech-design.md) | `V10` / `V11`; seed 后续统一迁到 `V26` |
| 2026-04-19 | P5 前端体验优化 | [P5 前端体验技术方案](requirements/archive/2026-04-19-P5-frontend-optimization/tech-design.md) | [前端视觉参考](references/design-references.md) | 前端 10 页面重构 |
| 2026-04-17 | N3 P2 Lifecycle Hook Method 体系 | [N3 Lifecycle Hooks 技术方案](requirements/archive/2026-04-17-N3-lifecycle-hooks/tech-design.md) | [N3 P2 Review A](requirements/archive/2026-04-17-N3-lifecycle-hooks/reviews/n3-p2-hook-method-review-a.md), [N3 P2 Review B](requirements/archive/2026-04-17-N3-lifecycle-hooks/reviews/n3-p2-hook-method-review-b.md), [N3 P2 Judge](requirements/archive/2026-04-17-N3-lifecycle-hooks/reviews/n3-p2-hook-method-judge.md) | 202 backend tests |
| 2026-04-17 | N3 P1 Lifecycle Hook 完善 | [N3-P1 Lifecycle Hook 增强技术方案](requirements/archive/2026-04-17-N3-P1-lifecycle-hook-enhancement/tech-design.md) | [N3-P1 Plan A](requirements/archive/2026-04-17-N3-P1-lifecycle-hook-enhancement/reviews/n3-p1-lifecycle-hook-plan-a.md), [N3-P1 Plan B](requirements/archive/2026-04-17-N3-P1-lifecycle-hook-enhancement/reviews/n3-p1-lifecycle-hook-plan-b.md), review/judge docs | 145 backend tests + browser e2e |
| 2026-04-17 | N3 P0 用户可配置 Lifecycle Hook | [N3 Lifecycle Hooks 技术方案](requirements/archive/2026-04-17-N3-lifecycle-hooks/tech-design.md) | [会话状态 / ask mode 技术方案](requirements/archive/2026-04-17-SESSION-state-ask-mode/tech-design.md) | `V9`; 25 tests + browser e2e |
| 2026-04-17 | N2 Agent 行为规范层 | [N2 行为规范技术方案](requirements/archive/2026-04-17-N2-behavioral-rules/tech-design.md) | - | `V8` |
| 2026-04-16 | N1 Memory 向量检索 | [N1 Memory 向量检索技术方案](requirements/archive/2026-04-16-N1-memory-vector-search/tech-design.md) | - | `V7`; graceful fallback |
| 2026-04-16 | P2-6 Session → Scenario 转换 | [P1 Self-Improve 技术方案](requirements/archive/2026-04-20-P1-self-improve-pipeline/tech-design.md) | [Eval 方法论](references/design-eval-methodology.md) | `V5` |
| 2026-04-16 | P2-1~P2-5 Self-Improve Pipeline Phase 2 | [P1 Self-Improve 技术方案](requirements/archive/2026-04-20-P1-self-improve-pipeline/tech-design.md) | [Eval 方法论](references/design-eval-methodology.md) | `V4` |
| 2026-04-16 | #5/#6 Self-Improve Pipeline Phase 1 + 完整方案设计 | [P1 Self-Improve 技术方案](requirements/archive/2026-04-20-P1-self-improve-pipeline/tech-design.md) | [Eval 方法论](references/design-eval-methodology.md) | `V3`; 13 scenario JSON |
| 2026-04-16 | Collab-3 CollabRun WS 广播 | [SubAgent 异步派发技术方案](requirements/archive/2026-04-16-SUBAGENT-async-dispatch/tech-design.md) | - | Teams page WS invalidate |
| 2026-04-15 | Compact-2 CompactionService 解锁 LLM 调用 | 无独立方案；后续由 P6/P9 文档承接 | [P6 消息行存储技术方案](requirements/archive/2026-04-21-P6-session-message-storage/tech-design.md), [P9 Tool 输出裁剪技术方案](requirements/archive/2026-04-22-P9-tool-result-compaction/tech-design.md) | 3-phase split |
| 2026-04-15 | Context-1 动态 Context Window | 无独立方案 | [ToDo](todo.md) | ModelConfig lookup |
| 2026-04-15 | 认证鉴权 MVP、API 响应统一、前端测试基础、前端安全、后端 IT、DTO 现代化、Chat 交互、Dashboard 视觉、长列表虚拟滚动、TanStack Query、Chat.tsx 重构、Teams 页面、ObjectMapper 修复 | 多个早期小项，无独立方案 | [2026-04-15 项目状态](references/project-status-2026-04-15.md), [ToDo](todo.md) | 早期交付批次 |

---

## 技术方案文档状态

| 文档 | 角色 | 当前状态 |
| --- | --- | --- |
| [P1 Self-Improve 技术方案](requirements/archive/2026-04-20-P1-self-improve-pipeline/tech-design.md) | Self-Improve / Eval / Scenario 主方案 | 已交付主线，仍作为后续 eval/skill evolution 参考 |
| [Eval 方法论](references/design-eval-methodology.md) | Eval 方法论 | 参考文档 |
| [N1 Memory 向量检索技术方案](requirements/archive/2026-04-16-N1-memory-vector-search/tech-design.md) | N1 Memory 向量检索 | 已交付 |
| [Memory v2 技术方案](requirements/archive/2026-04-27-MEMORY-v2/tech-design.md) | Memory v2 写入 / 召回 / 淘汰一体化重构 | 已交付 |
| [N2 行为规范技术方案](requirements/archive/2026-04-17-N2-behavioral-rules/tech-design.md) | N2 行为规则 | 已交付 |
| [N3 Lifecycle Hooks 技术方案](requirements/archive/2026-04-17-N3-lifecycle-hooks/tech-design.md) | N3 P0/P2 Lifecycle Hooks 总方案 | 已交付，后续 hook 相关继续复用 |
| [N3-P1 Lifecycle Hook 增强技术方案](requirements/archive/2026-04-17-N3-P1-lifecycle-hook-enhancement/tech-design.md) | N3 P1 最终实施方案 | 已交付 |
| [P4 Code Agent 技术方案](requirements/archive/2026-04-19-P4-code-agent/tech-design.md) | P4 Code Agent | 已交付；seed 机制已由 Java initializer 修正为 Flyway |
| [P5 前端体验技术方案](requirements/archive/2026-04-19-P5-frontend-optimization/tech-design.md) | P5 前端体验 | 已交付，仍作为 UI 风格参考 |
| [P6 消息行存储技术方案](requirements/archive/2026-04-21-P6-session-message-storage/tech-design.md) | P6 消息行存储 | 已交付，仍是 session persistence 参考 |
| [P9 Tool 输出裁剪技术方案](requirements/archive/2026-04-22-P9-tool-result-compaction/tech-design.md) | P9 Tool 输出裁剪 / 压缩 | 部分已交付；P9-2 仍在 Sprint 3 |
| [P7 飞书 WebSocket 技术方案](requirements/archive/2026-04-21-P7-feishu-websocket/tech-design.md) | P7 飞书 WS | 已交付 |
| [P2 Channel Gateway 技术方案](requirements/archive/2026-04-20-P2-channel-gateway/tech-design.md) | P2 Channel Gateway 采纳方案 | 已交付 |
| [P2 Channel Gateway Plan A 存档](requirements/archive/2026-04-20-P2-channel-gateway/reviews/plan-a.md) | P2 Channel Gateway 被替代方案 | 存档参考 |
| [SEC-2 技术方案](requirements/archive/2026-04-25-SEC-2-hook-source-protection/tech-design.md) | SEC-2 Hook 来源保护 | 已交付 |
| [Install Confirmation 技术方案](requirements/archive/2026-04-26-INSTALL-confirmation-flow/tech-design.md) | Install confirmation 授权流 | 已交付并被 CreateAgent confirmation 复用部分 pattern |
| [会话状态 / ask mode 技术方案](requirements/archive/2026-04-17-SESSION-state-ask-mode/tech-design.md) | 会话状态 / ask mode | 参考文档 |
| [SubAgent 异步派发技术方案](requirements/archive/2026-04-16-SUBAGENT-async-dispatch/tech-design.md) | SubAgent 异步派发 | 已交付，仍是协作链路参考 |
| [BrowserSkill 登录态技术方案](requirements/archive/2026-04-17-BROWSER-skill-login/tech-design.md) | BrowserSkill 登录态 | 设计/参考文档，未在 todo 主表中单独追踪 |
| [前端视觉参考](references/design-references.md) | 前端视觉参考 | 参考文档 |
