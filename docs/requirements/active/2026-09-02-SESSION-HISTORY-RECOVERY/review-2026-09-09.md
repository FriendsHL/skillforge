# 历史精确事实恢复 — 2026-09-09 Review 收口记录

范围：摘要和未压缩尾部不足时，通过 SessionHistorySearch → SessionHistoryRead 恢复当前 Session 原始证据。
本轮基于未提交工作区修复，不代表整个 durability/Batch 8 已验收，不修改生产开关，不提交或部署。

## Review 与修复

| 问题 | 修复 |
| --- | --- |
| 原始工具 content 为 TextNode 时 JSON secret key 未脱敏 | 与 archive 字符串走同一递归投影；projection version 升至 2，旧 cursor 明确失效 |
| legacy-only / mixed 会话追加新 row 后绕过回填检查 | admission 写入前、History scope 读取时验证完整 legacy 镜像前缀；不使用 rowCount 代替验证 |
| master-on 与 row-read/write 关闭组合读写不一致 | 启动配置校验拒绝不一致组合 |
| History 工具派生正文再次进入 Full Compact | 按调用 occurrence 保留两侧 name/id 占位符，排除派生正文，保留普通 siblings 和源 JSON |
| 超过 100K 后精确 ref/窄范围/tail/around 均失败 | selector 下推，窗口按 seq 分批推进，补齐前驱配对上下文，canonical archive 仍按 occurrence 验证 |
| 复审：prune 后 restore 镜像导致错误拒绝 | 实际 pruned 行允许严格等价的既有脱敏投影；普通内容及其他字段仍须一致 |
| 复审：malformed 重用 ID 的 intent 可遮蔽 History 配对 | SQL 与 Java block decode 条件对齐，Compact 同步校验 payload 存在 |

独立审查覆盖检索/安全、接入/配置和 Compact；复审发现的问题纳入修复。所有数据写入沿既有事务和权限边界，
未新增迁移、数据库持久缓存或第二份事实库。

## 验证记录

- 原始缺陷 RED：TextNode 脱敏、100001 行窄查询、legacy mixed、非法 flags、Compact 派生正文均按预期失败。
- Compact 最终聚焦：`mvn -pl skillforge-core -Dtest=FullCompactStrategyIncrementalTest test`，10 tests，零失败，`BUILD SUCCESS`。
- 后端联合聚焦：55 tests，零 failure/error/skip，`BUILD SUCCESS`。含真实 PostgreSQL 的 100001 行
  range/ref/tail/around/archive、malformed intent、legacy/pruned 前缀、admission；日志 `/tmp/history-review-final-green.log`。
- 100K 完整 legacy 镜像单次验证实测 158 ms；这是本机单次样本，不作为 p95 质量门。
- 前端：History decoder/card/live/replay 15 tests 通过；Chromium `session-history-recovery.spec.ts` 1 test 通过。
  浏览器使用模拟 API，并非真实模型/生产 Session 闭环。
- 全量：`TESTCONTAINERS_RYUK_DISABLED=true mvn -pl skillforge-server -am test`，**BUILD FAILURE**。
  server 4012 tests / 6 failures / 105 errors / 20 skips；日志 `/tmp/history-fix-full-regression.log`。
  不因本轮聚焦通过而把全量改记为成功。
- 已提交 HEAD `65c78d59e852b8debf78a363da78f98cf3ac6974` 的隔离基线：11 类、59 tests，
  6 failures / 25 errors / 0 skips，失败方法和数量与当前树一致。覆盖 Eval、Canary、Evolve、Flywheel、
  SessionAnnotation、OptReport、OriginFilter 和 Memory。另两个 Workflow 测试在纯 HEAD 也因找不到
  `@SpringBootConfiguration` 失败。基线只补本机 Docker host/API 1.44 环境，不带入业务修改。
- 全量后段大批 PG 上下文初始化失败原因是共享库连接数耗尽。以 `-Dspring.test.context.cache.maxSize=4`
  定向重跑后，77 个 durable PostgreSQL 用例不再出现该错误；同组两个 Workflow 引导错误仍存在，
  因此该命令整体仍为 BUILD FAILURE，不能当作完整门禁通过。
- 独立复审已确认本轮代码修复中的 pruned 误拒、native/Java 配对识别不一致和 Compact payload 条件均已修正。
- `git diff --check` 通过，临时 Testcontainers 已退出；未改生产开关、未提交或部署。

### 全量暴露的其他 durable 验收缺口

这些路径未在本轮修改，也不归为纯 HEAD 已复现的旧模块问题：

1. `SessionInteractiveControlTransactionServicePostgresIT` 中一个用例要求租约过期后只读回 ACK
   （`dispatchGranted=false`），实现却调用通用 claim 的活跃租约校验而拒绝。需要单独收敛
   ACK 回读语义；不能通过放松通用过期 claim 校验来让测试变绿。
2. `AgentLoopEngineDurableResultBarrierTest.userCancelDuringRunningToolFailsOpen_withoutResultCommitOrProviderContinuation`
   在首次全量和一次定向运行通过，在另两次运行出现预期异常为 null 的失败。已记录为未解释的
   取消路径稳定性问题，未延长等待或删除断言来掩盖。

因此本轮专项代码修复有聚焦验证，**整个需求的 Full/release gate 仍未通过**。

## 尚未关闭的验收门

- 宽泛关键词 Search 仍有每来源 100K 扫描预算，continuation 仍重扫冻结范围；完整跨来源 keyset 优化未在本轮实现。
  超限时可以使用真正下推的 seq 范围或精确 refs，不能将本轮结果表述为“任意长度全历史搜索已完成”。
- 单条 SQL 镜像校验避免分页/count N+1，但仍需读取和比较镜像；大正文、工具密集会话性能须持续观察。
- Ark/百炼账号阻塞已通过改用当前主 Agent 的 DeepSeek 直连完成 smoke；完整真实会话与 A-D 质量验收仍开放。
- 新 smoke 使用合成存储和 scope；真实模型全链路、完整 A-D/S1–S25 对照与整个 durability/crash/回滚验收仍开放。
- 2026-09-04 离线脚本结果属于 projection 1 的历史校准，不改写为本轮 projection 2 的真实模型结果。


## 主 Agent 切换 DeepSeek 后的复测

2026-09-09，从本机数据库只读确认 Main Assistant（id=3）模型为 `deepseek:deepseek-v4-pro`，
随后以同 provider/model 执行 opt-in `SessionHistoryLiveProviderIT`，日志 `/tmp/history-live-deepseek.log`。

- **BUILD SUCCESS**：1 test，0 failures/errors/skips；包含两个模型行为场景。
- 缺失事实：模型依次产生 5 次 `SessionHistorySearch` 和 1 次 `SessionHistoryRead`，最终精确返回随机生成、仅存于历史夹具的编号。
- 信息充分：从当前尾部直接回答，History 调用为 0。
- 两场景合计 5 次 provider 请求；本次样本的多次 Search 效率需要后续观察。
- 使用真实 Provider 与生产 query/tool/wire，存储和 scope 为合成夹具；未运行完整生产 AgentLoopEngine/Compact/持久恢复链路。

结论更新：**真实模型 smoke 已通过**，不再将账号额度视为该 smoke 的当前阻塞；Full/release gate、两项 durable 验收问题及完整质量实验仍开放。

## Search 效率专项（2026-09-09，用户调整范围）

用户要求优先 Search；上述 ACK 回读与取消稳定性两项暂停保留，本轮未修改其实现或测试。

### 实测与本轮修改

在 opt-in 合成夹具 smoke 中增加每次工具的参数、命中数、耗时，以及 provider 耗时/usage 日志；
固定 Search/Read schema 顺序，并提供仅测试使用的 `HISTORY_LIVE_SEARCH_BASELINE=true` 旧说明对照。
日志只包含合成历史，未增加生产查询正文日志。生产修改仅为 Search schema 的两处 description：
明确不区分大小写的完整字面子串匹配、空格不是分词；先一次查询，读结果后再追加；命中后批量 Read；
空结果可缩短片段或放宽筛选。未改检索、排序、授权、cursor 或 Read 语义。

旧说明与第一版新说明交替运行各 3 次；随后最终说明再运行 3 次，均为真实 deepseek-v4-pro、temperature=0，
同结构夹具，每次随机编号。最终版未交替执行；样本很小，缓存、服务端和随机波动未控制，不能推导普遍提速。

| 版本 | 三次 Search 数 | 每次 Read 数 | 每次 provider 请求数（含尾部充分场景） | provider 总耗时/次 |
| --- | --- | --- | --- | --- |
| 旧说明 | 2 / 2 / 2 | 1 | 4 | 8.479 / 6.584 / 7.829 s |
| 第一版说明 | 2 / 1 / 1 | 1 | 4 | 9.985 / 8.349 / 7.561 s |
| 最终说明 | 1 / 2 / 2 | 1 | 4 | 7.346 / 9.023 / 9.351 s |

九次均精确恢复随机编号，尾部充分场景均 0 History。旧版重复查询为“紫杉（项目）”与“发布编号”，
两者返回同样两个候选，发生在同一 provider 轮。最终版仍有并行重复查询；**说明只能改善指引，未证明稳定减少轮数或耗时**。
原始五次 Search 的 smoke 未记录参数，不把本次轨迹反推为其确定根因。
本机两行夹具的单次 Search 约 0–26 ms；该证据不代表真实长 Session 的数据库性能。

验证：最终三次 live 均各 1 test、零失败，BUILD SUCCESS；
`SessionHistoryToolSchemasTest,SessionHistoryQueryServiceTest,SessionHistoryToolsTest,SessionHistoryRecoveryEvalTest`
共 25 tests，零 failures/errors/skips，BUILD SUCCESS。两工具 schema 的 cl100k 估算为 1104 tokens，
通过既有预算门禁（不等同于 DeepSeek 实际计费 token）。日志：
`/tmp/history-search-comparison.log`、`/tmp/history-search-final-live.log`、`/tmp/history-search-final-tests.log`。

### 后续优化优先级（未实现）

1. 长 Session 首先测扫描行数、投影字符数、SQL/投影/排序各耗时和页数。当前 Search 的 limit 只是输出上限；
   无 seq 条件时仍加载来源预算内正文、投影、过滤并排序，每个 cursor 页重复这一过程。
   优先设计跨来源 keyset/候选分页，保持 ORIGINAL 优先、冻结 cutoff、授权投影和配对排除；
   不能简单把 limit 下推到未过滤行，也不能用 raw secret 字段命中来替代授权正文匹配。
2. preview 当前取前 240 code points；长正文命中位置较后时可改为命中附近片段，减少候选辨识成本。
   应以中部命中/Unicode/工具名命中场景验证；本轮短夹具未证明其是当前重复搜索原因。
3. 在代表性长历史上比较扫描量、p50/p95、Search/Read/provider 轮数及精确恢复率，
   再决定是否启用可重建的授权投影索引。向量检索不属于当前首版范围。

## 本机重新部署与真实浏览器 E2E（2026-09-09 18:26）

用户要求重启前后端并执行端到端测试。已完成构建、旧 JVM 完全退出确认、独立 jar 启动及 Vite 重启。

### 最终运行状态

- 后端：8080，PID 27717，`~/.skillforge/runtime/skillforge-server-history-summary-20260909.jar`。
- 前端：3000，Vite PID 26621，沿用本机原开发服务方式；`npm run build` 成功，未改为生产静态托管。
- 运行参数：`--skillforge.session-history.enabled=true`、`--skillforge.session-history.checkpoint-envelope-enabled=true`、
  `--skillforge.llm.default-provider=deepseek`、`--skillforge.llm.providers.deepseek.model=deepseek-v4-pro`。
  这些是本次启动覆盖，未改 application.yml 的默认 rollout 配置；后续启动需保留参数。
- 默认 provider 切换原因：Main Agent 虽已是 DeepSeek，Full Compact 仍使用全局默认 Ark，实跑返回
  HTTP 400 / InvalidSubscription。默认 provider 改为 DeepSeek 后，压缩和其他依赖全局默认 provider 的辅助调用会使用它。
- 重启前备份：`~/.skillforge/backups/pre-history-e2e-20260909.dump`，pg_dump exit 0。
- 后端日志：`~/.skillforge/logs/server-history-20260909.log`；前端日志：`~/.skillforge/logs/dashboard-history-20260909.log`。

### E2E 暴露并修复的摘要问题

DeepSeek v4 的摘要请求未明确关闭 thinking，2200-token 输出预算耗尽后返回 `max_tokens` 且正文为空，
Full Compact 因此 no-op。`FullCompactStrategy.callLlmWithSystem` 现在显式设置 `ThinkingMode.DISABLED`，
覆盖 full、分块/合并、incremental 摘要，不修改主对话 thinking 设置。

新增回归先 RED（expected DISABLED / actual null），修复后相关测试 **119 tests = core 47 + server 72**，
零 failures/errors/skips，BUILD SUCCESS；包括 full/incremental、provider thinking 协议及服务编排测试。
独立静态审查无阻塞。对不支持关闭 thinking 的模型，现有 provider 仍忽略设置，不宣称全部 reasoning 模型适用。

首次短夹具还分别触发 window<4 和无净 token 回收保护，均未记为压缩成功；补足可压缩历史后验证成功。
没有修改保护阈值、人工改写摘要、注入工具结果或绕过真实模型/数据库。

### 完整链路证据

独立测试 Session：`9e226fdc-67d8-4987-834d-f88eefd2aae4`，既有 Main Assistant / Agent 3，
所有聊天轮次由真实 Chat API/AgentLoopEngine 产生。准备 120 个随机虚构编号和可压缩的页面检查记录；
从 Chromium 页面点击 Full Compact、发送恢复问题，再检查实时 UI、刷新持久化和 Replay。

- 实际 compaction event **1355**：摘要范围 seq 0–25，保留 20 条尾部；46 帧变为 21 帧，
  **10952 → 2228 tokens，回收 8724**。原始消息仍保存在 row store。
- 120 个编号中有 118 个未进入摘要/尾部。选取遗漏项“青桐验收批次073”，原编号 `QT-937297a55f0d`。
  提问只提供批次名，不提供答案或指定工具调用序列。
- 实际调用：seq47 Search `query=青桐验收批次073`，seq49 Read `refs=[msg:e0:id107878:block0]`。
  **1 Search + 1 Read**，各有配对 result；Search 返回当前问题和 seq0 原始清单两个候选，模型读取原始清单。
- Trace `374474c4-b5dc-46d5-a4c0-c11e845ae058`：`ok`，总耗时 **5959 ms**；
  Search span **31 ms**、Read **25 ms**，3 个模型请求分别为 2939 / 1804 / 1046 ms。
  这是单次本机合成业务数据 E2E，不作为长历史 p95 或说明改动普遍提速证据。
- 三次实际 provider request blob 中目标编号出现情况为 **false / false / true**：Read 前确实没有答案；
  Read 后才获得原始事实。三次请求均包含本轮 Search 新说明，证明新代码已加载。
- 最终回答精确为“青桐验收批次073：QT-937297a55f0d”；实时页面、API/SQL、刷新后页面一致。
  Search 结果卡正确显示 ORIGINAL/ref/seq，Replay 正确显示 Search 与 Read evidence 卡。
- 信息充分对照：页面输入 `TAIL-QT-9042`，直接得到同样编号；仅新增一条 user 和一条 assistant，History 调用 **0**。
- 最终 Session 为 idle、runtimeError=null、recoveryState=none；54 行 seq0–53 连续无重复；
  两个 History durable attempts 均 RESULTS_COMMITTED。

本机验收资产：`/tmp/history-e2e-20260909/` 内保存 preparation/API/SQL 脚本、随机清单、摘要/尾部快照、
请求存在性检查、span 元数据、最终消息及 RED/GREEN/build 日志。
前端构建日志 `/tmp/history-redeploy-frontend-build.log`。测试 Session 保留供页面复核，未新增临时 Agent。

本次真实浏览器→ChatService/Engine→Compact→Search/Read→PostgreSQL→页面回放专项通过；
此前暂停的 ACK 回读与取消稳定性两项保持未关闭，未把本次结果当作整个需求 Full/release gate 或 crash recovery 验收。
未提交或推送。
